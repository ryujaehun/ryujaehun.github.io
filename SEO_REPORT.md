# SEO 감사 및 개선 기록 — 2026-09-18 ~ 19

jaehun.me 의 구글 검색 노출과 글 품질을 놓고 감사 → 개선 → 실측을 돌린
기록이다. 커밋 11 개, 파일 556 개가 바뀌었다.

숫자는 전부 **렌더 결과에서 직접 센 값**이다. 소스에서 센 것은 그렇다고
따로 적었다. 측정에 쓴 스크립트는 세션 스크래치패드에 있고, 재현이
필요하면 아래 "다시 재는 법" 을 보면 된다.

---

## 0. 한눈에

| | 전 | 후 |
|---|---:|---:|
| JSON-LD 구조화 데이터가 있는 페이지 | 0 | 1,489 |
| 프롬프트 문구가 검색 스니펫으로 나가던 페이지 | 391 | 0 |
| 서로 같은 description 을 쓰던 페이지 | 514 | 0 |
| 서로 같은 title 을 쓰던 페이지 | 487 | 78 † |
| h1 이 정확히 하나인 글 페이지 | 165 / 532 | 531 / 532 ‡ |
| 헤딩 레벨을 건너뛴 횟수 | 1,684 | 0 |
| 화면에 `**` 가 글자 그대로 찍힌 횟수 | 3,006 | 10 |
| 한국어 범위 표기가 취소선으로 렌더된 구간 | 158 | 0 |
| 코드 블록에 삼켜져 사라진 절 | 9 | 0 |
| alt 가 없는 본문 이미지 | 43 | 0 |
| 관련 글이 하나도 붙지 않던 글 | 158 | 14 |
| post-lint 문제 (611 편 전수) | 60 | 0 |
| ko 사이트맵 URL | 776 | 608 |
| static/ 용량 | 7.1 MB | 4.4 MB |
| 빌드 경고 | 1 | 0 |

† 남은 78 개는 같은 글의 한국어/영어 쌍이다. hreflang 으로 묶여 있어
  중복으로 취급되지 않는다.
‡ 나머지 하나는 중복 글을 지우고 남긴 리다이렉트 스텁이다.

---

## 1. 기술적 SEO

### 1.1 구조화 데이터 (JSON-LD) — 없던 것을 만들었다

테마는 Hugo 내장 `schema.html` 을 부르고 있었는데, 그건 itemscope 없는
`<meta itemprop>` 만 찍는다. 감싸는 타입이 없으면 마이크로데이터로
성립하지 않아 **Article, BreadcrumbList, Person 중 어느 것도 잡히지
않았다.**

`layouts/_partials/seo/json-ld.html` 을 새로 만들어 하나의 `@graph` 로
묶었다. 노드끼리 `@id` 로 참조하게 해서 글 → 저자 → 사이트가 같은
개체를 가리키는 것으로 읽히게 했다.

| 타입 | 페이지 수 |
|---|---:|
| WebSite | 1,489 |
| Person | 1,489 |
| BreadcrumbList | 1,487 |
| CollectionPage | 876 |
| BlogPosting | 609 |
| ProfilePage | 2 |

1,489 페이지 전부에서 JSON.parse 가 통과한다.

처음에는 `<script>` 안에서 Go 템플릿이 값을 JS 문자열로 감싸 버려
JSON 이 아니라 JSON 문자열이 나갔다. `safeJS` 로 고쳤다.

### 1.2 title 과 description

**문제 1 — 프롬프트 문구가 스니펫으로 나갔다.** 논문 글 391 편의
description 이 본문이 아니라 이렇게 시작했다.

```
논문 링크 Q : 이 논문을 아주 자세하게 읽고 논문의 강점과 독창적인
지점을 설명해주고 핵심 알고리즘을 예시 입력을 들어서 전체적인 과정을
설명해줘 추가적으로 논문의 한계점에 대해서도 알려줘논문 **"Discovering...
```

**문제 2 — 514 페이지가 같은 설명을 돌려썼다.** 태그·카테고리 페이지가
사이트 설명 한 줄("기술과 일상에 대한 생각을 기록하는 블로그입니다.")로
떨어졌다.

**문제 3 — 목록 103 페이지가 같은 제목이었다.** `게시글 | Jaehun's Blog`
가 90 개, `Posts | Jaehun's Blog` 가 13 개.

description 을 `layouts/_partials/seo/description.html` 한 곳에서
결정하게 만들고 meta, OpenGraph, Twitter, JSON-LD 가 같은 문자열을 쓰게
했다 (실측: 네 값의 불일치 0 건). 본문에서 뽑을 때는 코드 펜스, 수식,
프롬프트 헤딩, 첫 줄의 논문 링크를 걷어낸 뒤 첫 문단을 쓴다. 분류
페이지는 용어와 글 수로 문장을 만들고, 목록 2 쪽부터는 제목과 설명에
쪽 번호를 붙인다.

분류 페이지 제목에는 어느 축인지 밝혔다. 같은 말이 태그와 카테고리
양쪽에 있는 경우가 18 개인데 제목이 같으면 서로 자리를 다툰다.

```
Quantization | Jaehun's Blog          (태그·카테고리·ko·en 넷이 동일)
  ->  Quantization 태그의 글 | Jaehun's Blog
      Quantization 카테고리의 글 | Jaehun's Blog
      Posts tagged Quantization | Jaehun's Blog
      Posts in Quantization | Jaehun's Blog
```

**중간에 낸 사고 하나.** 쪽 번호를 붙이려고 `.Paginator` 를 봤는데,
그건 건드리는 것만으로 페이지를 만들어 낸다. 홈 레이아웃은 페이저를
그리지 않는데도 `/page/2` ~ `/page/90` 이 101 개 새로 생겼다. Kind 를
section/taxonomy/term 으로 제한해 되돌렸고, 빌드 통계가 baseline
(KO 393 / EN 65)과 같은지로 확인했다.

### 1.3 얇은 분류 페이지를 색인에서 뺐다

소스에서 세어 보니 태그 181 개 중 132 개가 2 편 이하, 59 개는 1 편뿐이다.
카테고리도 70 개 중 40 개가 2 편 이하다. 글 하나짜리 분류 페이지는 그
글의 제목과 요약을 되풀이할 뿐이라 원글과 검색 결과에서 자리를 다툰다.

3 편 미만인 term 페이지에 `noindex, follow` 를 주고 사이트맵에서 뺐다.
링크는 그대로라 사람이 태그를 눌러 들어오는 길과 크롤러가 글로 가는
길은 유지된다. 사이트맵에서도 빼야 Search Console 에 "제출됐으나
noindex" 경고가 쌓이지 않는다.

| | 전 | 후 |
|---|---:|---:|
| robots: index, follow | 1,489 | 1,191 |
| robots: noindex, follow | 0 | 298 |
| ko 사이트맵 | 776 URL | 608 URL |
| en 사이트맵 | 246 URL | 122 URL |
| 글 페이지 중 noindex 된 것 | 0 | 0 |

### 1.4 이미 괜찮았던 것

감사해 보니 손댈 필요가 없던 항목이다.

- **sitemap** — 언어별 sitemap 과 sitemapindex 가 정상. lastmod 와
  hreflang 대체 링크 포함.
- **robots.txt** — `Sitemap:` 줄 포함. `enableRobotsTXT: false` 로
  내장 템플릿이 덮어쓰지 않게 해 둔 것도 맞다.
- **canonical** — 1,489 페이지 전부 존재.
- **hreflang** — `custom-head.html` 이 번역이 있는 페이지에만 대체 URL
  과 x-default 를 내보낸다.

사이트맵 템플릿에서 폐기된 `.Language.LanguageCode` 를 쓰고 있어 빌드
경고가 하나 나던 것만 `.Language.Locale` 로 맞췄다 (경고 1 → 0).

---

## 2. 시맨틱 HTML

### 2.1 h1 이 페이지마다 여러 개였다

글 제목이 이미 `<h1>` 인데 본문에도 `<h1>` 이 2,707 개 더 있었다.
367 편이 페이지마다 h1 을 둘 이상 내보냈고, 그 h1 들은 대부분
"Q : 이 논문을 아주 자세하게 읽고 ..." 같은 프롬프트 문장이다.

목차에도 영향이 있었다. 목차는 h2~h4 를 담는데 최상위 절이 h1 이라
목차에서 통째로 빠지고, 그 아래 h3 들만 최상위인 척 나열됐다.

세 단계로 고쳤다.

1. 본문 헤딩을 한 단계씩 내렸다 (367 편, 25,849 개)
2. 스택으로 부모를 찾아 자식을 늘 부모+1 로 다시 매겼다 (138 편, 3,331 개).
   원래 레벨의 대소 관계만 쓰므로 글쓴이가 만든 중첩은 바뀌지 않는다
3. 수식 안에 홀로 놓인 `=` 줄 3 곳을 앞 줄에 붙였다. setext 밑줄로
   읽혀 앞 문단이 `<h1 id=endbmatrix>` 가 돼 있었다

| | 전 | 후 |
|---|---:|---:|
| h1 이 정확히 하나인 글 페이지 | 165 / 532 | 531 / 532 |
| 레벨 건너뜀이 있는 페이지 | 308 | 0 |
| 건너뜀 횟수 | 1,684 | 0 |

헤딩 앵커는 본문 글자에서 나오므로 레벨을 바꿔도 그대로다. 기존
딥링크는 깨지지 않는다.

### 2.2 접근성

| 항목 | 전 | 후 |
|---|---|---|
| 링크 글자색 대비비 (라이트 5 스킴) | 1.45 ~ 4.38 : 1 | 4.50 ~ 4.52 : 1 |
| 닫힌 검색 모달 | 탭 순서에 남아 보이지 않는 입력에 포커스 | `visibility: hidden` 으로 제외 |
| 연재 내비 | 배지 3.41, 장 번호 2.05, 현재 칸 3.75 | 모두 AA 통과 |
| "연재 전체 보기" 링크 | 접근 가능한 이름 없음 | `aria-label` |
| 코드 `--chroma-bp` (파이썬 `self`) | 4.34 : 1 | 5.09 : 1 |

링크 색은 `--color-primary` 를 그대로 쓰고 있었는데, 그 값은 버튼
배경이기도 해서 밝게 잡혀 있다. `--color-primary` 는 건드리지 않고 링크
글자 전용으로 `--color-link` 를 두어 색상(C·H)은 유지하고 명도(L)만
옮겼다. 배경은 background / card / muted 셋 모두를 기준으로 확인했다.

| 스킴 (라이트) | 전 | 후 |
|---|---:|---:|
| bumblebee | 1.45 | 4.51 |
| dracula | 2.13 | 4.52 |
| twitter | 2.33 | 4.50 |
| claude (기본) | 3.14 | 4.52 |
| amethyst | 3.64 | 4.51 |
| slate | 4.06 | 4.50 |
| dracula (다크) | 3.98 | 4.51 |

범블비만 변화가 크다 (`#fdc700` → `#9a6600`). 노란 글씨를 흰 바탕에
그대로 두는 쪽이 더 나쁘다고 판단했다.

---

## 3. 콘텐츠 렌더 결함

감사하면서 가장 많이 나온 것은 메타데이터가 아니라 **글이 화면에 제대로
나오지 않는 문제**였다. 렌더를 세어 보고서야 드러났다.

### 3.1 닫히지 않는 굵게 — 348 편, 3,006 곳

CommonMark 규칙 7 은 `**` 가 right-flanking 일 때만 강조를 닫는다고 한다.
한국어는 괄호나 따옴표로 끝난 뒤 조사가 바로 붙는다.

```
**방향성 하우스도르프 거리(directed Hausdorff distance)**로
**프롬프트 마크업 언어(PML)**를
```

앞은 구두점, 뒤는 글자라 두 갈래가 모두 막혀 닫기가 성립하지 않는다.
영어 문장에서는 거의 걸리지 않아 눈에 띄지 않았던 것 같다.

닫히지 않는 구간만 `<strong>...</strong>` 로 바꿨다. 글자는 하나도
바뀌지 않는다. 구두점 판정은 CommonMark 0.31 을 따랐다 — 0.30 까지는
유니코드 P 계열뿐이었지만 0.31 에서 S 계열이 들어왔고 goldmark 가
그쪽이다. 그래서 `**연산량 0.4×**로`, `**FLOPs 28.5 %**로` 도 같은
이유로 깨져 있었다.

`\*\*두 단계\*\*로` 처럼 모델이 강조 기호까지 이스케이프한 것도 48 편
303 곳 있었다.

> 처음엔 `_` 에 붙는 추가 조건을 `**` 에도 적용해 20,689 곳이 잡혔다.
> `**FlexGen**은` 처럼 멀쩡히 렌더되는 것까지 포함된 오검출이었다.
> 규칙 5/7 대로 플랭킹만 보도록 고치자 348 편으로 좁혀져 렌더에서 센
> 페이지 집합과 정확히 일치했다.

**3,006 곳 → 10 곳.** (3.6 의 취소선까지 고친 뒤의 수치다.) 남은 10 곳은
여러 줄에 걸치거나 안쪽에 `*` 가 든 경우, 혹은 원래 짝이 없는 경우다.

### 3.2 코드 펜스가 본문을 삼켰다 — 4 편

짝 없는 펜스 하나가 뒤따르는 모든 여닫이를 뒤집는다.

| 글 | 무슨 일이 있었나 |
|---|---|
| DeepSeekMoE | 닫는 펜스 뒤에 ChatGPT 인용 마커가 붙어 그 줄이 "정보 문자열이 달린 여는 펜스" 가 됐다. `Step 4`, `Step 5` 두 절이 통째로 사라져 있었다 |
| DeepSeekMoE (2) | 386 행의 고아 펜스 하나가 '비밀 병기' 절, '한줄 결론', 비교 표까지 127 줄을 코드 블록에 넣었다 |
| DeepSeek-Prover (ko/en) | 바깥 4 백틱 블록의 닫는 줄 두 개가 서로 바뀌었고, 467 행에 고아 펜스가 하나 더 있었다 |
| DroidSpeak (en) | 본문 전체가 ` ```markdown ` 래퍼 안에 들어가 260 줄이 코드로 렌더됐다. 같은 자리에 모델이 남긴 한국어 안내문("이제 ... 영어로 자연스럽게 번역하겠습니다")도 발행돼 있었다 |

**9 곳 → 0 곳.**

### 3.3 도구가 남긴 흔적 — 22 편, 64 곳

`:contentReference[oaicite:N]{index=N}`, `【N†source】`, URL 뒤의
`utm_source=chatgpt.com`. 글쓴이가 쓴 문장이 아니라 도구의 흔적이다.

### 3.4 산문의 통화 기호 — 17 편, 68 곳

```
"이미지에서 피자의 가격은 $9.99입니다. 두 개를 주문하므로 총 $19.98입니다."
```

금액 둘이 짝을 지어 사이 산문을 KaTeX 수식으로 렌더한다. `\$` 는
Goldmark 가 `$` 로 되돌려 통하지 않아 인라인 코드로 감쌌다.

### 3.5 특수 토큰이 HTML 로 먹혔다 — 4 편, 35 곳

goldmark 가 `unsafe: true` 라 `<box>`, `<img>`, `<work>` 같은 글자가
그대로 HTML 로 나간다. 브라우저는 모르는 요소로 보고 태그만 삼키므로
`<box> 토큰으로 처리` 가 화면에서는 " 토큰으로 처리" 가 됐다. 하필 그
토큰을 설명하는 글이다. `<img>` 는 src 없는 빈 이미지 요소까지 만들었다.

TeraPipe 글은 사정이 달랐다. `\(\texttt{<sos>}\)` 처럼 수식 안에 있어서
passthrough 가 손대지 않고 HTML 로 내보내는데, 브라우저가 `<sos>` 를
요소로 파싱해 텍스트 노드가 쪼개지고 KaTeX 가 짝 맞는 `\)` 를 찾지 못해
수식이 통째로 깨져 있었다. HTML 엔티티로 바꿨고, KaTeX 0.16.22 로 직접
렌더해 정상 출력을 확인했다.

### 3.6 한국어 범위 표기가 취소선이 됐다 — 78 편, 158 곳

가장 많이 퍼져 있던 결함이다. GFM 취소선은 물결 **하나**로도 열린다.
한국어는 범위를 `2~3명`, `10~30%`, `4~6주` 로 쓰는데, 한 줄에 물결이
둘이면 그 사이가 통째로 취소선이 된다.

```
소스:  - **라우팅 알고리즘 개발**: 2~3명의 개발자가 약 **2~4주** 소요.
렌더:  라우팅 알고리즘 개발: 2<del>3명의 개발자가 약 **2</del>4주** 소요.
```

"3명의 개발자가 약 \*\*2" 에 줄이 그어진 채로 발행돼 있었다. 굵게
표시도 `<del>` 경계에 잘려 `**` 가 그대로 보였다 — 3.1 에서 남았던
23 곳 중 13 곳이 사실 이 문제였다.

이 블로그에 진짜 취소선은 한 곳도 없다. `~~` 가 있는 8 줄도 전부
`1~~2주` 처럼 물결을 두 번 친 범위 표기였고(이것도 취소선이 됐다),
나머지 하나는 코드 블록 안의 컴파일러 캐럿 줄이다.

`markup.goldmark.extensions.strikethrough: false` 로 확장을 껐다. 글을
고치는 대신 설정을 끈 이유는 산문의 단일 물결이 3,091 개 / 343 편이기
때문이다. 지금 깨진 158 곳만 이스케이프하면 나중에 같은 줄에 물결이
하나 더 들어오는 순간 다시 깨진다.

**158 곳 → 0 곳.**

### 3.7 깨진 이미지와 밀린 그림

- GitHub 이 `render.githubusercontent.com/render/math` 를 내렸다(실측
  400). 그 자리의 수식이 깨진 이미지로 나오고 있었다 → KaTeX 로 교체.
  codecogs 수식 4 개도 함께 옮겼다(살아 있지만 수식 하나에 외부 요청을
  하나씩 쓸 이유가 없다).
- DARTS 글의 그림이 한 칸씩 밀려 각 표의 캡션과 섹션 제목이 어긋나
  있었다. `### PTB` 아래에 CIFAR-10 표가, `### ImageNet` 아래에 PTB 표가
  있었고 ImageNet 표(darts8.PNG)는 어디에서도 쓰이지 않았다. 표 안에
  적힌 데이터셋 이름으로 확인해 제자리에 돌려놨다.

### 3.8 같은 글이 두 주소로

`2024-11-29-tow36.md` 는 제목이 "Abseil Tip 36 새로운 Join API" 인데
본문이 `2024-11-28-tow131.md` 와 sha256 까지 같았다. 복사 오류다.
중복을 지우고 그 주소는 alias 로 131 편에 넘겼다.

### 3.9 pre-commit 검사기의 오검출도 함께 고쳤다

전체 글에 검사기를 돌렸다가 **자동 수정이 수식의 뜻을 바꾸는 것**을
발견했다.

```
\|x\|_2        L2 노름      ->  |x|_2      절대값
KL(P \| Q)     KL 발산      ->  KL(P | Q)  조건부 확률
```

`UNESCAPE` 표에 `\| -> |` 가 들어 있었다. 마크다운 이스케이프처럼
생겼지만 KaTeX 에서 `\|` 는 이중 세로선이다. 표에서 빼고 회귀 테스트를
붙였다. 더불어 `^{*}` 같은 정상 LaTeX 를 마크다운 강조로 보던 규칙과,
`$0.90 + 0.055n$` 같은 비용 모델식을 금액으로 보던 규칙도 좁혔다.

**post-lint 문제 60 건 → 0 건 (611 편 전수), automation 894 tests / ruff clean.**

---

## 4. 내부 링크

본문 안 문맥 링크가 27 편 / 51 개뿐이었다. 사람이 써야 하는 부분이라
그대로 두고, 구조로 메울 수 있는 두 가지를 했다.

### 4.1 관련 글

`related` 설정이 없어 Hugo 기본값(keywords + date)이 돌고 있었다. 이
블로그는 front matter 에 keywords 를 쓰지 않으니 사실상 date 만 남는다.
532 편 중 158 편에 관련 글이 하나도 붙지 않았다.

태그·카테고리로 색인하고 값을 실측으로 골랐다.

| 설정 | 관련 글 0 개인 글 | 3 개 다 붙은 글 |
|---|---:|---:|
| 설정 없음 (기본) | 158 | 349 |
| threshold 20, cardinality 없음 | 2 | 524 |
| threshold 20, cardinality 40 | 180 | 338 |
| **threshold 20, cardinality 60** | **14** | **510** |
| threshold 60, cardinality 없음 | 35 | 493 |

cardinality 60 이면 `paper-review`(76%)·`with-gpt`(61%) 만 빠진다. 둘 다
거의 모든 글에 달려 있어 관련성 신호가 되지 못하는 반면
`Machine Learning`(35%)·`NLP`(33%) 는 남아 최소한의 주제 구분을 한다.
cardinality 를 40 까지 조이면 색인이 비어 오히려 나빠진다.

### 4.2 연재 묶기

30 편짜리 코드 리딩 연재 — 이 블로그에서 가장 공들인 글들 — 이 서로
이어지지 않았다. 1 편을 읽은 독자도 크롤러도 2 편으로 갈 길이 없었다.

테마에는 이미 연재 UI 가 있었다. `post-meta.html` 이 `series` 텀과
`series_order` 를 보고 장 목록과 "3/8" 진행 표시를 그린다. 그런데 글에는
파이프라인이 쓰는 `series_id` / `chapter_order` 만 있어서 Hugo 가 분류를
만들지 못했다. 둘 다 내보내도록 했고, 파이프라인(automation)도 앞으로
같은 필드를 쓰도록 고쳤다.

**결과: 글마다 다른 글로 가는 고유 링크가 중앙값 6 개, 최대 11 개.
링크가 하나도 없는 페이지는 1 개(리다이렉트 스텁)뿐이다.**

### 4.3 주제 허브

분류 페이지 여섯 곳과 연재 두 곳에 `_index.md` 를 두고 소개글을 썼다.
테마가 `.Content` 를 렌더하므로 목록 위에 실제 문장이 뜬다. 생성된
문구 대신 사람이 쓴 description 도 함께 들어간다.

| 페이지 | 글 수 |
|---|---:|
| `/categories/abseil/` | 82 |
| `/categories/llm-inference/` | 16 (+ 오타 주소 alias) |
| `/categories/code-series/` | 15 |
| `/tags/distributed-computing/` | 59 |
| `/tags/long-context/` | 18 |
| `/tags/kv-cache/` | 17 |
| `/series/sgl-project--mini-sglang/` | 8 |
| `/series/jmaczan--tiny-vllm/` | 7 |

### 4.4 홈에서 주제로 가는 길

홈은 이 사이트에서 가장 강한 페이지인데 내보내는 링크가 최근 글
다섯 개뿐이었다. 주제별로 쌓인 글 수백 편에 닿으려면 상단 메뉴의
"카테고리" / "태그" 를 거쳐 100 개가 넘는 목록에서 골라야 했다.

홈에 "주제별로 둘러보기" 섹션을 넣어 4.3 의 허브로 바로 잇는다. 테마
파일을 복제하지 않고 `home.contentOrder` 확장점에 파셜 하나를 더한
것이라 테마를 올려도 영향이 없다. 목록은 `params.home.topics` 에서
오고(언어별), 제목·설명·글 수는 대상 페이지에서 직접 읽으므로 허브의
`_index.md` 를 고치면 홈도 따라 바뀐다.

실측: 홈 Lighthouse perf 96 / a11y 100 / seo 100 / BP 100, 변화 없음.

---

## 5. 글 자체를 손본 것

### 5.1 본문이 짧은 옛 글 16 편

2021 년에 쓴 명령어 메모들이다. 설명이 없어 검색 결과에 사이트 설명이
그대로 나갔다.

- `slug` 을 먼저 박아 현재 주소를 고정한 뒤 제목을 손봤다. 지금 주소는
  제목에서 파생되므로 제목만 고치면 URL 이 바뀐다
- 제목만 보고 무슨 글인지 알 수 있게 고치고 오타도 함께 고쳤다
  (`ElementryOS mouch pad` → `elementary OS ... 트랙패드`)
- description 을 본문에 있는 내용만으로 썼다

`inplace_swap` 은 본문이 코드 6 줄뿐이라 설명을 지을 근거조차 없었다.
XOR 이 왜 맞바꾸기가 되는지와, 두 포인터가 같은 곳을 가리킬 때 값이
0 으로 날아가는 함정을 적었다. 둘 다 그 코드에서 바로 따라 나오는
성질이다.

### 5.2 Abseil 글 19 편의 Jekyll 잔재

abseil.io 원문이 Jekyll 이라 `layout: tips`, `permalink: tips/86` 가 든
머리말이 본문 맨 앞에 남아 화면에 찍히고 meta description 까지 거기서
만들어지고 있었다.

### 5.3 이미지

| | 전 | 후 |
|---|---:|---:|
| alt 없는 본문 이미지 | 43 | 0 |
| 1 MB 넘는 이미지 | 3 | 0 |
| static/ 용량 | 7.1 MB | 4.4 MB |

alt 는 그림을 하나씩 열어 보고 적었다. 논문 그림은 캡션이 그림 안에
박혀 있어 화면 낭독기가 읽지 못하므로 그 내용도 밖으로 꺼냈다.

matplotlib 이 4800px 로 뽑아 둔 차트를 본문 폭(최대 900px)에 맞춰
1600px webp 로 줄였다. 변환 결과를 열어 축 라벨과 범례가 읽히는지
확인했다.

---

## 6. Core Web Vitals

### 6.1 라이브 실측 (개선 전, 2026-09-18)

| | 논문 글 | mermaid 글 |
|---|---:|---:|
| Performance | 79 | 71 |
| LCP | 4.2 s | 5.0 s |
| TBT | 220 ms | 180 ms |
| CLS | 0 | 0 |

LCP 4.2 s 의 내역이 중요하다.

| 구간 | 비중 | 시간 |
|---|---:|---:|
| TTFB | 19% | 811 ms |
| Load Delay | 0% | 0 ms |
| Load Time | 0% | 0 ms |
| **Render Delay** | **81%** | **3,406 ms** |

병목은 서버가 아니라 렌더다. Lighthouse 가 지목한 절감 대상은
google-analytics 420 ms, giscus 296 ms 였고, KaTeX·mermaid 는
cdn.jsdelivr.net 에서 오는데 그 태그가 본문 끝에 있어 문서를 거의 다
파싱해야 발견된다. 특히 `katex.min.css` 는 렌더를 막는 스타일시트다.

### 6.2 한 일

필요한 페이지에서만 `preconnect` / `dns-prefetch` 를 붙이고 KaTeX CSS 는
`preload` 했다 (preconnect 는 4 개를 넘기지 않는다).

```
모든 페이지       googletagmanager (preconnect), google-analytics (dns-prefetch)
수식/도표 있는 글  cdn.jsdelivr.net (preconnect) + katex.min.css (preload)
글 페이지         giscus.app (dns-prefetch)
```

**이 절감폭은 Lighthouse 추정치다.** 로컬 서버에서는 TTFB 가 거의 0 이라
재현되지 않는다. 배포 후 라이브에서 다시 재야 한다 (아래 9.1).

### 6.3 DOM 을 줄였다 — 헤딩 앵커 아이콘

라이브 실측에서 주 스레드 작업 1.3 초 중 가장 큰 몫이 **Style & Layout
647 ms** 였다. 서드파티는 생각보다 작다 — giscus 는 11 ms / 3 KB 로
사실상 공짜고, GTM 이 205 ms / 172 KB 다.

DOM 구성을 세어 보니 논문 글 한 편의 2,815 요소 중 **560 개(19%)가
헤딩 앵커 아이콘**이었다. 헤딩마다 인라인 SVG 가 들어가는데 앵커
하나가 요소 5 개(a, svg, g, path, path)와 HTML 308 바이트를 쓴다.
헤딩이 140 개인 글에서 42 KB 다.

아이콘을 CSS 마스크로 옮겨 앵커를 요소 하나로 줄였다. 색은 마스크
위에 `currentColor` 를 깔아 칠하므로 테마의 hover 규칙이 그대로 먹는다.
`_markup/render-heading.html` 은 7 줄짜리라 복제 비용도 거의 없다.

| | 전 | 후 |
|---|---:|---:|
| 요소 수 (논문 글 한 편) | 2,815 | 2,255 |
| Lighthouse DOM size | 2,876 | 2,316 |
| Performance (로컬) | 89 | 92 |
| LCP (로컬) | 3.0 s | 2.8 s |

마스크가 실제로 그려지는지는 Chrome 헤드리스로 렌더해 확인했다
(shorthand / longhand / background 세 방식 모두 동일).

### 6.4 로컬 실측 (개선 후)

로컬 서버(`python3 -m http.server`)라 gzip 과 캐시 헤더가 없다. 라이브
보다 TTFB 가 훨씬 짧으므로 **점수를 라이브와 직접 비교하면 안 된다.**
페이지 종류별로 남은 문제를 보는 용도다.

| 페이지 | Perf | A11y | SEO | BP | LCP | TBT | CLS | 남은 실패 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| 홈 | 96 | 100 | 100 | 100 | 2.4 s | 110 ms | 0 | 없음 |
| 논문 글 | 89 | 100 | 100 | 100 | 3.2 s | 110 ms | 0 | 없음 |
| 연재 글 | 89 | 100 | 100 | 100 | 2.9 s | 200 ms | 0 | 없음 |
| 태그 `/tags/kv-cache/` | 95 | 98 | 100 | 100 | 2.4 s | 110 ms | 0 | `heading-order` |
| 연재 `/series/…mini-sglang/` | 96 | 98 | 100 | 100 | 2.3 s | 110 ms | 0 | `heading-order` |
| 카테고리 `/categories/abseil/` | 91 | 98 | 100 | 100 | 2.9 s | 100 ms | 0 | `heading-order` |
| 소개 | 96 | 100 | 100 | 100 | 2.4 s | 50 ms | 0 | 없음 |

**SEO 와 Best Practices 는 모든 페이지에서 100.** 글 페이지의 접근성도
100 이다 (감사 시작 시 논문 글 91, 연재 글 92). 목록 페이지에 남은
`heading-order` 는 8.5 에 적었다.

아래 표는 6.3 을 적용하기 전에 잰 값이다. 논문 글은 그 뒤 92 / LCP 2.8 s
로 올랐다.

---

## 7. 분류 체계 — 고친 것과 남긴 것

### 7.1 고친 것 (명백한 결함)

`data/paper-filter.yaml` 의 `topics` 가 주제 카테고리의 정본이다.
거기에 맞지 않거나 표기가 갈린 것만 고쳤다.

```
LLM-Infernce                     -> LLM-Inference          (오타, alias 유지)
Speculative Decoding (카테고리)    -> speculative-decoding   (닫힌 목록 표기)
tags: [Gemini 2.5, null]         -> null 제거              (이름 없는 태그 페이지)
cross-llm-kv-reuse (en)          -> Cross-LLM KV Reuse     (번역쌍 표기 통일)
연속층-부분-재계산 (contiguous-...) -> contiguous-layer-recompute
멀티모달                           -> Multimodal            (한 글 안 표기 혼용)
```

### 7.2 남긴 것 (글쓴이가 정할 일)

같은 말이 카테고리와 태그 양쪽에 있는 경우가 18 개다. 그중 10 개는
7.3 의 noindex 로 자연히 해소됐고, **8 개가 남아 둘 다 색인된다.**

| 용어 | 카테고리 | 태그 |
|---|---:|---:|
| kv-cache / KV Cache | 17 | 33 |
| LLM-Inference / LLM Inference | 15 | 13 |
| speculative-decoding / Speculative Decoding | 7 | 8 |
| LLM | 6 | 4 |
| mixture-of-experts / Mixture of Experts | 6 | 17 |
| DeepSeek | 4 | 13 |
| long-context / Long Context | 4 | 33 |
| quantization / Quantization | 4 | 9 |

이건 파이프라인이 **의도해서** 만든 구조다. `data/paper-filter.yaml` 이
"주제 카테고리. **닫힌 목록이다.** 자유 입력으로 두면 카테고리가
60종까지 늘어나 같은 뜻이 갈라진다" 라고 적고 있고, 태그는 기존 글에서
세어 온다. 축이 둘인 것 자체가 설계다.

검색 관점에서는 두 페이지가 같은 질의를 놓고 다툰다. 정리하려면 둘 중
하나를 골라야 하는데, 어느 쪽을 정본으로 삼을지는 글쓴이의 분류 철학에
달린 문제라 손대지 않았다. 고른다면 이렇게 하면 된다.

- **카테고리를 정본으로** — 옛 글에 닫힌 목록의 주제 카테고리를 채워
  넣고, 겹치는 태그를 어휘에서 빼고, `/tags/<x>/` → `/categories/<x>/`
  alias. 카테고리 페이지가 완전해지는 대신 손댈 글이 많다
- **태그를 정본으로** — 주제 카테고리를 빼고 `/categories/<x>/` →
  `/tags/<x>/` alias. 손댈 글은 적지만 파이프라인의 닫힌 목록 설계를
  버리게 된다

### 7.3 `with-*` 카테고리

`with-gpt`(371), `with-deepseek-v4-pro`(48), `with-gemini-2.5-pro-preview`(20)
등 9 개는 주제가 아니라 **어떤 모델로 정리했는지** 를 밝히는 표기다.
검색 대상으로서는 값이 없지만 출처 공개라는 역할이 있어 그대로 뒀다.
글 수가 적은 것들(2편 이하)은 이미 noindex 로 빠졌다.

---

## 8. 남은 기회

영향이 큰 순서로.

### 8.1 LCP render delay (가장 큼)

라이브 LCP 4.2 s 중 3.4 s 가 render delay 다. preconnect 로 깎이는 건
그중 일부다. 더 줄이려면:

- **gtag.js 171 KB (42% 미사용)** — 가장 큰 단일 스크립트다. GA4 를
  `partytown` 같은 워커로 빼거나, 서버사이드 태깅으로 옮기거나, 더
  가벼운 분석 도구로 바꾸는 선택지가 있다
- **렌더를 막는 CSS 3 개 (21 KB)** — critical CSS 를 인라인하고 나머지를
  비동기로 내리면 first paint 가 앞당겨진다. 회귀 위험이 있어 별도
  작업으로 다루는 게 낫다
- ~~giscus 지연 로드~~ — 재어 보니 giscus 는 11 ms / 3 KB 로 사실상
  공짜다. 할 가치가 없다
- **DOM** — 헤딩 앵커로 19% 를 줄였다(6.3). 남은 것은 논문 글이 길어서
  생기는 구조적 비용이다. 더 줄이려면 접는 섹션 같은 구조 변경이 필요하다

### 8.2 본문 안 문맥 링크

여전히 27 편 / 51 개다. 관련 글과 연재 내비로 구조적 링크는 채웠지만,
"이 주제는 [다른 글]에서 다뤘다" 같은 문맥 링크는 사람이 써야 한다.
KV 캐시·롱 컨텍스트·MoE 처럼 글이 여러 편 쌓인 주제부터 손보면 효과가
크다.

### 8.3 프롬프트 문장이 그대로 소제목인 글

논문 글의 소제목 2,707 개 중 약 1,950 개가 12 종의 반복 프롬프트
문장이다.

```
## Q : 이 논문의 한계를 극복하기 위한 방법으로 어떤 연구흐름이 있는지 정리해서 자세하게 설명해 줘   (289 편)
## Q : 이 논문에서 제시한 결과를 자세하게 보고 ...                                          (287 편)
## Q : 이 논문을 아주 자세하게 읽고 논문의 강점과 ...                                        (279 편)
```

**레벨은 고쳤지만 문장은 그대로 뒀다.** 목차에 그대로 실려 읽기 나쁘고,
같은 문장이 289 편에 반복돼 사이트 전체로 보면 상당한 분량의 중복
문구다. 짧은 소제목으로 바꾸고 원래 프롬프트는 인용으로 남기면 목차가
쓸모 있어지고 중복도 준다. 다만 이건 글쓴이의 Q&A 형식 자체를 바꾸는
일이라 판단을 남겨 뒀다.

### 8.4 Abseil Tip 36 본문 없음

중복이던 `tow36.md` 를 지우면서 Tip 36("새로운 Join API") 본문이 블로그에
없는 상태가 됐다. 지금은 그 주소가 Tip 131 로 넘어간다. 원문
(abseil.io/tips/36)을 옮겨 쓰면 주소를 되살릴 수 있다.

### 8.5 목록 페이지의 헤딩 순서

`/posts/`, `/tags/<x>/`, `/series/<x>/` 가 h1 다음에 카드 h3 로 한 단계를
건너뛴다 (Lighthouse a11y 98). 고치려면 테마의 `card-base.html`(109 줄)을
복제해 h3 → h2 로 바꾸거나, `post-list.html`·`list.html` 에 보이지 않는
h2 를 넣어야 한다. 검색에는 영향이 없고 복제 비용이 커서 두었다.

### 8.6 자잘한 것들

- 화면에 `**` 가 남은 10 곳 (여러 줄에 걸치거나 안쪽에 `*` 가 든 경우,
  혹은 원래 짝이 없는 경우). 하나하나 사람이 읽고 판단해야 한다
- 2021 년 글들의 오래된 절차 — `docker-tag` 글이 쓰는 Docker Registry
  v1 엔드포인트, `sudo pip install virtualenv` 등. 확인 없이 고칠 수
  없어 손대지 않았다
- `og:locale` 이 `ko_kr` 로 나간다. 페이스북 규격은 `ko_KR` 이다

---

## 9. 사람이 해야 할 일

### 9.1 배포 후

1. **Search Console 에 사이트맵 재제출** — URL 수가 크게 줄었다
   (ko 776 → 608, en 246 → 122). 얇은 분류 페이지 298 개를 뺀 결과이고
   의도한 변화다. 재제출해야 반영이 빠르다
2. **"제외됨 — noindex 태그에 의해 제외" 가 늘어나는 것은 정상** —
   298 페이지가 여기로 들어온다
3. **색인 요청** — 새로 만든 허브 8 개
   (`/categories/abseil/`, `/categories/code-series/`,
   `/categories/llm-inference/`, `/tags/kv-cache/`,
   `/tags/long-context/`, `/tags/distributed-computing/`,
   `/series/sgl-project--mini-sglang/`, `/series/jmaczan--tiny-vllm/`)
4. **리다이렉트 확인** — 두 주소가 meta refresh + canonical 로 넘어간다.
   Search Console 에서 원래 주소가 색인에서 빠지는지 지켜보면 된다
   - `/posts/abseil-tip-36-새로운-join-api/` → Tip 131
   - `/categories/llm-infernce/` → `/categories/llm-inference/`
5. **Core Web Vitals 재측정** — 6.2 의 절감폭은 Lighthouse 추정치다.
   배포 후 라이브에서 다시 재고, 28 일 필드 데이터가 쌓이면 Search
   Console 의 CWV 보고서도 확인한다
6. **리치 결과 테스트** — search.google.com/test/rich-results 에 글
   URL 하나를 넣어 BlogPosting 과 BreadcrumbList 가 잡히는지 확인한다.
   로컬에서 JSON.parse 는 통과하지만 구글의 판정은 따로다

### 9.2 지금 확인이 필요한 것

- **automation 서브모듈에 푸시되지 않은 커밋이 2 개 있다.** 블로그
  리포의 커밋이 그 SHA 를 가리키므로, 블로그를 푸시하기 전에
  automation 부터 푸시해야 한다. (요청받지 않아 푸시하지 않았다)
  ```
  6e6dc7c  fix(mathfix): stop turning the norm delimiter into an absolute value
  c8c9994  feat(code-series): emit the series taxonomy the theme reads
  ```
- **범블비 스킴의 링크 색이 눈에 띄게 바뀐다** (`#fdc700` → `#9a6600`).
  대비 1.45:1 을 고치려면 불가피했지만, 원치 않으면
  `assets/css/custom/overrides.css` 의 해당 줄을 지우면 된다

---

## 10. 다시 재는 법

```bash
# 빌드 (CI 와 같은 조건)
hugo --gc --minify --printPathWarnings

# 글 검사 (LaTeX·mermaid·코드펜스)
python3 automation/tasks/papers/cli.py check

# Lighthouse (로컬)
python3 -m http.server 8811 --directory public &
CHROME_PATH=/usr/bin/google-chrome npx -y lighthouse@12 \
  http://127.0.0.1:8811/posts/<slug>/ \
  --only-categories=performance,accessibility,seo,best-practices \
  --chrome-flags="--headless=new --no-sandbox"
```

렌더에서 지표를 세는 스크립트(중복 title/description, JSON-LD, 헤딩
구조, literal `**`, alt, 내부 링크)는 이 세션의 스크래치패드에 있다.
리포에 남기려면 `automation` 쪽 검사기에 붙이는 게 맞다.
