# Chapter 8 translation review

Read the English against the Korean line by line and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 14 references in both, identical in path and line
  range, with the same 10 code blocks.
- **Structure.** Five second-level sections in each, same order. Both visual
  comments keep their declared kinds: `rank-sync-protocol` as a mermaid sequence
  diagram with the same three participants and the same note placement,
  `tp-shard-map` as a five-row table.
- **The central claim.** "Ranks never exchange decisions; they take the same input
  and run the same code" is carried with the same force, and so is the reason the
  count must be broadcast — that PUB/SUB cannot express "read everything so far",
  and that a one-message difference reaches the `break` from chapter 3.
- **The replication case.** The `num_kv_heads < n` path is stated the same way in
  both: not cut, but replicated across ranks, tied back to chapter 4's
  `allow_replicate=True`.
- **Axis vocabulary.** Output axis versus input axis, and which one needs the
  all-reduce, stay consistent through the English; no sentence swaps them.
- **Closing.** The series-ending paragraph lists the same seven stations in the
  same order and makes the same claim about following each chapter's handoff.
  The "ten thousand lines" figure appears in both and refers to the guide's own
  scoping of `python/`, not to a measurement made here.

## Required fixes

None.
