# Chapter 4 translation review

Read the English against the Korean line by line and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 14 references in both, identical in path and line
  range, with the same 12 code blocks. The `engine/engine.py:55-59` excerpt added
  during review appears in both languages with the same elision.
- **Structure.** Seven second-level sections in each, same order. Both visual
  comments carry their declared kinds: `kv-buffer-shape` as a six-row table,
  `page-table-layout` as a mermaid flowchart with the same three subgraphs and
  the same edge labels.
- **The central claim.** The causal chain is preserved without weakening:
  `_storage_shape` collapses the page and offset axes, `view` applies it before
  the kernel, the kernel flattens once more, therefore a page number would cost a
  multiplication per access. The English keeps the repository's own comment
  ("store raw locations instead of pages") as the anchor rather than paraphrasing
  it.
- **The dummy row.** Both versions state the corrected fact: the pool is
  allocated with `num_pages + 1`, so the dummy row addresses real memory that the
  cache manager never hands out. Neither version says it points outside the
  buffer.
- **Units.** Token locations versus page numbers are distinguished consistently
  in English — "token location", "page number", "first token location of each
  page" — with no sentence where the two blur.
- **No new facts.** Nothing is asserted in English that the Korean does not
  assert, and no measurement appears in either.

## Required fixes

None.
