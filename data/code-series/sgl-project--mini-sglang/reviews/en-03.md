# Chapter 3 translation review

Read the English against the Korean line by line, and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 12 references appear in both, identical in path and
  line range, carrying the same 10 code blocks. Nothing was re-trimmed.
- **Structure.** Six second-level sections in each, same order. Both visual
  comments are present with their declared kinds: `prefill-budget-checks` as a
  five-row table, `chunked-req-split` as a mermaid flowchart. The flowchart keeps
  the same nine nodes and the same branch labels; only the prose inside the nodes
  is translated, and the identifiers (`ChunkedReq`, `can_decode`, `cached_len`,
  `chunked_req`, `pending_list`, `CLS = Req`) are left as code.
- **The central claim.** "Why evaluate the same inequality twice" survives with
  its reasoning intact: locking moves pages out of the evictable pool, so the
  right-hand side shrinks. The English keeps this as an explanation of the code
  rather than promoting it to a measured fact.
- **Hedges.** The Korean qualifies the estimate as "neither optimistic nor
  sophisticated … but conservative", quoting the `TODO` comment; the English
  keeps both halves. No performance claim is introduced in either version, and
  neither cites a throughput or latency number.
- **Scope handoffs.** Identical and pointing at the same chapters: what
  `page_table` holds to 4, locking and free space to 5. The closing paragraph
  defers the same question in both.
- **Further reading.** Both cite Sarathi-Serve with the same arXiv link, and both
  attribute it to `docs/features.md` — which does link it, on the line that also
  states chunked prefill is enabled by default.

## Required fixes

None.
