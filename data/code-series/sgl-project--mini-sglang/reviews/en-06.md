# Chapter 6 translation review

Read the English against the Korean line by line and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 16 references in both, identical in path and line
  range, with the same 11 code blocks — including the two `engine/engine.py`
  excerpts added during review.
- **Structure.** Six second-level sections in each, same order. Both visual
  comments keep their declared kinds: `normal-vs-overlap` as a six-row table,
  `overlap-timeline` as a mermaid sequence diagram with the same participants,
  the same message order and the same notes.
- **The central claim.** "The next input never goes through the CPU" is carried
  with its mechanism intact: read from `token_pool`, write to `token_pool`, both
  GPU tensors, therefore no dependency on host-side values. The English keeps the
  distinction that the CPU copy exists "for the answer being sent to a person,
  not for the next computation".
- **The deferral framing.** The Korean says overlapping does not remove the wait
  but moves it one iteration later; the English says "defers it by one
  iteration". Same claim, no strengthening.
- **No performance claims.** Neither version attaches a number to the overlap, and
  the timeline diagram shows ordering only, not durations. The repository's own
  wording about hiding CPU latency appears only inside the quoted docstring, in
  both languages.
- **Scope handoffs.** `pad_batch` and `prepare_metadata` go to chapter 7 in both;
  chapter 2's `token_pool`, chapter 3's prefix copy and chapter 4's page-table
  write are referenced back identically.

## Required fixes

None.
