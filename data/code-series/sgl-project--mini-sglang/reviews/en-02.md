# Chapter 2 translation review

Read the English article against the Korean source line by line, and checked the
citations against the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`
rather than against the Korean text alone.

## Decision

**PASS**

## Fidelity

- **Citations.** Both versions carry the same 14 references, identical in path
  and line range, and the same 11 code blocks with the same excerpts. Nothing was
  re-trimmed in translation.
- **Structure.** Six second-level sections in each, same order. Both visual
  comments survive with their ids and declared kinds: `req-length-fields` as a
  five-row table, `req-lifecycle` as a mermaid state diagram. The diagram's
  transitions match one for one; only the state labels are translated, and the
  two that name code (`table_manager.allocate`, `table_manager.free`) keep their
  identifiers.
- **The central claim.** The chapter turns on the ordering of two updates, and
  the English keeps it exact: "the length grows first on the GPU side, and the
  content is filled in later on the CPU side", with the same two citations
  (`engine/engine.py:199-203` before sampling, `scheduler/scheduler.py:143-151`
  after `copy_done.synchronize()`). The Korean reading of the invariant as "not
  defensive code but the definition of the three values" is carried over rather
  than softened.
- **Scope handoffs.** Every deferral is intact and points at the same chapter:
  batching rules to 3, `ChunkedReq` to 3, `out_loc` to 4, the page-size comment
  to 4, the GPU-resident token pool to 6, `attn_metadata` to 7.
- **No new facts.** The English introduces no claim the Korean does not make. Two
  renderings are idiomatic rather than literal — "Pop from one list, push back
  onto it" for the allocate/free pair, "one seat number" for 자기 자리 번호 — and
  both restate the cited code without adding to it.
- **Further reading.** Both point at the repository's own `docs/structures.md`
  and quote the same line about `minisgl.scheduler` running per TP worker. This
  is the corrected version; the draft's original claim about that file did not
  exist in it, and the fix is reflected in both languages.

## Required fixes

None.
