# Chapter 7 translation review

Read the English against the Korean line by line and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 14 references in both, identical in path and line
  range, with the same 11 code blocks — including the `attention/base.py` excerpt
  added during review.
- **Structure.** Five second-level sections in each, same order. Both visual
  comments carry their declared kinds: `metadata-conversion` as a six-row table,
  `graph-padding` as a mermaid flowchart with the same seven nodes and the same
  yes/no branches.
- **The conversion table.** Every row maps the same scheduler value to the same
  kernel argument by the same method. The distinction that `cache_seqlens` alone
  is not accumulated survives.
- **The three-way branch.** The English keeps all three cases and the reason each
  exists, including that the no-cache-hit case **reuses the same tensor** rather
  than building an equal one — a detail easy to flatten in translation.
- **Chapter links.** The pointers back to chapters 2, 4, 5 and 6 and forward to 8
  are identical, and the claim that a prefix-reuse outcome decides a branch is
  stated with the same strength in both.
- **CUDA graph reasoning.** "Tensor addresses and shapes must stay the same",
  why prefill is excluded, and "the same storage gets new values" are carried
  without added or removed certainty. No timing or throughput claim appears in
  either version.

## Required fixes

None.
