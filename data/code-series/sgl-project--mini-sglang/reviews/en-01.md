# Chapter 1 translation review

Checked the English article line by line against the Korean source, and checked
every citation against the pinned revision
`9a91cfafe754aa85daee49998176275667eb58f2` rather than against the Korean text
alone.

## Decision

**PASS**

## Fidelity

- **Citations.** Both versions carry the same 16 source references, identical in
  path and line range. Diffing the two sets leaves nothing on either side. The
  ten code blocks are the same excerpts of the same files; no snippet was
  re-trimmed or re-indented in translation.
- **Structure.** Six second-level sections in each, in the same order, and both
  visual comments are present with their ids and kinds unchanged —
  `process-roles` as a table, `request-process-flow` as a mermaid diagram. The
  table has the same four rows and the same addresses.
- **Technical meaning.** The load-bearing statements survive. `UserMsg.input_ids`
  is still "a 1-D int32 tensor on the CPU", and the reason it matters — the token
  sequence crosses the boundary in host memory, and moves to the GPU only when
  the scheduler assembles a batch — is preserved rather than paraphrased away.
  Rank 0 still re-sends the raw bytes "before decoding them", which is the point
  of that paragraph. The ack arithmetic keeps its condition: one ack from the
  schedulers because only the primary rank sends one.
- **Caveats and scope handoffs.** Every deferral is intact: TP semantics to
  chapter 8, the broadcast protocol to chapter 8, the scheduler-side
  representation to chapter 2. The closing sentence keeps the emphasis that the
  scheduler code ahead runs once per rank.
- **No new facts.** The English adds no claim the Korean does not make. Two
  sentences are looser in English than a literal rendering would be — "less
  machinery" for 단순하다, "the price is the constraint asserted right there in
  the code" for the 1-D assertion — but both state the same fact as the source
  and neither introduces a measurement or a causal claim of its own.
- **Further reading.** Both versions point at the same two places, the SGLang
  repository and this repository's `docs/structures.md`. Neither invents an
  external source the repository does not itself cite.

## Required fixes

None.
