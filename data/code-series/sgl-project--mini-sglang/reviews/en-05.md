# Chapter 5 translation review

Read the English against the Korean line by line and checked citations against
the pinned revision `9a91cfafe754aa85daee49998176275667eb58f2`.

## Decision

**PASS**

## Fidelity

- **Citations.** The same 20 references in both, identical in path and line
  range, with the same 14 code blocks.
- **Structure.** Eight second-level sections in each, same order, and both visual
  comments carry their declared kinds: `radix-tree-split` as a mermaid flowchart,
  `lock-evict-sizes` as a four-row table. The table's arithmetic is identical,
  including the two rows where nothing moves.
- **The illustrative example.** The Korean diagram uses invented Korean tokens
  ("사과는 빨갛다") to show the split; the English uses invented English tokens
  ("an apple is red"). Both are clearly hypothetical inputs rather than repository
  data, and neither claims otherwise. Translating the example rather than
  transliterating it keeps it readable without changing what it demonstrates.
- **The central claims.** Inheritance of `ref_count` on split, the classification
  (not creation) of space by locking, propagation to the root and its link back to
  chapter 3's re-check, the three eviction rules and LRU ordering — all preserved
  with the same citations and the same emphasis.
- **Hedged statements stay hedged.** The claim about `fast_compare_key` remains
  conditional in English ("leave this line as a Python loop and … turns straight
  into latency"), with no measurement attached, matching the Korean.
- **Terminology ownership.** The English defers the same terms to the same
  chapters and defines only what this chapter owns: prefix cache, eviction,
  reference count.

## Required fixes

None.
