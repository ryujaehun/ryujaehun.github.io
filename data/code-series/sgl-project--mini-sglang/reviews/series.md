# Mini-SGLang 시리즈 전체 검토

8 편 전부와 편별 영문 검토, `guide.md`, `series.yaml`, `evidence/claims.md`, 그리고
고정 revision `9a91cfafe754aa85daee49998176275667eb58f2` 의 소스를 대조했다.
결정 가능한 검사는 `check` 가 먼저 돌았고 문제 0 건으로 통과했다.

## Decision

**PASS**

## Coverage

`series.yaml` 이 선언한 8 편이 모두 한국어·영어 쌍으로 존재하고, 각 편의 핵심
질문에 답한다.

| 편 | 질문에 답했는가 | 근거(인용 건수 code/tests, 최소값) |
| --- | --- | --- |
| 1 process-topology | 프로세스 4 종과 경계 메시지를 확정 | 15/1 (5/1) |
| 2 request-state-ledger | 세 길이와 두 갱신 경로를 확정 | 14/0 (4/0) |
| 3 prefill-scheduling | 예산 둘과 조건 다섯, 클래스 교체 | 12/0 (4/0) |
| 4 page-table-and-allocation | raw location 인 이유를 커널 인덱싱으로 | 12/2 (5/1) |
| 5 radix-prefix-cache | 분할·참조 수·퇴출 규칙 | 17/3 (5/2) |
| 6 overlap-scheduling | 두 루프의 차이와 token_pool 경로 | 16/0 (5/0) |
| 7 attention-metadata-and-cuda-graph | 변환 표와 패딩 | 14/0 (5/0) |
| 8 tensor-parallel-ledger | 입력 동기화와 이름 기반 분할 | 13/1 (5/1) |

선언된 16 개 시각 자료가 모두 선언된 형식(표 8, mermaid 8)으로 존재하고, 선언되지
않은 표나 다이어그램은 어디에도 없다. 각 편이 선언한 `code_scopes` 는 전부 해당
편 본문에 인용됐다 — 6 편과 7 편은 초고 단계에서 하나씩 빠졌고 편별 검토에서
채웠다.

측정값은 어느 편에도 없다. `required_evidence.runtime` 이 전부 0 이라는 선언과
일치하며, 성능을 주장하는 문장은 저장소 문서를 인용한 문맥 밖에서는 나오지 않는다.

## Cross-chapter consistency

**넘긴 질문이 모두 회수됐다.** 각 편 말미가 다음 편에 넘긴 약속을 추적했다.

- 1 편 → 2 편: "CPU 1 차원 int32 텐서가 스케줄러 안에서 무엇이 되는가" → 2 편이
  `Req` 의 세 길이로 답함.
- 2 편 → 3 편: `ChunkedReq` 의 정의와 `available_size` 의 쓰임 → 3 편이 둘 다 답함.
- 3 편 → 4 편: `page_table` 에 무엇이 적히는가 → 4 편이 `engine/engine.py:66` 의
  주석으로 답함.
- 4 편 → 5 편: evictable 이 어떻게 늘고 주는가 → 5 편이 `lock_handle` 과 `SizeInfo`
  로 답함. 4 편 검토가 넘긴 `cache_req` 의 구간 주석도 5 편이 다룸.
- 5 편 → 6 편: 접두사 탐색의 CPU 시간을 어디에 숨기는가 → 6 편이 `overlap_loop`
  으로 답함.
- 6 편 → 7 편: `pad_batch` 와 `prepare_metadata` → 7 편이 각각 답함.
- 7 편 → 8 편: rank 동기화 규약, `BaseOP` 의 자체 `state_dict`, 분할 축 → 8 편이
  셋 다 답함. 1 편이 미뤄 둔 규약과 2 편이 언급만 한 `BaseOP` 도 여기서 회수됐다.

**용어 소유 규칙이 지켜졌다.** `guide.md` 의 용어 표대로, 프로세스 경계는 1 편,
`Req`·`Batch`·`Context` 와 길이 필드는 2 편, 토큰 예산과 chunked prefill 은 3 편,
페이지와 `out_loc` 은 4 편, prefix cache 와 참조 수는 5 편, 스트림과 overlap 은
6 편, 메타데이터와 CUDA graph 는 7 편, TP 와 샤딩은 8 편에서만 정의된다. 다른 편은
정의를 반복하지 않고 소유 편을 가리킨다.

**공유 파일의 줄 범위가 분담됐다.** 여러 편이 인용한 파일을 전수 검사했다.

- `scheduler/scheduler.py` — 2 편 143-151·232, 3 편 219-224, 6 편 83-131·204-233.
  `_forward`(227-233)는 6 편이 코드 블록으로 설명하고 2 편은 232 행 한 줄을
  참조만 한다. `_process_last_data` 진입(143)은 2 편이 설명, 6 편이 참조.
- `engine/engine.py` — 2 편 199-203, 4 편 55-73·98, 6 편 38-39·191-206, 7 편 98 참조.
  203 행이 2 편과 6 편에 함께 나오지만 2 편은 `complete_one` 의 순서를, 6 편은 복사
  이벤트를 설명해 초점이 다르다.
- `scheduler/cache.py` — 4 편 20·42-53·83-89·119-146, 5 편 70-74, 6 편 144-146 참조.
- `scheduler/io.py` — 1 편이 96-107 을 개요로, 8 편이 88-122 를 규약으로 다룬다.
  가장 넓게 겹치는 쌍이지만 1 편은 "rank 0 만 우체통을 갖는다"까지, 8 편은 개수
  동기화가 없으면 무엇이 깨지는지까지로 깊이가 다르고, 1 편이 명시적으로 8 편을
  가리킨다.
- `core.py` 100-104 와 `message/utils.py` 20-35 는 각각 2 편·1 편이 코드 블록으로
  설명하고 4 편·8 편은 경로만 참조한다. 중복 설명이 아니다.

**서사 순서가 의존성 사슬과 일치한다.** `guide.md` 가 선언한 선형 사슬대로 각 편이
앞 편의 결과 위에서만 새 개념을 세운다. 뒤 편의 개념을 앞당겨 정의한 곳은 없다.

## Problems

- **`scheduler/io.py` 의 1 편·8 편 겹침이 이 시리즈에서 가장 큰 중복이다.** 규칙
  위반은 아니지만(1 편이 8 편을 가리키고 깊이가 다르다), 같은 코드 블록의 상당
  부분이 두 번 실린다. 1 편이 흐름 파악에 필요한 최소한만 싣고 나머지를 8 편에
  넘기는 편이 더 깔끔했을 것이다. 게시를 막을 문제는 아니다.
- **편별 검토가 두 번 같은 종류의 문제를 잡았다.** 1 편과 2 편 모두 "더 읽을거리"
  에서 저장소가 뒷받침하지 않는 출처를 적었다(외부 링크 하나, 저장소 문서에 없는
  문장 하나). 둘 다 검토 중 고쳐졌고 3 편부터는 외부 참고를 쓰기 전에 원문을 먼저
  확인해 재발하지 않았다. 최종본에는 남아 있지 않다.
- **6 편과 7 편은 초고에서 선언된 코드 범위를 하나씩 빠뜨렸다.** 각각
  `engine/engine.py` 와 `attention/base.py` 다. 편별 검토에서 인용을 추가해 채웠고,
  7 편 검토가 8 편에 남긴 지적("초고 전에 선언 범위를 먼저 확인하라") 이후 8 편은
  처음부터 8 개 경로를 모두 인용했다.
- **`prepare_for_replay` 의 내부가 시리즈 어디에서도 열리지 않는다.** 7 편이 호출만
  보이고 범위 밖으로 넘겼으며 이후 편도 다루지 않는다. 선언된 질문에 답하는 데는
  필요하지 않지만, 시리즈를 끝까지 읽은 독자에게 남는 유일한 빈칸이다.

## Required fixes

없다. 게시 가능하다.

남길 기록: 이 시리즈의 초고·검토·번역은 opencode-go 제공자의 월 사용량이 소진된
기간에 작성됐다(F-36, F-39). `evidence/` 의 recon·trace·claims 는 무료 폴백
모델이 만들었고, `briefs.md` 와 8 편의 본문·검토·번역은 사람이 여는 세션에서
작성됐다. 인용은 전부 고정 revision 의 원본과 대조했으며, 편마다 인용 줄 범위의
존재와 코드 블록의 문자 단위 일치를 기계적으로 확인했다.
