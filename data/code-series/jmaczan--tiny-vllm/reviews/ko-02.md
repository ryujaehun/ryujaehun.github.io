# ko-02 리뷰 — 가중치 적재와 GPU 버퍼 설계

고정 revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf` 의 체크아웃 트리
(`src/main.cpp`)와 첨부 증거(`guide.md`, `series.yaml`, `briefs.md`,
`evidence/claims.md`, `evidence/trace.md`, `evidence/recon.md`)를 기준으로
`articles/ko-02.md` 의 모든 기술 주장·인용·범위·산술·성능 서술을 대조했다.

## Decision

**PASS**

`loadWeights` 의 네 단계, `Weights` 구조체 필드, 버퍼 16개 표의 크기 식·바이트·역할·
줄 인용, 크기 산술, 버퍼 별칭 설계, paged KV cache·슬롯 초기 배치, 소유권 위임,
한계 서술 모두 실제 소스와 첨부 증거에 정확히 일치한다. 모든 바이트 수치는 컴파일
타임 상수에서 직접 검산했고, 측정값으로 읽힐 성능 수치는 없다. 수정을 요구하는
오류는 찾지 못했다. 아래 2건은 비차단 관찰이며 수정 선택 사항이다.

## Verified claims

소스/증거와 정확히 일치함을 확인했다.

- **질문·범위·근거·의존성.** 핵심 질문이 `series.yaml:34`, code_scopes
  `src/main.cpp` 하나가 `series.yaml:35-36`, 1편 의존이 `series.yaml:37-38`,
  `code 3 / tests 0 / runtime 0` 이 `series.yaml:39-42` 및 `guide.md:156` 과
  정확히 일치한다. 고정 revision 해시(`guide.md:6`)와 `파일:시작-끝` 표기도 맞다.
- **소유권·중복 방지.** 버퍼 크기 산정의 2편 소유(`guide.md:194`), 빌드·진입 1편
  (`guide.md:193`), prefill·병렬 리덕션 3편(`guide.md:195-196`), cuBLAS 레이아웃
  4편(`guide.md:197`), decode 5편(`guide.md:198`), paged attention 6편
  (`guide.md:199`), 슬롯·큐 7편(`guide.md:200`) 위임이 초안 14-17행, 31-32행,
  175-185행과 전부 일치한다.
- **main 진입 순서.** `cublasCreate`(`557-563`) → `loadWeights` 호출(`566-569`)
  → RoPE 초기화(`572`) → paged KV cache 할당자(`574-581`) → 프롬프트 큐
  (`583-594`) → 슬롯·배치 상태(`597-610`) → 연산 버퍼(`617-693`) → 초기 prefill
  루프(`695-708`) 순서가 실제 소스 및 `trace.md:33-77` 과 일치한다.
- **loadWeights 네 단계.** `checkGPUStatus` 호출(`81-84`)과 장치 없음 시 오류·`return
  1`(`44-48`), `model.safetensors` 이진 오픈(`87`)·실패 `return 1`(`88-93`),
  헤더 길이 8바이트(`96-97`)·헤더 읽기(`99-101`), JSON 파싱(`104`)·`__metadata__`
  건너뜀(`106-111`)·`data_offsets` 끝값으로 `max_offset` 갱신·시작값 맵 기록
  (`112-117`)이 전부 정확하다.
- **단일 할당·단일 H2D.** `max_offset` 크기로 `cudaMalloc` 한 번(`120-121`),
  같은 크기 CPU 벡터(`123-125`), H2D 복사 한 번(`127`), 이름→오프셋으로
  포인터 매핑(`132-145`)이 소스 및 `claims.md:54-56`(Claim 3)과 일치한다.
- **Weights 구조체.** 범위 `64-77`, `embed_tokens`(`66`)·`norm`(`76`) 단일
  포인터 2개, 레이어별 배열 9종(`67-75`, `N_LAYERS=16` at `15`)으로 필드 11개,
  매핑 루프 `134-145` 가 9종을 채운다는 서술이 실제 구조체와 일치한다. 모델 구조
  가정 주석(`130-131`)과 하드코딩 경로(`87`)도 맞다.
- **버퍼 16개 표.** 16행 전부 실제 `cudaMalloc`/호스트 벡터와 일치한다:
  `gpu_input_tokens`(617-618), `input_embeddings`(619-620),
  `hidden_state`(622-623), `rms_norms`(625-626), `buf_2048_1`(628-629),
  `k_proj_temp_buf`(635-636), `v_proj_temp_buf`(638-639),
  `prefill_attn_scores`(647-648), `buf_2048_2`(656-657), `gate`(662-663),
  `up`(667-668), `embed_proj`(676-677), `embed_proj_cpu`(681-682),
  `gpu_last_tokens`(684-685), `k_proj_batched_buffer`(689-690),
  `v_proj_batched_buffer`(692-693). `trace.md:66-74` 와도 일치한다. 표에서
  제외된 `q_proj`·`attn_scores_v`·`o_proj`·`down` 이 포인터 변수일 뿐
  할당이 아니라는 구분도 정확하다.
- **표의 필수 주장.** `prefill_attn_scores` 가 `MAX_PROMPT_LEN² × NUM_Q_HEADS`
  크기(`647-648`)이고 decode 전용·K/V 임시 버퍼가 공용 버퍼와 별도로 잡히는
  것이 `claims.md:75-78`(Claim 4)과 일치하며, 이 표가 `series.yaml:44-47`
  의 `buffer-allocation-table` 필수 주장을 답한다.
- **크기 산술 전부 검산.** `MAX_BUFFER_SIZE = max(512, 2) = 512`(`31`).
  `prefill_attn_scores` `512×512×32=8,388,608` 원소·16 MiB, `gate`/`up`
  `512×8192=4,194,304` 원소·8 MiB, `embed_proj` `512×128,256=65,667,072`
  원소·125.25 MiB, `embed_proj ÷ prefill_attn_scores ≈ 7.83`, K/V 임시
  `512×512`·512 KiB, decode 배치 `2×512`·2 KiB, `gpu_input_tokens` 2048 B,
  `gpu_last_tokens` 8 B 가 전부 상수(`15,17,20,25,30,31`)에서 직접 검산되어
  맞다. `embed_proj` 가 16개 중 최대 단일 버퍼라는 서술도 맞다.
- **버퍼 별칭 설계.** `buf_2048_1` 의 q_proj(`177`)→점수 Q 입력(`304`)→
  attn_scores_v(`343`) 소비 순서와 `buf_2048_2` 의 o_proj(`377`)→residual
  (`399`)→down(`470`) 순서가 소스와 정확히 일치하고, "동시에 한 역할만
  쓴다"는 정적 주장임을 명시한 점이 `claims.md:103-104`(Claim 5) 및
  `trace.md:183-189` 와 맞다. decode 의 `buf_2048_1` 재사용(`878`)과 6편
  위임도 정확하다.
- **paged KV cache·슬롯 초기 배치.** `kv_cache` 2GB(`575-576`, 상수 `35`),
  `free_blocks` 0..65535(`577-578`, 상수 `37`), `block_table` 전부 `-1`
  (`579`), `block_table_gpu` 할당(`580-581`), 첫 H2D 가 `prefill` 끝(`552`)
  이라는 서술이 소스·`trace.md:52-56` 과 일치한다. `is_slot_free`(`597`),
  토큰·길이 상태(`599-601`), `active_slots`/`active_tokens`(`604-605`),
  `gpu_active_slots`/`gpu_seq_lens`(`607-610`), 7편 위임도 맞다.
- **범위·한계·출처.** "다루지 않는 것" 여섯 항목의 편 위임, GPU·툴체인 부재
  (`guide.md:75-87`), gated 가중치 확보 제약(`claims.md:66-68`,
  `recon.md:91-92`), 별칭 안전성의 정적 성격, 하드코딩 상수·경로 한계가 전부
  증거와 일치한다. 출처 절의 줄 범위 7건(`12-38`, `40-62`·`79-147`, `64-77`,
  `555-610`, `617-693`, `132-145`·`177`·`301-343`·`377-399`·`470`, `552`)
  도 실제와 일치한다.
- **성능 서술.** 표의 바이트가 "측정값이 아니라 산정값"임을 명시하고
  (`guide.md:86-87`, `222-224`) 커널 실행 시간·처리량·메모리 사용량 수치가
  전혀 없어 측정값으로 읽힐 서술이 없다.

## Problems

없음. 아래 2건은 수정이 필요한 오류가 아닌 정밀도 관찰이다.

- **O1 (선택, 인용 정밀도).** 한계 절에서 "`MAX_SEQ_LEN`, `BATCH_SIZE`,
  `MAX_PROMPT_LEN` 등은 TODO 주석이 붙은 하드코딩 상수다(`src/main.cpp:28-31`)"
  라고 한 범위 인용에서 `src/main.cpp:31`(`MAX_BUFFER_SIZE`)은 TODO 주석이
  없는 줄이다. `28`·`29`·`30` 이 각각 TODO 주석을 갖고 "등은"으로 묶었으므로
  오류는 아니고, `28-30` 으로 좁히면 더 정확하다.
- **O2 (선택, 서술).** "구조체 주석은 ... 밝히고"라는 표현은 엄밀히는
  `loadWeights` 의 포인터 매핑 직전 주석(`130-131`)을 가리키는 것으로, 주석이
  구조체의 목적("A HELPER STRUCT")을 설명하므로 사실과 어긋나지 않는다.
  "구조체 매핑 주석"으로 다듬으면 더 명확하다.

## Required fixes

없음. O1·O2 는 모두 선택 사항이며 게시 조건(`guide.md:206-220`)을 위반하지
않는다.