# Review · en-09.md vs ko-09.md

## Decision

PASS

## Fidelity

The English translation is a faithful, paragraph-level rendering of the Korean
source. Every section, table, diagram, and code block is preserved with
equivalent content and ordering:

- **Scope**: `src/kernels.cu` and `tests/test_softmax.cu`
  (`series.yaml:29-30`), Claim 8 owned + reference, Claim 2 Limitation by
  reference, evidence ranges (`kernels.cu:509-519`/`:522`/
  `claims.md:80-88`, `:489-493`/`:494-507`, `claims.md:24`/`recon.md:12`),
  boundary deferrals (`briefs.md:213-233`, `briefs.md:204`) — all match.
- **Why online softmax**: three-state table (`:474`/`:475`/`:476`),
  `d` comment quoted verbatim, no score-row array, token loop `:478-482` —
  all match.
- **Update rule**: `current_max`/`cf`/`exp_score`/`d`/`acc` pseudocode
  (`:510-519`), `acc/d` output (`:522`), single-invocation (not incremental)
  semantics — all match.
- **Warp tree sum**: `HEAD_DIM=64` (`:525-527`, `:11`), the shfl offsets
  16·8·4·2·1 (`:489-493`), `dot_products[2]` combine (`:463`, `:494-507`),
  `SQRT_HEAD_DIM=8` (`:12`), cross-check with `main.cpp:649` and
  `main.cpp:298`, `WARP_FULL_MASK` split (`cuda_to_hip.h:50`/`:59`) —
  all match.
- **Prefill vs decode**: `:281` rescale, `trace.md:112-113` — match.
- **Call site**: KV scatter (`main.cpp:851-873`), H2D sync (`:876`),
  `pagedAttention` (`:878`), grid/block launch (`kernels.cu:525-527`),
  `num_blocks`/`tokens_in_block` masking (`:471`, `:481-482`,
  `claims.md:88`) — all match.
- **Verification / not covered / limitations / summary**: `test_softmax.cu`
  (`recon.md:12`), `recon.md:4`, `briefs.md:203`, `guide.md:7-9`,
  `trace.md:216-219` (`softmaxDecode`/`softmaxKernelDecode` dead code),
  all four limitation bullets, the two ASCII diagrams and the summary — all
  match.

Source citations were verified directly against the pinned commit
`1896ff5c37a241050dbcd9527caf9dc1d3087a61` (`src/kernels.cu:461-523`,
`:408-458`, `:474-476`, `:489-508`, `:510-522`, `:525-527`, `src/main.cpp:298`,
`:649`, `:851-878`, `src/cuda_to_hip.h:50`, `:59`) and against the evidence
files (`evidence/claims.md:80-88`, `:24`, `:88`, `evidence/recon.md:4`, `:12`,
`evidence/trace.md:112-113`, `:216-219`, `briefs.md:203/204/213-233`,
`guide.md:7-9/11-16`, `series.yaml:29-30`). All are correct. No invented
facts were introduced.

Code identifiers (`current_max`/`acc`/`d`/`dot_products`/`SQRT_HEAD_DIM`),
API names (`__shfl_down_sync`, `expf`, `cudaMemcpy`), the quoted source
comment "denominator, same name as in paper above", token ID literals, and
commit-line citations are preserved verbatim. Terminology is consistently
rendered (prefill/decode/slot/block table/online softmax).

## Required fixes

None.