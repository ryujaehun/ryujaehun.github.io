# en-03 review — The prefill path that streams the whole prompt in one pass

Reviewed `articles/en-03.md` against the Korean source `articles/ko-03.md`, and
checked every citation against the pinned tree and the attached evidence
(`evidence/trace.md`, `evidence/claims.md`) for revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf`.

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the kernel ordering, index arithmetic, reduction
description, GQA ratios, scope delegation, and limitations match the Korean
exactly. The two Mermaid diagrams and their `<!-- visual: ... -->` markers are
retained verbatim in structure and content. All line citations are identical to
the Korean and consistent with the verified evidence. No translation-introduced
factual error was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 14 sections of the Korean are present and
  in order: question, where prefill is called, prefill kernel order, embedding
  and hidden_state init, RMSNorm/reduction, Q/K/V projection and RoPE, K/V paged
  scatter, attention scores and GQA, scores×V/O/residual/SwiGLU, final logits,
  tests and committed reference output, non-coverage, limitations, sources.
  Headings translate the Korean directly (`prefill 이 호출되는 자리` → "Where
  prefill is called"; `RMSNorm과 블록 내 병렬 리덕션` → "RMSNorm and intra-block
  parallel reduction"). No paragraph is omitted or added.
- **Citations preserved.** Every source range in the Korean appears identically
  in the English, including `40-62`, `79-147`, `150-553`, `555-1044`,
  `695-708`, `724-737`, `152-158`, `160-163`, `164-493`, `178-240`, `244-245`,
  `251-288`, `301-327`, `329`, `331`, `343-396`, `399`, `401`, `414-489`, `492`,
  `494`, `509-527`, `529-544`, `546-552`, and the kernel ranges
  `kernels.cu:32-53`, `55-94`, `96-152`, `173-222`, `224-309`, `311-344`. The
  inline citation link `src/kernels.cu:61-75` (`...L61-L75`) is reproduced
  unchanged.
- **Citations verified.** The citation anchors are identical to the Korean and
  match the evidence: host functions and their ranges (`trace.md:26-29`),
  prefill call sites `695-708`/`724-737` (`trace.md:75-77`, `123-126`), the
  ordered kernel path (`trace.md:79-116`, Claim 6), RMSNorm reduction
  `57/61/64-71/72-75/78-79` and launch `84-86` (Claim 7), GQA and `attn_alpha`
  `1/8 = 1/sqrt(64)` (Claim 8), paged scatter `264-277`/`280-287` (Claim 14,
  Claim 15), and the launch guards `205-209`, `241-245`, `295-299`
  (`trace.md:203-206`). The V-after-block-start placement described in the
  scatter section matches Claim 15 (`claims.md:287`).
- **Mermaid diagrams.** `prefill-kernel-sequence` retains all 19 nodes with the
  same labels and `main.cpp` line anchors; `rmsnorm-reduction` retains all four
  nodes with the same `kernels.cu` anchors (`61`, `64-71`, `72-75`, `78-79`).
  Both `<!-- visual: ... supports: [...] -->` markers are carried over exactly.
- **Technical content.** Embedding gather (`<<<num_input_tokens, 1024>>>`, the
  1024-thread cap comment), the D2D residual seed, the Q/K/V projection shapes
  and the `prompt_len` n-argument, RoPE applying to Q and K only (not
  `v_proj_temp_buf`), the `BLOCK_SIZE=16` / `16 × KV_DIM` block layout, the
  `-1`/`assert(false)` branch, the 32 Q heads at 4:1 over 8 K/V heads, the
  online softmax tree reduction, the in-place `silu`, and the final CPU argmax
  over only the last token row are all translated without drift.
- **Scope, limitations, performance framing.** The non-coverage delegation list
  (parts 1, 2, 4, 5/6, 6, 7), the GPU/CUDA-toolchain-absent caveat, the
  `code 6 / tests 1 / runtime 0` minimum evidence, the committed-artifact
  framing of `tests/test_softmax.cu` and the reference outputs, the
  `MAX_PROMPT_LEN = 512` constraint, the `proj_dim/2` thread counts and guard,
  the static nature of the buffer-alias claim, and the gated-model follow-up are
  all carried over exactly. No performance figure is present, so nothing can be
  read as a measured value.
- **Minor phrasing observations (non-blocking).** Two clauses are slightly
  awkward in English though their meaning survives: "When slots empty and refill
  is owned by part 7" renders `슬롯이 언제 비고 다시 채워지는지는 7편이 소유한다`
  and would read more cleanly as "when slots empty and are refilled is owned by
  part 7"; and "V at the block start plus `V_OFFSET` back" renders
  `V 는 블록 시작에서 V_OFFSET 만큼 뒤에`. Neither changes a citation, number, or
  technical claim, so neither is a required fix.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered, and the two phrasing observations above are optional polish only.
