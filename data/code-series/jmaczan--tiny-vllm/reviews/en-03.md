# en-03 review — The prefill path: flowing the entire prompt at once

Reviewed `articles/en-03.md` against the Korean source `articles/ko-03.md`
paragraph by paragraph, section by section, and at the token level for every
backtick-quoted identifier, citation, constant, and URL. Source-citation
accuracy of the Korean article against the pinned revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf` was already established in
`reviews/ko-03.md` (PASS, including a `diff` re-verification of the
`reference.txt`/`python/rms_norm_crosscheck.txt` 1-151 line-identity claim);
the task here is confirming the English renders that Korean faithfully and
carries the verified citations over unchanged.

## Decision

**PASS**

The English translation is a complete, faithful rendering of the Korean
source. All 12 sections appear in the same order with directly translated
headings, no paragraph is added or dropped, and every source citation, code
block, constant, URL, and visual marker is carried over unchanged. No
translation-introduced error was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All sections present and in order: the
  prefill entry point, the kernel-sequence map, embeddingGather, RMSNorm
  (with the reference-output evidence), Q/K/V projection, RoPE, K/V into
  paged blocks, per-head scores and GQA, causalMask and online softmax,
  scores×V → O → residual → SwiGLU, final RMSNorm/logits/argmax, further
  reading, and limitations. Heading titles translate the Korean directly
  (`토큰 ID 를 임베딩으로: embeddingGather` → "From token ID to embedding:
  embeddingGather"; `Q/K/V 투영: cuBLAS 세 번` → "Q/K/V projection: three
  cuBLAS calls"; `causalMask 와 온라인 softmax` → "causalMask and online
  softmax"; `이 글의 한계` → "Limitations of this article").
- **Prose fidelity.** Every technical claim reads the Korean correctly:
  prefill vs. decode's batch-vs-token dimension, `N_LAYERS = 16`, the
  thread-per-block 1024 limit behind `<<<num_input_tokens, 1024>>>`, the tree
  reduction and its `sqrt(sum/2048 + 1e-5)`, row-major vs. column-major with
  the transpose convention deferred to Part 4, the precomputed RoPE tables and
  the launch-guard contrast (token count for causalMask/softmax vs. projection
  width for `rope()`, `MAX_PROMPT_LEN = 512`), block allocation with
  `assert(false)` and its decode meaning deferred to Part 5, GQA's
  `k_head_idx = i / 4` and `attn_alpha = 1.0f / 8.0f`, the `-HUGE_VALF` mask
  rationale, the online-softmax merge rule (`m_new = max(m_a, m_b)`,
  `d_new = d_a·exp(m_a - m_new) + d_b·exp(m_b - m_new)`), the `silu` in-place
  gate overwrite, and the CPU argmax TODO at `last_token_offset`. Numeric
  values, identifiers, and constants are all preserved verbatim.
- **Citations preserved and unchanged.** A token-level comparison shows every
  citation string byte-identical to the Korean: `src/main.cpp:152-166`,
  `177-196`, `244-245`, `264-287`, `301-327`, `343-350`, `378-396`, `455-459`,
  `471-489`, `494`, `509-527`, `531-532`, `533-543`, `546-552`, `572`, `647-648`,
  `649`; `src/kernels.cu:32-45`, `55-81`, `96-152`, `173-200`, `202-212`,
  `224-237`, `247`, `257-290`, `311-316`, `331-338`; `reference.txt:78-90`;
  `python/reference.py:43-44`, `python/rms_norm.py:43-44`;
  `tests/test_softmax.cu:1-52`, `87-107`, `114-119`. All source permalinks
  point at the pinned revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf`, and
  the two external paper/lecture URLs (Zhang & Sennrich, Ainslie et al.,
  CSE599M, Fleetwood) and the six "Further reading" links are identical to the
  Korean. All code blocks are carried over verbatim (code is not translated).
- **Visual assets.** Both `<!-- visual: ... -->` markers
  (`prefill-kernel-sequence`, `rmsnorm-reduction`) are retained verbatim, and
  both mermaid diagrams are node-for-node identical to the Korean with only the
  labels translated (`블록 내 트리 리덕션` → "in-block tree reduction",
  `온라인 병합` → "online merge", `레이어 16개를 다 돌았나?` → "all 16 layers
  done?").
- **Reference output.** The ten bfloat16 values quoted from `reference.txt:78-90`
  (`0.021240234375` … `0.0225830078125`) and the `Token IDs: [128000, 791,
  6864, 315, 9822, 374]` / `torch.Size([1, 6, 2048])` lines match the Korean
  exactly, as does the claim about the 1-151 identical front portions of the
  two crosscheck files.
- **Test evidence.** The `tests/test_softmax.cu` code excerpts (`87-107`,
  `114-119`), the five stress-case labels (`ordinary_8`, `tied_max_8`,
  `stability_spike_8`, `all_negative_8`, `bigger_16`), and the attribution of
  the "bit-identical to two-pass" claim to the file's own header comment are
  translated without distortion.
- **Deferrals and cross-references.** Ownership/deferral references are
  carried over exactly: column-major convention to Part 4, decode warp-shuffle
  and the decode block branch to Parts 5, block layout and reading-side kernel
  to Part 6, the `buf_2048_1` alias from Part 2, and the PagedAttention
  write-side cross-reference to Part 1. None are altered or dropped.
- **Limitations.** The not-built/not-run disclosure, the commit `e25bf19`
  source-reading basis, the "quoted committed reference output, not re-run
  measurements" caveat, and the no-GPU/no-CUDA-toolchain statement all match
  the Korean. No measured performance figures are introduced.

## Required fixes

None. The translation is complete and accurate; no citation, number, constant,
URL, code block, or claim was altered, and no content was added or omitted.