# en-03.md 리뷰

검토 기준: 한국어 원본 `articles/ko-03.md`와 영어 번역 `articles/en-03.md`를
대조해 충실도·누락·오역을 검사했고, 고정 커밋
`1896ff5c37a241050dbcd9527caf9dc1d3087a61`의 실제 소스(`src/main.cpp`,
`src/kernels.cu`)와 `evidence/claims.md`, `evidence/trace.md`,
`evidence/recon.md`, `guide.md`, `series.yaml`로 번역이 인용한 모든 줄 번호를
일일이 대조했다. 프롬프트 4개(17·14·13·14 토큰)는 벡터를 직접 세어 확인했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 문단·번호 목록·표·ASCII 다이어그램까지 빠짐없이
옮겼으며, 의미 왜곡·누락·오역이 발견되지 않았다. 번역이 인용한 모든 소스
경로·줄 범위와 근거 문서 참조가 고정 커밋 소스 및 근거 문서와 일치한다.
한국어 리뷰(`ko-03.md`)가 지적한 3건의 표현상 정밀도 문제는 원본(`ko-03.md`)에
이미 존재하는 내용으로 번역이 그대로 옮긴 것이며, 번역 오류가 아니다.

## Fidelity

**구조·범위** — 9개 절(제목·서문·파일과 범위·프롬프트는 이미 토큰·embeddingGather·
두 가지 gather·CPU argmax·검증 경로·다루지 않는 것·한계·정리)이 모두 존재하고
순서가 원본과 일치한다. prefill 4단계 번호 목록, 7행 비교 표, 5줄 ASCII
다이어그램, "다루지 않는 것" 불릿 5개, "한계" 불릿 8개, 정리 2문단이 원본과
1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`loadWeights`, `embed_tokens`,
`embeddingGather`, `embeddingGatherDecode`, `hidden_state`,
`gpu_input_tokens`, `gpu_last_tokens`, `input_embeddings`,
`embed_proj_cpu`, `rmsNorm`, `causalMask`, `softmax`, `MAX_SEQ_LEN`,
`EOT_ID`, `VOCAB_SIZE`, `EMBEDDING_LENGTH`, `MAX_BUFFER_SIZE`,
`tests/test_softmax.cu`, `python/tokenizer.py`)가 원문 그대로 보존되었다.
`Claim 2/3/4/9`, 장 번호(4·5·6·7·8·10장), `guide.md`/`series.yaml`/`evidence/*.md`
인용 표기도 동일하다. 따옴표 안의 Claim 요약 문구("four fixed prompts in the
queue", "batch the active slots together and run the argmax on the CPU")도
원문과 일치한다.

**인용 대조 (고정 커밋 소스로 직접 확인)** — 다음이 모두 정확했다.

- 프롬프트 4개가 토큰 ID 벡터로 하드코딩됨 `src/main.cpp:584-594`, 길이
  17·14·13·14(직접 계수) ✓
- include 목록 `src/main.cpp:1-8`(`iostream`, `numeric`, `fstream`,
  `cuda_to_hip.h`, `queue`, `json.hpp`, `kernels.cuh`) ✓
- prefill 경계 `:150-553`, 프롬프트 pop `:152-155`, GPU 복사 `:157`,
  `embeddingGather` 호출 `:158`, `hidden_state` 복사 `:160-163` ✓
- `embeddingGather` 래퍼 `src/kernels.cu:42-53`(실행 구성 `<<<num_input_tokens,
  1024>>>`), 커널은 `:32-40` ✓
- 오프셋 기반 포인터 배정 `src/main.cpp:132-145`(embed_tokens 포함) ✓
- 버퍼 라벨 `input_embeddings(:620)`, `hidden_state(:623)` ✓
- 최종 rmsNorm `:494`, 로짓 GEMM `:509-527`, D2H `:529`, CPU argmax
  `:533-543`, 출력 `:544`, 상태 갱신 `:546-548` ✓
- decode GPU 복사 `:749-757`, `embeddingGatherDecode` 호출 `:759` ✓
- `embeddingGatherDecode` 커널 `src/kernels.cu:347-356`, 래퍼 `:358-369` ✓
- decode 최종 rmsNorm `:974`, 로짓 GEMM `:976-994`, D2H `:996`, 슬롯별
  argmax `:1000-1012`, bf16→float 캐스팅 `:1003-1010`, EOT/EOT_ID/최대 길이
  슬롯 해제 `:1015-1031`, 갱신·길이 +1 `:1032-1037` ✓
- `embed_proj_cpu` 할당 `:681-682`, TODO `:686`(`:531-532` 포함) ✓
- `prompt_len > 1024` 가드 `src/kernels.cu:241-244`, `:295-298` ✓
- `evidence/trace.md:69`(길이 17/14/13/14), `:91-92`(블록=토큰 수,
  스레드=1024), `:141-142`(블록=활성 슬롯 수), `:240-242`(embed_proj_cpu
  잔여 데이터) ✓
- `evidence/claims.md:23`(로짓 GEMM 형태), `:28`(Claim 3 최종 rmsNorm 후
  로짓 GEMM), `:34`("1024 스레드×2" 표기), `:35`(17토큰 이하), `:39`(Claim 4
  CPU argmax), `:47`(D2H 비용 TODO), `:99`(고정 4개 프롬프트) ✓
- `evidence/recon.md:4`(저장소 미실행), `:12`(유일 테스트
  `tests/test_softmax.cu`), `:13`(파이썬 참조 구현군) ✓
- `guide.md:11-16`(증거 규칙), `series.yaml:10-12`(3장 대상 파일) ✓

**원본과 동일하게 번역으로 이월된 표현상 정밀도 사항** (번역 오류 아님,
`ko-03.md` Problems 1~3과 동일):

1. "the three steps" 뒤 번호 목록이 4개(프롬프트 pop, GPU 복사,
   `embeddingGather`, `hidden_state` 복사)다. 숫자와 항목 수가 어긋난다.
2. decode gather 표의 스레드 열을 "no evidence"로 비워 두었는데, 인용된 래퍼
   범위 `src/kernels.cu:358-369` 내부인 `:361`에
   `embeddingGatherKernelDecode<<<num_tokens, 1024>>>`가 있어 스레드 수 1024가
   소스에 존재한다. 한계 절의 "추적 메모에 없어 비워 두었다"는 정확하지만,
   표의 "no evidence"는 소스 근거까지 없는 듯 읽힐 수 있다.
3. "2048 elements per token, 1024 threads×2"는 Claim 3의 표현
   (`evidence/claims.md:34`)이며 "×2"(스레드당 2요소 수집)는 커널 본문
   `src/kernels.cu:37-38`에 있다. 인용이 틀렸다고 볼 수는 없으나 커널 본문
   범위(`:32-40`)를 병기하면 더 정밀하다.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다.

- (선택) 원본의 표현상 정밀도 문제(위 1~3)를 해결하려면 원본(`ko-03.md`)을
  먼저 수정하고 번역을 재동기화할 것. 이 리뷰는 번역 충실도만 판정하며 원본
  내용의 오류로 보지 않는다.