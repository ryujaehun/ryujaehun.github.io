# ko-07 리뷰 — 슬롯과 큐로 여러 요청을 겹치는 continuous batching

고정 revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf` 체크아웃
(`/home/jaehun/blog/.cache/code-series/repos/jmaczan--tiny-vllm/e25bf1994efa90bc98b721ba7c527402f86fbeaf`)의
`src/main.cpp`, `src/kernels.cu`, `python/batching_test_tokens.py`, `README.md`
실제 본문과 첨부 증거(`guide.md`, `series.yaml`, `evidence/claims.md`,
`evidence/trace.md`), 그리고 이전 편 리뷰(`ko-01`~`ko-06`, `en-07`)를 기준으로
`articles/ko-07.md` 의 모든 기술 주장·인용·범위·시각 자료·성능 서술을 대조했다.
초안이 인용한 줄 번호 전부를 소스 본문과 문자 단위로 직접 대조했고, 교차 참조
(1·3·5·6편)는 해당 편 초안에서 존재를 확인했다.

## Decision

**PASS**

큐의 토큰 열과 길이(`main.cpp:583-594`, 17/14/13/14), `BATCH_SIZE=2` 주석과
`MAX_SEQUENCES=BATCH_SIZE`(`main.cpp:38`), 2×16×128=4096·슬롯당 2048 항목 산술,
`is_slot_free`·세 슬롯 벡터·`active_slots`/`active_tokens`(597, 599-601, 604-605),
초기 prefill 루프 조건 `slot < is_slot_free.size() && !queue.empty()`(695-708),
prefill 진입부(152-155)와 상태 기록(`current_prompt_len = prompt_len`, 548),
decode 루프 머리(720-746)의 슬롯 재구성·`push_back` 이 `if` 블록 밖(735-736)·
break/continue 분기(738-746)·TODO 주석(743), H2D 세 배열(749-757)과 소비자 분리
(`embeddingGatherDecode` 759 vs `pagedAttention` 878), `seq_lens = current_prompt_len + 1`
(755)이 `kernels.cu:471` 의 `(seq_len + BLOCK_SIZE - 1) / BLOCK_SIZE` 로 소비되는
흐름, 종료 분기(1015-1037)의 조건·슬롯 해제·세 차원 순회 반납·`block_table_gpu`
H2D 동기화(1030), `MAX_NEW_TOKENS_GENERATED = 20`(`main.cpp:12`) 미참조 — 전부 고정
revision 소스와 줄 단위로 일치한다. `python/batching_test_tokens.py:1-15` 인용도
문자 단위로 일치하고, 스크립트 출력 주석(`length 17/14/13/14`)이 `main.cpp` 의 네
push 와 정확히 맞는다. `slot-lifecycle` mermaid 마커와 `series.yaml` 의
`required_claims` 가 일치하고, 근거 최소값(`code 4 / tests 0 / runtime 0`)을
넘는다. 성능 수치는 전혀 없으며, 한계 절의 네 지점이 결정 003·`claims.md`
Limitation·`trace.md` 미해결 항목에 그대로 대응한다. 아래 Problems 는 모두
비차단 관찰이다.

## Verified claims

고정 revision 소스와 정확히 일치함을 확인했다.

- **큐의 네 프롬프트.** `main.cpp:583-594` 인용이 문자 단위로 일치한다. 벡터의
  토큰 수를 세어 17·14·13·14 를 확인했다. `python/batching_test_tokens.py:1-15`
  인용도 소스와 문자 단위로 일치하고, 템플릿 문자열(`<|begin_of_text|>...
  <|start_header_id|>assistant<|end_header_id|>`)과 `add_special_tokens=False`,
  주석 출력 형식이 모두 맞다. 스크립트의 실제 출력(코멘트된 `input_tokens.
  push_back` 열)이 `main.cpp` 의 `queue.push` 토큰 ID 열과 정확히 일치한다.
- **상수와 크기.** `BATCH_SIZE = 2` 의 주석("not even close to being good, it's
  just here to have batching")이 `main.cpp:29` 에 그대로 있고, `MAX_SEQUENCES =
  BATCH_SIZE` 가 `main.cpp:38` 에 있다. 2×16×128=4096, 슬롯당 16×128=2048 항목
  묶음이라는 산술이 맞다. 초안이 `MAX_SEQUENCES` 를 `main.cpp:38` 로 인용한 것은
  `claims.md` 보다 정확한 소스 직접 확인이다.
- **슬롯 상태 벡터.** `is_slot_free(BATCH_SIZE, true)`(597)와 주석("set to false
  when slot taken, set to true when free"), `generated_tokens`·`last_generated_
  tokens`·`current_prompt_len(BATCH_SIZE, 0)`(599-601), "needed to provide
  contiguous data for decode" 주석과 `active_slots`·`active_tokens`(604-605)가
  전부 소스와 일치한다.
- **초기 채움 루프.** `main.cpp:695-708` 인용이 문자 단위로 일치한다. 조건은
  `slot < is_slot_free.size() && !queue.empty()` 로, 초안 인용이 `trace.md:75` 의
  축약(`slot < BATCH_SIZE`)보다 소스에 정확하다. `BATCH_SIZE=2`·큐 4개이므로
  슬롯 0·1 이 프롬프트 0·1 을 가져가고 프롬프트 2·3 이 남는 서술이 맞다. `continue`
  가 초기에는 발동하지 않는다는 관찰도 옳다.
- **prefill 진입부와 상태 기록.** `prompt = queue.front()`, `prompt_len =
  prompt.size()`, `queue.pop()`, `is_slot_free[slot] = false`(152-155)가 문자
  단위로 일치한다. "큐에서 슬롯으로 프롬프트가 옮겨가는 유일한 지점"이라는 서술이
  맞다. `current_prompt_len[slot] = prompt_len`(548)과 그 뒤 block table H2D
  동기화(552)도 확인했다.
- **decode 루프 머리.** `main.cpp:720-746` 인용이 문자 단위로 일치한다.
  `while(true)` 주석("supposed to run foreveeer!!!", 720), 매 반복 두 활성 배열
  초기화(722-723), 슬롯별 분기(726-734, `generated_tokens[slot].clear()` 는 732),
  `active_slots.push_back(slot)` 이 `if` 블록 밖(735)·`active_tokens.push_back`
  (736), `num_active_slots == 0` 분기(738-746)가 전부 소스와 맞다. break 의 TODO
  주석(743)도 문자 단위로 일치한다. "방금 채운 슬롯도, 점유 중인 슬롯도 이번 스텝의
  활성 목록에 들어간다"는 해석이 정확하다. break 로 끝나는 유한 실행이라는 서술도
  `trace.md:162-164` 와 일치한다.
- **활성 배열 H2D 와 소비자 분리.** `main.cpp:748-757` 인용이 문자 단위로
  일치한다. `gpu_last_tokens`(749)·`gpu_active_slots`(750)·`seq_lens =
  current_prompt_len[active_slot] + 1`(752-755)·`gpu_seq_lens`(757). 소비자 분리
  서술이 정확하다. `embeddingGatherDecode`(759)는 `gpu_last_tokens` 를 쓰고,
  `pagedAttention`(878)은 `kv_cache`·`block_table_gpu`·`gpu_seq_lens`·
  `gpu_active_slots` 네 상태만 KV 관련 인자로 받는다(`kernels.cu:465,470,480`).
- **`+1` 의 소비.** `seq_lens` 의 `+1` 이 6편의 `(seq_len + BLOCK_SIZE - 1) /
  BLOCK_SIZE`(`kernels.cu:471`)로 소비된다는 서술이 소스와 일치한다. 5편의
  `ko-05.md:24` 교차 참조도 그대로 존재한다.
- **종료와 반납.** `main.cpp:1015-1037` 인용이 문자 단위로 일치한다. 조건 세 가지
  (`END_OF_TEXT_TOKEN_ID`=128001 `main.cpp:26`, `EOT_ID_TOKEN_ID`=128009
  `main.cpp:27`, `current_prompt_len == MAX_SEQ_LEN-1` 1015)가 맞고, 마지막 조건이
  생성 토큰 수가 아니라 총 시퀀스 길이의 상한이라는 해석이 정확하다. 종료 시 슬롯
  해제(1017)·세 차원 순회 반납(1018-1029)·`block_table_gpu` H2D(1030)와
  else 분기(1032-1037)가 전부 소스와 맞다. "반납된 물리 블록을 다른 슬롯의
  `pop_back` 이 다시 쓴다"는 서술도 `free_blocks` 흐름과 일치한다.
- **`MAX_NEW_TOKENS_GENERATED` 미참조.** `main.cpp:12` 선언 한 곳뿐임을 grep 으로
  확인했다. "종료 조건으로 동작하지 않는다"는 서술이 정확하다.
- **시각 자료·범위·교차 참조.** `<!-- visual: slot-lifecycle supports:
  [slot-lifecycle] -->` 마커가 `series.yaml` 의 `required_claims:
  [slot-lifecycle]`(mermaid)과 대응하고, 다이어그램의 각 노드 라인 번호(597, 695-708,
  152-155, 720, 722-723, 726-734, 735-736, 738-746, 1040, 748-757, 759, 878, 1015,
  1015-1031, 1032-1037)가 소스와 일치한다. 교차 참조 — 1편의 토큰 ID 설명(`ko-01.
  md:76`), 3편의 prefill 상태 기록·블록 동기화(`ko-03.md:497`), 5편의 decode 커널
  변형(`ko-05.md:24`), 6편의 block table 읽기·세 차원 인덱스·`ko-06.md`)가 각 편
  초안에 존재한다. "younger and smaller sibling"(`README.md:3`)과 README 의
  continuous batching 문단도 저장소 본문에서 의미가 일치함을 확인했다.
- **성능 서술.** 커널 실행 시간·처리량·메모리 사용량 수치가 전혀 없다. H2D 비용은
  "측정하지 않았다"고 명시한다. 한계 절의 네 지점(EOS/EOT·길이 한도 종료만 존재,
  break 로 끝나는 유한 실행, `+1` 해석의 실행 미검증, 레이어마다·반납 후 block
  table H2D 미측정)이 `claims.md` Claim 13·20 의 Limitation 과 `trace.md` 미해결
  항목에 그대로 대응한다. gated 가중치 한정도 `guide.md:78-79` 와 일치한다.

## Problems

비차단 관찰이다. guide 의 통과 조건(모든 기술 주장의 permalink 인용, 최소 근거
code 4 충족, runtime 0, required_claims 충족)을 어기지 않는다.

1. **README 인용에 permalink 가 없다.** 서두(3행)의 continuous batching 문단 인용과
   "더 읽을거리" 의 "younger and smaller sibling"(`README.md:3`)이 permalink 없이
   서술된다. 내용은 저장소 본문과 일치함을 확인했고 기술 오류는 아니지만, guide 의
   "고정 revision permalink 인용" 요건 관점에서 링크가 있으면 독자가 확인 지점을
   얻는다.
2. **(정보용) 제목 불일치.** 초안 제목 "슬롯과 큐로 여러 요청을 겹치기 —
   continuous batching 의 구현"이 `series.yaml` 챕터 제목("슬롯 테이블로 여러
   요청을 함께 돌리기") 및 `en-07.md` 제목("Running Multiple Requests Together
   with a Slot Table")과 다르다. 기술 주장과는 무관한 표제 정밀도다.
3. **(정보용) 스크립트 출력 형식.** "main.cpp 의 네 push 는 그 출력을 옮겨 적은
   것이다"에서 `batching_test_tokens.py` 의 출력은 `input_tokens.push_back(...)`
   형식이고 `main.cpp` 는 `queue.push({...})` 형식이라 표기만 다르며 토큰 ID 열은
   정확히 일치한다. 사실과 어긋나지 않는다.

## Required fixes

**없음.** PASS 조건을 모두 충족한다. Problem 1 은 표기 정밀도, Problem 2·3 은
정보용이며 어느 것도 초안의 기술 주장을 뒤집지 않는다.