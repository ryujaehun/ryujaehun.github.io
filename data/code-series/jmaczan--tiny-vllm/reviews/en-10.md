# Review · en-10.md vs ko-10.md

## Decision

PASS

## Fidelity

The English translation is a faithful, paragraph-level rendering of the Korean
source. Every section, table, diagram, and code block is preserved with
equivalent content and ordering:

- **Intro / scope**: chapter 9 handoff (`kernels.cu:461-523`), the
  physical block pool / logical→physical table / kernel traversal skeleton
  (`Claim 5`, `Claim 10`, `Claim 8`), primary ownership of Claim 8 with
  Claims 5·6·7·10 as reference/owned (`claims.md:80-88`, `:49-57`, `:59-67`,
  `:69-78`, `:102-110`), pinned commit `1896ff5c...` evidence rules
  (`guide.md:5-9`, `:11-16`), `series.yaml:31-33` scope — all match.
- **KV cache**: `2GiB`/`BLOCK_SIZE=16`/`KV_DIM=512`/bf16/`V_OFFSET`/
  `BLOCK_BYTES=32768`/`NUM_BLOCKS=65536`/`MAX_BLOCKS_PER_SEQ=128`
  (`main.cpp:32-37`), redefinition TODO (`kernels.cu:6`, `:16-19`,
  `claims.md:57`), three-piece pool management (`main.cpp:574-581`,
  `claims.md:51`), K/V byte offsets (`trace.md:105-106`, `:152`), ASCII
  layout diagram — all match.
- **KV scatter**: prefill block-wise pop with `assert(false)` (`main.cpp:
  251-288`, split `:251-264`/`:264-272`/`:273-277`/`:280-287`), decode
  token-wise with pop only at `% == 0` (`:851-873`, split `:856-857`/
  `:859-865`/`:866-872`), both D2D `cudaMemcpy` (`claims.md:59-60`, `:67`) —
  all match.
- **GQA**: `NUM_Q_HEADS=32`/`NUM_K_HEADS=NUM_V_HEADS=8`/ratio 4
  (`main.cpp:20-23`, `kernels.cu:468`), block_table mapping independent of
  head, ownership in chapter 5 (`briefs.md:115`), recompilation note
  (`claims.md:78`) — all match.
- **pagedAttentionKernel**: grid `(num_active_slots, NUM_Q_HEADS)`, block
  `HEAD_DIM=64` (`kernels.cu:525-527`), call site (`main.cpp:878`), five
  numbered steps (`:464-469`/`:471`/`:478-482`/`:484-508`/`:510-519`,
  `:463`, `:522`), dual indexing with `gpu_active_slots` (`:464-470`, `:480`,
  `main.cpp:722-737`, `claims.md:39`, `trace.md:235-239`), two flow diagrams
  — all match.
- **Causal masking**: `num_blocks`/`tokens_in_block` (`kernels.cu:471`,
  `:481-482`), seq_len=34 example, `claims.md:88` — all match.
- **Resync**: KV scatter → H2D full copy → call (`main.cpp:851-873`, `:876`,
  `:878`), sync size `2×16×128×4` (`claims.md:107`), other sync points
  (`:552`, `:1030`, `claims.md:104`), TODO at `:551` (`claims.md:110`) — all
  match.
- **Verification / not covered / limitations / summary**: `test_softmax.cu`
  (`recon.md:4`, `:12`, `:38`), boundary deferrals (`briefs.md:226`, `:115`,
  `:229`, `guide.md:7-9`, `guide.md:11-16`, `briefs.md:142`), all six
  limitation bullets including `dot_products[2]`/`HEAD_DIM` (`kernels.cu:
  525-527`, `:489-493`, `:494-501`, `:463`) and `WARP_FULL_MASK`
  (`cuda_to_hip.h:50`, `:59`), the summary — all match.

Source citations were verified directly against the pinned commit
`1896ff5c37a241050dbcd9527caf9dc1d3087a61` (`src/main.cpp:32-37`, `:247-288`,
`:574-581`, `:851-878`, `:722-737`, `:552`, `:1030`, `:551`;
`src/kernels.cu:461-523`, `:468`, `:525-527`, `:16-19`, `:6`;
`src/cuda_to_hip.h:50`, `:59`) and against the evidence files
(`evidence/claims.md:80-88`, `:49-57`, `:59-67`, `:69-78`, `:102-110`, `:51`,
`:57`, `:59-60`, `:67`, `:78`, `:39`, `:88`, `:104`, `:107`, `:110`,
`evidence/recon.md:4`, `:12`, `:38`, `evidence/trace.md:105-106`, `:152`,
`:235-239`, `briefs.md:115`, `:142`, `:226`, `:229`, `guide.md:5-9`, `:11-16`,
`:24-29`, `series.yaml:31-33`). All are correct. No invented facts were
introduced.

Code identifiers (`pagedAttentionKernel`, `block_table_gpu`, `gpu_active_slots`,
`kv_head_idx`, `BLOCK_BYTES`, `V_OFFSET`, `KV_CACHE_SIZE_BYTES`,
`MAX_BLOCKS_PER_SEQ`, `free_blocks`, `current_max`/`d`/`acc`,
`dot_products`, `HEAD_DIM`, `WARP_FULL_MASK`, `__shfl_down_sync`), the quoted
TODO ("do it more clever and not copy full table unnecessarily"), and
commit-line citations are preserved verbatim. Terminology is consistently
rendered (prefill/decode/slot/block table/online softmax/logical vs physical
block).

## Required fixes

None.