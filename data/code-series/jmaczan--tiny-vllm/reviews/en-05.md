# en-05 review — The decode path that produces one token

Reviewed `articles/en-05.md` against the Korean source `articles/ko-05.md`
paragraph by paragraph, section by section, and at the token level for every
backtick-quoted identifier, citation, constant, and URL. A `rg` comparison of
both files confirms the citation sets are byte-identical, the code blocks are
byte-identical, the URL sets are identical, the visual marker is retained
verbatim, and all 11 sections appear in the same order with directly
translated headings.

## Decision

**PASS**

The English translation is a complete, faithful rendering of the Korean
source. All 11 sections appear in the same order, no paragraph is added or
dropped, and every source citation, code block, constant, URL, and visual
marker is carried over unchanged. No translation-introduced error was found,
and no fix is required.

## Fidelity

- **Structure and completeness.** All 11 sections present and in order: the
  decode loop start, the prefill/decode contrast table, the embedding kernel,
  RoPE, the K/V batch-and-scatter step, paged attention, remaining layers and
  logits, the verification script, further reading, and limitations. Heading
  titles translate the Korean directly (`임베딩: blockIdx.x 는 슬롯이다` →
  "Embedding: `blockIdx.x` is the slot"; `K/V 를 배치 버퍼에 모아 블록에
  흩뿌리기` → "Gathering K/V into a batched buffer and scattering into
  blocks"; `이 글의 한계` → "Limitations of this article"). The title keeps
  the em-dash structure of the Korean.
- **Prose fidelity.** Every technical claim reads the Korean correctly: the
  `while (true)` loop head at `src/main.cpp:720-1039` rebuilding slot state,
  the `seq_lens[slot] = current_prompt_len[active_slot] + 1` upload and the
  reasoning that the length must include the current token before attention
  (`src/main.cpp:851-873` writes before the `gpu_seq_lens`-driven block
  count at `src/kernels.cu:470-481`); the four decode-only kernels
  (`embeddingGatherDecode`, `ropeDecode`, `softmaxDecode`,
  `pagedAttention`); `blockIdx.x` meaning the slot in
  `embeddingGatherKernelDecode`; RoPE's single-position, per-call
  recomputation with the hardcoded `500000.0`/`32` and the TODO at
  `src/kernels.cu:375`; the Llama 3 frequency-scaling difference between the
  prefill table and the unscaled decode recomputation; the per-token K/V
  copy with block allocation only at offset 0 (`token_in_block_idx == 0`),
  `V_OFFSET` and `BLOCK_BYTES` (`src/main.cpp:33-34`); the single
  `pagedAttention` call replacing `causalMask`+`softmax`, with online softmax
  and "iterating only up to the sequence length" standing in for masking;
  the never-called `softmaxDecode`/`softmaxKernelDecode` with its
  `MAX_SEQ_LEN`-stride assumption vs. prefill's `num_tokens` stride; the
  shared post-attention kernels with only the token-count argument differing;
  the per-slot CPU argmax over `VOCAB_SIZE`; and the EOT/EOT_ID/`MAX_SEQ_LEN-1`
  termination branch deferred to part 7. Numeric values, identifiers, and
  constants are all preserved verbatim.
- **Citations preserved and unchanged.** A token-level comparison shows every
  citation string byte-identical to the Korean (the same set in both files,
  e.g. `src/kernels.cu:26-344` and `346-523` for the prefill/decode
  sections, `src/kernels.cuh:22-28` and `25`, `src/kernels.cu:32-40` vs
  `347-356` and `361` for the embedding kernels, `173-200`/`371-384` and
  `375`/`375-382`/`376-377`/`378`/`397` for RoPE, `106-124` and `96-152` for
  `init_rope_frequencies`, `main.cpp:802-821`/`824-842`/`683-693`/
  `844-849`/`847-848`/`851-873`/`855-872`/`859-865`/`876`/`551` for the K/V
  scatter, `main.cpp:329`/`331` vs `kernels.cu:461`/`470-481`/`473`/
  `478-485`/`486-519`/`509-519`/`522`/`413`/`262` for attention, and the
  logits/slot-release spans `974`/`976-994`/`996`/`998-1012`/`1015-1037`).
  All source permalinks point at the pinned revision
  `e25bf1994efa90bc98b721ba7c527402f86fbeaf`, and the four "Further reading"
  URLs (Fleetwood RoPE, FlashAttention notes, PagedAttention paper, CUDA
  reduction) are identical to the Korean.
- **Code and identifiers.** All ten `cpp` code blocks are byte-identical
  between the two files (code is not translated), as is every backtick-quoted
  identifier and constant (`embeddingGatherKernelDecode`, `gpu_last_tokens`,
  `ropeKernelDecode`, `position_in_sequence`, `init_rope_frequencies`,
  `pagedAttentionKernel`, `block_table_gpu`, `gpu_seq_lens`,
  `gpu_active_slots`, `k_proj_batched_buffer`, `v_proj_batched_buffer`,
  `BLOCK_SIZE`, `KV_DIM`, `V_OFFSET`, `BLOCK_BYTES`, `buf_2048_1`,
  `VOCAB_SIZE`, `embed_proj_cpu`, `<|end_of_text|>`(128001),
  `<|eot_id|>`(128009), etc.). Constants like `BATCH_SIZE=2`,
  `MAX_SEQ_LEN=2048`, `BLOCK_SIZE=16`, `500000.0`, `32`, `8192`, `factor`=32,
  `high_freq_factor`=4 are preserved.
- **Visual assets.** The `<!-- visual: prefill-decode-contrast supports:
  [prefill-decode-contrast] -->` marker is retained verbatim, and the
  prefill/decode contrast table has all seven rows translated faithfully with
  the "Evidence" (`근거`) citation columns unchanged.
- **Deferrals and cross-references.** Ownership/deferral references are
  carried over exactly: the paged KV cache block layout to part 6, the
  slot·queue lifetime, termination branch, and slot release to part 7, the
  transpose trick "same skeleton" from part 4, and the attention kernel's
  warp-shuffle reduction internals to part 6.
- **Limitations.** The not-built/not-run disclosure, the commit `e25bf19`
  source-reading basis, the three unverified points (Llama 3-scaled table vs.
  unscaled decode recomputation equality; the differing `MAX_SEQ_LEN` vs
  `num_tokens` stride assumptions with the uncalled `softmaxKernelDecode`;
  the per-layer full `block_table` H2D copy cost) all match the Korean, as
  does the compile-time-constant dependency note.

## Required fixes

None. The translation is complete and accurate; no citation, number, constant,
URL, code block, or claim was altered, and no content was added or omitted.
Two non-blocking stylistic nits only, neither of which changes meaning: the
heading "## Attention is finished off by `pagedAttentionKernel`" renders
`pagedAttentionKernel` in backticks where the Korean has no code formatting
(`어텐션은 pagedAttentionKernel 이 끝낸다`), and two sentences are slightly
awkward in English — "so the length has to contain **this token included**"
(line 24) and "the meaning of the rotation decode recomputes the angle for
every time" (line 238). Both are cosmetic; the Korean meaning survives intact.