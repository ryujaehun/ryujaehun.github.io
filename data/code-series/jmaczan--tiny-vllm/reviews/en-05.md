# en-05.md 리뷰

검토 기준: 한국어 원본 `articles/ko-05.md`와 영어 번역 `articles/en-05.md`를
대조해 충실도·누락·오역을 검사했고, 번역이 인용한 모든 소스 경로·줄 범위와
`evidence/claims.md`, `evidence/trace.md`, `evidence/recon.md`, `guide.md`,
`series.yaml` 참조가 한국어 원본과 1:1로 일치하는지 일일이 대조했다. 아울러
고정 커밋(`1896ff5c…`) 소스 `src/main.cpp`, `src/kernels.cu`를 직접 열어
번역이 인용한 줄 범위가 실제 코드 위치와 맞는지도 확인했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 절·다이어그램·불릿까지 빠짐없이 옮겼으며, 의미
왜곡·누락·오역이 발견되지 않았다. 번역이 인용한 모든 소스 경로·줄 범위와 근거
문서 참조가 한국어 원본과 정확히 일치하고, 원본이 세운 줄 범위 자체도 고정
커밋 소스와 실제로 일치한다.

## Fidelity

**구조·범위** — 11개 절(제목·서문·파일과 범위·KV 캐시 블록 풀·block_table·prefill
산포·decode 산포·GQA attention·검증 경로·다루지 않는 것·한계·정리)이 모두
존재하고 순서가 원본과 일치한다. ASCII 다이어그램 2개(캐시 구조, prefill/decode
비교), GQA 매핑 다이어그램 1개, 상수 블록 1개, "다루지 않는 것" 불릿 7개,
"한계" 불릿 6개, 정리 2문단이 원본과 1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`kv_cache`, `free_blocks`, `block_table`,
`block_table_gpu`, `BLOCK_SIZE`, `KV_DIM`, `V_OFFSET`, `BLOCK_BYTES`,
`KV_CACHE_SIZE_BYTES`, `MAX_BLOCKS_PER_SEQ`, `NUM_BLOCKS`, `token_idx`,
`block_idx`, `logical_block_idx`, `token_in_block_idx`, `seq_len`,
`k_head_idx`, `v_head_idx`, `kv_head_idx`, `GQA_Q_TO_K_RATIO`,
`GQA_ATTN_SCORES_TO_V_RATIO`, `NUM_Q_HEADS`, `NUM_K_HEADS`, `NUM_V_HEADS`,
`attn_alpha`, `attn_score_head`, `causalMask`, `softmax`, `prefill_attn_scores`,
`k_proj_temp_buf`, `v_proj_temp_buf`, `pagedAttentionKernel`, `gpu_active_slots`,
`HEAD_DIM`, `cudaMemcpy`, `cudaMalloc`, `std::iota`, `tests/test_softmax.cu`,
`python/reference.py`)가 원문 그대로 보존되었다. Claim 5/6/7, 장 번호
(4·7·8·9·10장), `guide.md`/`series.yaml`/`evidence/*.md` 인용 표기도 동일하다.
"산포"→"scatter", "흩뿌리다"→"scatter", "덮어쓰다"→"overwrite", "블록 한
판"→"block plane", "추적 메모"→"trace memo", "예약 방식"→"page reservation
scheme" 등 기술 용어 번역이 일관적이고 원문 뉘앙스를 유지한다.

**인용 대조 (원본·번역·실제 소스 3자 대조)** — 다음이 모두 번역에서 그대로
유지되었고, 실제 소스와도 맞는다.

- `src/main.cpp:32-37` 상수(`BLOCK_SIZE=16`, `V_OFFSET=16384`, `BLOCK_BYTES=
  32768`, `KV_CACHE_SIZE_BYTES=2GiB`, `MAX_BLOCKS_PER_SEQ=128`,
  `NUM_BLOCKS=65536`) ✓
- KV 캐시 초기화 `:574-581`(`kv_cache` cudaMalloc, `free_blocks` iota,
  `block_table` -1, `block_table_gpu` 할당) ✓
- `block_table` 인덱싱 `:265`/`:271`/`:858`/`:864`, 캐시 주소 곱셈
  `:280`/`:285`/`:866`/`:870`, 슬롯 해제 `free_blocks` 반환 `:1025`,
  `trace.md:181` ✓
- prefill 산포 `:251-288`(토큰 수 제한 `:253-257`, `block_idx=token_idx/
  BLOCK_SIZE` `:264`, pop·기록 `:266-272`, `assert(false)` `:273-277`, 소스
  주석 "block must be -1 during prefill" `:275`, D2D 복사 `:280-287`) ✓
- decode 산포 `:851-873`(`seq_len=current_prompt_len[active_slot]` `:855`,
  논리 블록/토큰 위치 `:856-857`, 첫 토큰일 때만 pop `:859-865`, 복사량
  `KV_DIM*bf16` `:866-872`) ✓
- `block_table_gpu` 재동기화 `:876`, `pagedAttention` 호출 `:878` ✓
- attention 파이프라인: 스코어 GEMM `:301-327`, `causalMask` `:329`
  (`kernels.cu:239-255`, 커널 `:224-237`), `softmax` `:331`
  (`kernels.cu:293-309`, 커널 `:257-290`), scores×V `:343-371` ✓
- GQA: 헤드 상수 `src/main.cpp:20-24`, `k_head_idx=i/GQA_Q_TO_K_RATIO` `:303`,
  소스 주석 `attn_score_head=Q_head*K_head^T/sqrt(64)` `:298`,
  `attn_alpha=1.0f/8.0f` `:649`, `k_proj_temp_buf+k_head_idx*HEAD_DIM` `:305`,
  `v_head_idx` `:346`, `v_proj_temp_buf+v_head_idx*HEAD_DIM` `:349`, 출력
  `(num_tok, 2048)` 주석 `:342`, paged attention `kv_head_idx=q_head_id/
  GQA_Q_TO_K_RATIO` `kernels.cu:468`, 블록 테이블 순회 K·V 읽기
  `kernels.cu:480-485` ✓
- 근거 문서 `evidence/claims.md:51,54,57,61,64,65,67,71,77,78`,
  `evidence/trace.md:181,235-239`, `evidence/recon.md:4,12`, `guide.md:11-16`,
  `series.yaml:16-18` ✓

**번역 표현 품질** — "2GiB"→"2 GiB", "16씩"→"in 16s", "K 판/ V 판"→"K plane/V
plane" 등 단위·다이어그램 표기가 자연스럽게 옮겨졌다. 서문의 "짧게만 확인한
것은"→"briefly verified then", "이 장의 몫이다"→"are this chapter's share"
등 문체상의 관용 표현이 원문의 호흡을 살렸다. 수식(`BLOCK_SIZE*KV_DIM*bf16`,
`kv_cache + block*BLOCK_BYTES (+ V_OFFSET) + token_in_block_idx*KV_DIM*bf16`,
`Q_head * K_head^T / sqrt(64)`)과 다이어그램 내부 표기도 원문과 동일하다.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다. 모든 인용이 원본과 1:1로
일치하고 고정 커밋 소스와도 실제로 부합한다.