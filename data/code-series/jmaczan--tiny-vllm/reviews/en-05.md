# en-05 review — The decode path that produces a single token

Reviewed `articles/en-05.md` against the Korean source `articles/ko-05.md`, and
spot-checked every source range against the pinned tree at revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf`
(`src/main.cpp`, `src/kernels.cu`, `src/kernels.cuh`, `python/decode_test.py`).

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the decode-family kernel taxonomy, the
prefill/decode contrast table, the RoPE and K/V descriptions, the `pagedAttention`
substitution, the unused-`softmaxDecode` observation, the scope delegation, and
the limitations all match the Korean exactly. The `<!-- visual: ... -->` marker
is retained verbatim. No translation-introduced factual error was found, and no
fix is required.

## Fidelity

- **Structure and completeness.** All 13 sections of the Korean are present and
  in order: the question, where decode runs, the decode-family kernels and
  contrast, embedding, RoPE, K/V gathering and scattering, `pagedAttention`,
  the remaining layers and logits, non-coverage, limitations, and sources.
  Headings translate the Korean directly (`decode 는 prefill 다음에 어디서 도는가`
  → "Where decode runs after prefill"; `K/V 를 배치 버퍼에 모아 블록에 흩뿌린다`
  → "Gathering K/V into batch buffers and scattering them into blocks"). No
  paragraph is omitted or added.
- **Citations preserved exactly.** A mechanical extraction of every
  `src/*.cpp:start-end` token shows the two files are identical, with matching
  multiplicities: `kernels.cu` `26-344`, `32-40`, `45`, `55-81`, `96-152`,
  `146-151`, `173-200`, `173-212`, `224-309`, `262`, `311-344`, `331-338`,
  `346-523`, `347-349`, `347-356`, `347-369`, `349`, `350`, `358-369`, `361`,
  `371-384`, `371-405`, `375`, `376-377`, `376-378`, `378`, `379-382`,
  `388-405`, `397`, `408-439`, `408-458`, `413`, `442-458`, `461`, `461-523`,
  `461-527`, `478-485`, `486-519`, `509-519`, `522`, `525-527`; `main.cpp`
  `12-38`, `28`, `29`, `32`, `33`, `34`, `150-158`, `150-288`, `150-553`,
  `182`, `251-288`, `329`, `329-331`, `331`, `550-552`, `551`, `572`, `684-693`,
  `720-757`, `720-1039`, `722-723`, `724-737`, `726-734`, `749-757`, `759`,
  `759-849`, `769`, `802-821`, `824-842`, `844-849`, `847-848`, `851-873`,
  `851-878`, `855-857`, `856-865`, `858-865`, `859-865`, `866-872`, `876`,
  `878`, `880-971`, `892`, `902`, `904`, `907-969`, `971`, `974`, `974-1037`,
  `976-994`, `996`, `1000-1014`, `1015-1031`, `1032-1037`; `kernels.cuh`
  `11-28`, `23-28`, `25`. Both inline citation links
  (`kernels.cu:376-378` → `...L376-L378`, `main.cpp:856-865` → `...L856-L865`)
  are reproduced unchanged.
- **Citations verified.** Spot checks against the pinned tree confirm the
  anchors: `prefill`'s signature and `prompt_len` handling
  (`src/main.cpp:150-158`), the scatter block and `block_table` H2D
  (`src/main.cpp:855-876`), the decode embedding kernel (`src/kernels.cu:347-356`),
  the wrapper declarations (`src/kernels.cuh:23-28`), the quoted RoPE lines
  (`src/kernels.cu:376-378`), `V_OFFSET`/`BLOCK_BYTES` (`src/main.cpp:33-34`),
  the `embeddingGatherDecode` call (`src/main.cpp:759`), the TODO
  (`src/main.cpp:551`), and the constants `MAX_SEQ_LEN`/`BATCH_SIZE`/
  `BLOCK_SIZE` (`src/main.cpp:28-32`). All match.
- **Code blocks and visual marker.** The RoPE snippet (`src/kernels.cu:376-378`)
  and the block-allocation snippet (`src/main.cpp:856-865`) are reproduced
  character-for-character with the Korean, and the marker
  `<!-- visual: prefill-decode-contrast supports: [prefill-decode-contrast] -->`
  is identical.
- **Technical content.** The "one block per token" → slot-axis swap in
  `embeddingGatherKernelDecode`, the single-block/single-position RoPE with the
  hardcoded `500000.0` and `32` and the precompute TODO, the `n =
  num_active_slots` cuBLAS calls into `k_proj_batched_buffer`/
  `v_proj_batched_buffer`, the one-token K/V copy with allocation only at
  `token_in_block_idx == 0`, the `V_OFFSET` placement, the `pagedAttention`
  signature and its online softmax, the unused `softmaxDecode`, and the
  final argmax/termination branch are all translated without drift.
- **Scope, limitations, performance framing.** The non-coverage delegation list
  (parts 3, 4, 6, 7, 2), the GPU/CUDA-toolchain-absent caveat, the
  `code 4 / tests 1 / runtime 0` minimum evidence, the treatment of
  `python/decode_test.py` as scope-only with no claim, and the gated-model
  follow-up are all carried over exactly. No performance figure is present, so
  nothing can be read as a measured value.
- **Optional source-precision observations.** The four non-blocking precision
  notes raised in `reviews/ko-05.md` (O1 score computation at `478-485` vs
  `486`, O2 wrapper-vs-kernel declaration attribution, O3 the TODO's prefill
  placement, O4 the `current_prompt_len[slot]` index notation) concern the
  Korean source's own citations, not the translation. The English renders each
  of them faithfully and identically to the Korean, so none is a
  translation-introduced error.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered, and no fix is required.
