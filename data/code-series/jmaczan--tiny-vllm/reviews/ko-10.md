# Review: ko-10.md (paged KV cache와 paged attention)

## Decision

PASS

## Verified claims

고정 커밋 `1896ff5c37a241050dbcd9527caf9dc1d3087a61`의 `src/main.cpp`, `src/kernels.cu`,
`src/cuda_to_hip.h`와 `evidence/claims.md`, `evidence/trace.md`, `evidence/recon.md`,
`briefs.md`, `guide.md`, `series.yaml`을 대조해 모든 기술 주장과 인용을 확인했다.
인용된 줄 범위가 소스와 전부 일치한다.

- **범위·소유 주장** — `series.yaml:31-33`이 10장 scope를 `src/main.cpp`,
  `src/kernels.cu`로 둔 것을 확인. briefs.md 10장(`:213-233`)에서 Claim 8이 주 소유,
  Claim 5·6·7·10이 재사용 주장임을 확인. "배칭 정책은 7·8장"(`briefs.md:226`),
  "vLLM 등 외부 성능 비교는 범위 밖"(`briefs.md:229`) 경계가 그대로 지켜졌다.
- **KV 캐시 상수** — `KV_CACHE_SIZE_BYTES=2GiB`(`main.cpp:35`),
  `V_OFFSET=16*512*2`(`:33`), `BLOCK_BYTES=32768`(`:34`),
  `NUM_BLOCKS=2GiB/32768=65536`(`:37`), `MAX_BLOCKS_PER_SEQ=128`(`:36`) 전부 일치.
  `kernels.cu:16-19` 재정의와 중복 TODO(`kernels.cu:6`)도 일치.
- **블록 풀 초기화** — `kv_cache` 2GiB 할당(`:576`), `free_blocks` 0..65535 채움
  (`:577-578`), `block_table` 전부 -1(`:579`), `block_table_gpu` 할당(`:580-581`)
  일치. K=`block*BLOCK_BYTES`, V=`+V_OFFSET` 배치(`trace.md:105-106`, `:152`) 일치.
- **prefill 산포** — `main.cpp:247-288`에서 논리 블록 계산(`:251-264`), -1이면 pop 후
  기록(`:264-272`), 아니면 `assert(false)`(`:273-277`), 최대 BLOCK_SIZE 토큰
  청크(마지막은 일부) D2D 복사(`:280-287`) 전부 소스와 일치.
- **decode 산포** — `main.cpp:851-873`에서 논리 블록=`seq_len/16`,
  블록 내 위치=`seq_len%16`(`:856-857`), 첫 토큰일 때만 pop(`:859-865`),
  K/V 토큰 오프셋 D2D 복사(`:866-872`) 일치. "두 산포 모두 커널이 아니라 D2D
  `cudaMemcpy`"(`claims.md:67`) 일치.
- **GQA** — `NUM_Q_HEADS=32`, `NUM_K_HEADS=NUM_V_HEADS=8`(`main.cpp:20-23`),
  `kv_head_idx = q_head_id / 4`(`kernels.cu:468`, `main.cpp:23`) 일치. "block_table
  매핑은 head와 무관하게 slot/layer/logical block 기준"이라는 해석이 커널의
  `block_table_gpu` 인덱싱(`kernels.cu:480`)과 부합. 주 소유 5장(`briefs.md:115`)
  위임도 일치.
- **그리드·블록 구성** — `pagedAttention` 래퍼(`kernels.cu:525-527`)가
  `<<<dim3(num_active_slots, NUM_Q_HEADS), HEAD_DIM>>>`로 띄우고, 호출 지점이 decode
  레이어 루프 `main.cpp:878`임을 확인. 64 스레드 = 2 warp 주장 타당.
- **커널 내 인덱싱** — `blockIdx.x`가 활성 슬롯 내 위치(`:464`), 실제 슬롯은
  `gpu_active_slots` 매핑(`:465`), `kv_head_idx`(`:468`), `num_blocks`(`:471`),
  물리 블록 조회(`:480`), `tokens_in_block`(`:481-482`) 전부 일치. "중간 매핑이 없으면
  활성 슬롯이 아닌 슬롯의 block_table을 읽게 된다"는 해석이 trace.md:235-239 및 소스와
  부합. decode 배치(`main.cpp:722-737`, `claims.md:39`) 일치.
- **causal masking** — `num_blocks=ceil(seq_len/16)`(`:471`)와 `tokens_in_block=min`
  (`:481-482`)만 순회하므로 미래 토큰을 읽지 않는 구조, "누적 K만 읽음 = 마스킹"
  (`claims.md:88`) 일치. `seq_len=34` 예시(논리0·1 전부, 논리2는 2토큰)의 계산도 정확.
- **block_table 재동기화** — decode 레이어 루프의 산포(`:851-873`) → 전체 H2D
  재복사(`:876`) → `pagedAttention`(`:878`) 순서, 크기 2×16×128×4바이트(`claims.md:107`),
  prefill 종료(`:552`)·슬롯 해제(`:1030`) 동기화, 해제 시 -1 복원과 `free_blocks` 반환
  (`claims.md:104`) 전부 일치. TODO 인용("do it more clever...", `main.cpp:551`)도 일치.
- **한계 절** — `dot_products[2]`의 `HEAD_DIM=64` 의존(shfl `:489-493`, thread 0/32
  분기 `:494-501`, 배열 `:463`), `WARP_FULL_MASK`가 CUDA/HIP 경로에서 갈린다는 서술
  (`cuda_to_hip.h:50`, `:59`) 정확. 산포가 D2D `cudaMemcpy`라는 한계(`claims.md:67`),
  상수 중복(`kernels.cu:6`, `:8-19`; `claims.md:57`), 전체 테이블 재복사
  (`claims.md:110`) 일치.
- **검증 경로·범위 규율** — 커널 테스트가 `tests/test_softmax.cu` 하나뿐
  (`recon.md:12`), 저장소 미실행(`recon.md:4`), 산포·paged attention 정확성 미검증
  (`recon.md:38`) 서술이 근거와 일치. 성능 수치·실행 시간 주장이 없고 "성능 주장 없음"을
  명시하며, README 주장 비인용(`guide.md:11-16`, `briefs.md:142`) 규칙을 지켰다.
  "배치가 작아도 테이블 크기가 고정이라 일어나는 비용"은 측정값 없는 구조적 서술로
  성능 주장이 아니다.

## Problems

기술적 오류나 미지원 주장은 발견되지 않았다. 아래는 수정을 요구하지 않는 관찰이다.

1. **`briefs.md:115` 인용 범위** — "prefill의 attention 스코어 GEMM, GQA의 prefill
   측 매핑, 별도 softmax 커널 — 4·5장(`briefs.md:115`)"에서 `briefs.md:115`는 ch5의
   Claim 7(GQA 매핑)만 직접 뒷받침한다. attention GEMM·softmax의 4·5장 위임은
   `briefs.md:97`(ch4 exclusion)이 더 직접적이다. 묶음 인용이지만 실질 오류는 없다.
2. **"참조·소유 주장" 표기** — Claim 5·6·7·10을 "참조·소유 주장"이라 표현했으나,
   이들은 5·8장이 소유하고 10장에서 참조로 재사용되는 주장이다. briefs.md 10장이
   소유 표기를 하지 않으므로 혼동 소지가 있지만, 주 소유인 Claim 8과의 대비 목적상
   유효한 서술이다.
3. **`gpu_seq_lens` 미언급** — "누적 K 토큰만 읽음" 논증에서 `seq_len`이
   `current_prompt_len+1`로 잡힌다는 근거(`main.cpp:755`, `trace.md:139-140`)를
   언급하지 않는다. 인용 오류는 아니며, Q가 자기 K까지 내적한다는 사실은 예시 계산과
   부합한다.

## Required fixes

없음. 인용된 줄 범위가 소스와 전부 일치하고, Claim 5·6·7·8·10의 근거가
`claims.md:49-57`, `:59-67`, `:69-78`, `:80-88`, `:102-110` 및 briefs.md 10장과
정렬되어 있으며, 배칭 정책·vLLM 비교·성능 수치 같은 범위 밖 주장이 없다. 즉시
게재 가능하다.