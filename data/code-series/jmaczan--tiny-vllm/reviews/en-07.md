# Review · en-07.md vs ko-07.md

## Decision

PASS

## Fidelity

The English translation is a faithful, paragraph-level rendering of the Korean
source. Every section, table, and diagram is preserved with equivalent content
and ordering:

- **Skeleton**: `BATCH_SIZE=2`(`:29`), `MAX_SEQ_LEN=2048`(`:12-38`;
  `trace.md:27`), `is_slot_free`(`:597`), three slot state fields(`:599-601`),
  the 4-prompt hardcoded queue 17/14/13/14(`:584-594`; `trace.md:69`) — all match.
- **Initial prefill**: `for slot in 0..BATCH_SIZE` + `!queue.empty()` loop
  (`:695-708`), occupancy at `:152-155`/`:155`, `current_prompt_len` set at
  `:548`, prompts 3·4 remaining, `trace.md:84-85` — all match.
- **Decode**: loop `:720-1039`, active slot reconstruction `:722-737`,
  `embeddingGatherDecode` block count = active slots(`:759`;
  `kernels.cu:358-369`), Q/K/V GEMM `n=num_active_slots`(`:763-842`),
  `num_active_slots ∈ {0,1,2}` bound, break/continue at `:739-744`/`:745` — all
  match.
- **Termination/release**: `:1015-1037`, EOT IDs 128001/128009,
  `MAX_SEQ_LEN-1`=2047, `trace.md:169-171`, +1 update `:1032-1037`, the 4-row
  release table (`:1017`, `:1025`, `:1026`, `:1030`), `free_blocks`
  (`:577-578`) — all match.
- **Lifecycle table and both diagrams**: state rows, timeline arrow labels, and
  citations (`:584-594`, `:695-708`, `:720-1039`, `:1015-1031`, `:739-746`,
  `:29`, `:722-737`, `:759`, `:763-842`) — all match.
- **Verification / not covered / limitations / summary**: `test_softmax.cu`
  (`recon.md:12`), `recon.md:4`, `briefs.md:177`, Ch8/Ch9-10 hand-offs,
  `guide.md:7-9`, Claim references, `MAX_NEW_TOKENS_GENERATED`(`:12`),
  `:739-746`, `claims.md:100` — all match.

Source citations were verified against the evidence files
(`evidence/claims.md:90-100` for Claim 9, `evidence/trace.md:27/69/84-85/169-171`,
`guide.md:11-16` for the evidence-rules section) and are correct. No invented
facts were introduced.

Minor stylistic departures ("tackles the stage ... head-on" for "정면으로
다룬다", "fixed pair of slots" for "고정된 슬롯 두 칸") are idiomatic English
and do not alter meaning. Terminology is consistently rendered
(prefill/decode/slot/block table/free_blocks), and code identifiers, token IDs,
and commit-line citations are preserved verbatim.

## Required fixes

None.