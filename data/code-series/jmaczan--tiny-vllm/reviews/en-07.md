# Review: en-07.md (English translation of ko-07.md)

## Decision

PASS

## Fidelity

The English translation is line-for-line faithful to the Korean source (both 259 lines). Technical terminology, code blocks, bold emphasis, mermaid diagram, and citation formatting are preserved exactly. Translation choices are accurate: "연속 배열" → "contiguous arrays", "수집소" → "collection point", "배타적으로 가진다" → "holds it exclusively", "상시 운영 서버" → "always-on server", "후속 과제로 남는다" → "remains a follow-up task". Quotes from the README and code comments are rendered in English with the meaning intact. No content was added, dropped, or reordered.

## Required fixes

None.

All source citations were checked against the repository at commit `e25bf1994efa90bc98b721ba7c527402f86fbeaf` (local checkout in `.cache/code-series/repos/jmaczan--tiny-vllm`) and are accurate in both versions:

- `src/main.cpp:12` — `MAX_NEW_TOKENS_GENERATED = 20` declared, and grep confirms it is never referenced anywhere else (claim at en-07:248 correct).
- `src/main.cpp:26,27` — EOT/EOS token IDs 128001/128009.
- `src/main.cpp:29` — `BATCH_SIZE = 2` with the quoted TODO comment.
- `src/main.cpp:38` — `MAX_SEQUENCES = BATCH_SIZE`.
- `src/main.cpp:152-155` — prefill entry `queue.front()` / `prompt.size()` / `queue.pop()` / `is_slot_free[slot] = false`.
- `src/main.cpp:548` — `current_prompt_len[slot] = prompt_len`.
- `src/main.cpp:583-594` — four `queue.push` blocks with matching length comments (17/14/13/14).
- `src/main.cpp:597, 599-605` — `is_slot_free`, `generated_tokens`, `last_generated_tokens`, `current_prompt_len`, `active_slots`, `active_tokens`.
- `src/main.cpp:695-708` — initial fill loop with condition `slot < is_slot_free.size() && !queue.empty()`.
- `src/main.cpp:720-746` — decode `while (true)` loop; `active_slots.push_back` at 735 is outside the `if` block; `break`/TODO at 743, `continue` at 745.
- `src/main.cpp:748-757` — three H2D copies; `seq_lens = current_prompt_len + 1`.
- `src/main.cpp:759` — `embeddingGatherDecode` consumer of `gpu_last_tokens`.
- `src/main.cpp:876` — block table H2D copy inside the per-layer decode loop (4096 entries), matching the "per layer" claim.
- `src/main.cpp:878` — `pagedAttention` passes exactly `kv_cache, block_table_gpu, gpu_seq_lens, gpu_active_slots`.
- `src/main.cpp:1015-1037` — termination conditions, `is_slot_free[active_slot] = true` (1017), block return walk, H2D sync (1030), else branch updates (1032-1037).
- `src/main.cpp:1040` — "Ok bye!".
- `python/batching_test_tokens.py:1-15` — script lines match the quoted block and the length comments.

No factual errors were introduced; the four limitations listed in the "Limitations" section (unreferenced `MAX_NEW_TOKENS_GENERATED`, finite execution vs. server comment, `+1` interpretation, unmeasured copy cost) are all accurately stated and consistent with the source.