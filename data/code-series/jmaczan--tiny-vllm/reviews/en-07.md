# en-07 review — Running Multiple Requests Together with a Slot Table

Reviewed `articles/en-07.md` against the Korean source `articles/ko-07.md`. The
review is a translation-fidelity check: it verifies that the English is a
complete, unaltered rendering of the Korean, and that every source citation in
the English matches the Korean and resolves to the claimed range in the pinned
tree at revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf`. The Korean source's
own factual accuracy is covered by the companion `ko-07.md` review and is not
re-adjudicated here.

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the slot/queue stores, initial prefill fill, decode
loop rebuild, active-array H2D copies, the `+1` sequence-length interpretation,
the slot lifecycle diagram, termination and block return, the unreferenced
`MAX_NEW_TOKENS_GENERATED` observation, the non-coverage delegation list, and the
limitations all match the Korean. The Mermaid diagram with its
`<!-- visual: ... -->` marker is retained in structure and content. All inline
citation links are reproduced unchanged, and each cited range was checked against
the pinned tree. No translation-introduced factual error or dropped citation was
found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 13 sections of the Korean are present and
  in order: question, the two stores, initial fill, decode loop rebuild, active
  arrays to GPU, slot lifecycle, termination and block return, non-coverage,
  limitations, and sources. Headings translate the Korean directly
  (`이 장의 질문` → "The Question of This Chapter"; `두 저장소: 슬롯과 큐` → "Two
  Stores: Slots and Queues"; `초기 채움: 슬롯 0·1 이 프롬프트를 가진다` → "Initial
  Fill: Slots 0 and 1 Take Prompts"; `decode 루프가 슬롯을 재구성한다` → "The
  Decode Loop Rebuilds the Slots"; `활성 배열을 GPU 로 옮긴다` → "Moving the
  Active Arrays to the GPU"; `슬롯 수명주기` → "Slot Lifecycle"; `종료와 블록
  반납` → "Termination and Block Return"; `이 장이 다루지 않는 것` → "What This
  Chapter Does Not Cover"; `이 장의 한계` → "Limitations of This Chapter";
  `출처` → "Sources"). No paragraph is omitted or added.
- **Citations preserved and verified.** Every source range in the Korean appears
  identically in the English: `src/main.cpp:584`, `583-594`, `29`, `38`, `579`,
  `597`, `599-601`, `603-605`, `607-608`, `609-610`, `684-685`, `695-708`,
  `697-700`, `152-155`, `720`, `722-723`, `732`, `548`, `728-731`, `735`, `736`,
  `738-746`, `743`, `748-757`, `749`, `750`, `751-757`, `759`, `878`,
  `1015-1037`, `1017`, `1018-1029`, `1030`, `1032-1037`, `12`, `26`, `27`,
  `1015`, `876`, `551`, `546-548`, `579-581`, `1040-1043`, and
  `python/batching_test_tokens.py:1-21`. Spot-checking the pinned tree confirms
  the quoted ranges: line 12 declares `MAX_NEW_TOKENS_GENERATED`; 26-29 hold the
  EOS/EOT IDs, `MAX_SEQ_LEN`, and `BATCH_SIZE` with its "not even close to being
  good" comment; 152-155 are the `prefill` entry (`queue.front()`, `prompt.size()`,
  `queue.pop()`, `is_slot_free[slot] = false`); 546-548 record the slot state;
  579-581 allocate the block table and device copy; 583-594 push the four
  prompts; 597 initializes `is_slot_free`; 599-605 declare the per-slot state and
  active arrays; 607-610 and 684-685 allocate the device arrays; 695-708 and
  720-746 are the initial prefill loop and the decode rebuild/branch; 749-757 are
  the H2D copies and `seq_lens` computation; 759 and 878 are the
  `embeddingGatherDecode` and `pagedAttention` calls; 1015-1037 are the
  termination decision and block return; 1040-1043 are the "Ok bye!" output and
  cleanup.
- **Inline citation links.** The four markdown links
  `src/main.cpp:695-708` (`...L695-L708`), `src/main.cpp:722-737`
  (`...L722-L737`), `src/main.cpp:743` (`...L743`), and `src/main.cpp:1015-1037`
  (`...L1015-L1037`) are reproduced unchanged.
- **Technical content.** `BATCH_SIZE = 2`, the `MAX_SEQUENCES = BATCH_SIZE`
  alias, the `2 × 16 × 128 = 4096` block-table size, the
  `N_LAYERS × MAX_BLOCKS_PER_SEQ = 16 × 128 = 2048` per-slot bundle, the prompt
  lengths 17/14/13/14, the `<|end_of_text|>` 128001 and `<|eot_id|>` 128009 IDs,
  the `MAX_SEQ_LEN - 1` total-sequence-length cap, the three-dimensional block
  index arithmetic, and `MAX_NEW_TOKENS_GENERATED = 20` are all translated without
  drift.
- **Argument separation.** The distinction that `gpu_last_tokens` feeds
  `embeddingGatherDecode` while `pagedAttentionKernel` receives only `kv_cache`,
  `block_table_gpu`, `gpu_seq_lens`, and `gpu_active_slots` is carried over
  exactly, including the note that the two device arrays are not mixed.
- **Mermaid diagram.** The `slot-lifecycle` diagram retains all nodes A–Z with
  the same `main.cpp` anchors (`597`, `695-708`, `152-155`, `720`, `722-723`,
  `726-734`, `735-736`, `738-746`, `749-757`, `759`, `878`, `1015-1031`,
  `1032-1037`, `1040`) and the same edge labels translated without drift
  (`빈 슬롯 + 큐에 남음` → "empty slot + prompts remain in queue"; `점유 슬롯` →
  "occupied slot"; `0 + 큐 빔` → "0 + queue empty"; `아니오` → "no"; `예` → "yes";
  `블록 반납 push_back` → "return blocks push_back"). The
  `<!-- visual: slot-lifecycle supports: [slot-lifecycle, decode-termination] -->`
  marker is carried over exactly.
- **Scope, limitations, framing.** The minimum-evidence line (4 code citations, 0
  tests, 0 executions), the no-GPU/CUDA-toolchain caveat, the ownership
  delegation list (episodes 6, 3, 4, 5, 2), the unverified index arithmetic, the
  `MAX_SEQ_LEN - 1` early-end note, the unreferenced `MAX_NEW_TOKENS_GENERATED`,
  the finite-execution `break` versus the "foreveeer" comment, the `+1`
  interpretation as an inference beyond the cited range, the per-layer 4096-entry
  H2D copy with unresolved TODO `551`, and the gated
  `meta-llama/Llama-3.2-1B-Instruct` follow-up are all carried over exactly. No
  performance figure is present, so nothing can be read as a measured value.
- **Minor phrasing observation (non-blocking).** "If the number of slots is 0"
  renders `슬롯 수가 0 이면`, which in context refers to `num_active_slots`; the
  English preserves the Korean's wording and the diagram's `num_active_slots == 0`
  node disambiguates it. Neither alters a citation, number, or technical claim.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered, and no translation-introduced error was found. Every cited range
resolves to the claimed content in the pinned tree.
