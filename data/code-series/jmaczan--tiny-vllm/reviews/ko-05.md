# 리뷰: ko-05 — tiny-vllm 5장 · attention과 KV cache

## Decision

**PASS**

5장 초안은 KV 캐시 구조(Claim 5), prefill/decode 산포 경로(Claim 6), GQA 헤드
재사용 규칙(Claim 7)을 세 축으로 잡고, 모든 기술적 주장을 증거 파일의 소스
경로·줄 범위·claim 번호로 정확히 인용한다. 상수 산술, 실행 순서, 전방 참조,
성능 주장 회피가 모두 근거와 일치한다. 이하 문제는 모두 사소한 인용 정밀도
수준이며 차단성(blocking)이 아니다.

## Verified claims

- **캐시 상수 산술**: `BLOCK_SIZE=16`, `KV_DIM=512`, `V_OFFSET=16*512*2=16384`,
  `BLOCK_BYTES=V_OFFSET*2=32768`, `MAX_BLOCKS_PER_SEQ=2048/16=128`,
  `NUM_BLOCKS=2GiB/32768=65536` — 전부 올바르며 `claims.md:54`의
  `src/main.cpp:32-37` 범위와 일치.
- **할당/소유 구조**(`src/main.cpp:574-581`): `kv_cache` 2GiB `cudaMalloc`,
  `free_blocks` 0..65535 `iota`, `block_table` `-1` 초기화, `block_table_gpu`
  동일 크기 — `claims.md:55`와 일치. Claim 5 귀속(`claims.md:51`)과 2GiB 예약
  한계(`claims.md:57`) 정확.
- **block_table 인덱싱**: `slot*N_LAYERS*MAX_BLOCKS_PER_SEQ + layer*
  MAX_BLOCKS_PER_SEQ + block_idx`(`:265,:271,:858,:864`), 주소
  `kv_cache + block*BLOCK_BYTES`(`:280,:285,:866,:870`), 슬롯 해제 시
  `free_blocks` 반환(`:1025`, `trace.md:181`) — 전부 정확.
- **prefill 산포**(`src/main.cpp:251-288`): 토큰 수를 `BLOCK_SIZE`로 제한
  (`:253-257`), 논리 블록 `token_idx/BLOCK_SIZE`(`:264`), `-1`이면 pop
  (`:266-272`), 기존 할당 시 `assert(false)`("block must be -1 during prefill",
  `:275`), K/V D2D 복사(`:280-287`) — `claims.md:64`와 일치. Claim 6 절반
  귀속 정확.
- **decode 산포**(`src/main.cpp:851-873`): `seq_len=current_prompt_len[slot]`
  (`:855`), 논리 블록/토큰 위치(`:856-857`), 첫 토큰일 때만 pop(`:859-865`),
  K/V 복사량 `KV_DIM*bf16`(`:866-872`) — `claims.md:65`와 일치. Claim 6
  decode 절반 귀속 정확.
- **GQA 규칙**(`src/main.cpp:20-24`): Q 32, K/V 8, `GQA_Q_TO_K_RATIO=4`,
  `GQA_ATTN_SCORES_TO_V_RATIO=4`. 스코어 `k_head_idx=i/4`(`:303`),
  scores×V `v_head_idx=i/4`(`:346`), paged attention
  `kv_head_idx=q_head_id/4`(`kernels.cu:468`) — `claims.md:74-77`와 일치.
  Claim 7 귀속 정확.
- **attention 파이프라인 순서**: 스코어 GEMM(`:301-327`) → causalMask(`:329`;
  `kernels.cu:239-255`, 커널 `:224-237`) → softmax(`:331`;
  `kernels.cu:293-309`, 커널 `:257-290`) → scores×V(`:343-371`) —
  `trace.md:107-115`, ko-04.md와 정확히 일치.
- **prefill 스코어 GEMM은 캐시가 아닌 임시 버퍼를 씀**(`k_proj_temp_buf +
  k_head_idx*HEAD_DIM`, `:305`) — `trace.md:107-109` 관찰과 일치, 해석이
  "decode가 캐시에서 읽기 위해 미리 저장"이라고 적절히 한정.
- **스케일 상수**: `attn_alpha=1/8`(`:649`)이 `sqrt(64)=8`에 대응한다는 주석
  관찰(`:298`) — ko-04.md와 동일한 방식으로 취급하며 수치 검증으로
  주장하지 않음.
- **검증 경로**: 유일한 커널 테스트 `tests/test_softmax.cu`
  (`recon.md:12`), 산포가 D2D `cudaMemcpy`라 커널 테스트 범위 밖
  (`claims.md:67`), 저장소 미실행(`recon.md:4`) — 정확하고 성능/수치
  정확성을 주장하지 않는 스코프 유지.
- **인덱싱 혼재 한계**(`trace.md:235-239`)와 캐시 상수 중복 정의 TODO
  (`kernels.cu:6`, `claims.md:57`) — 정확.
- **전방 참조 일관성**: paged attention 커널 10장, 배칭·슬롯 7·8장,
  `block_table_gpu` 재동기화 8장(Claim 10), series.yaml 5장 대상 파일
  (`series.yaml:16-18`) — ko-01/ko-04 및 series.yaml과 일치.
- **성능 주장 회피**: "블록 단위/토큰 단위"가 구조적 작업량 차이일 뿐 실행
  시간·처리량을 근거 없이 쓰지 않는다는 한계 명시 — guide.md 스코프 원칙 준수.

## Problems

1. **사소한 인용 범위 오차(비차단)**: paged attention 커널의 블록 순회를
   `:480-485`로 인용했으나, `trace.md:158`은 순회를 `:478-481`, K·V 읽기를
   `:484-485`로 구분한다. 초안의 범위(480-485)는 커널 본문 `:461-523` 안에
   들어 K/V 읽기 지점(`:484-485`)은 포함하므로 의미는 같고, 시작줄이 2줄
   짧게 잡혀 있을 뿐이다. 사실 오류는 아님.
2. **줄 단위 세부 인용의 원본 미확인(비차단, 관찰)**: `:33`(V_OFFSET),
   `:34`(BLOCK_BYTES), `:37`(NUM_BLOCKS), `:305`(k_proj_temp_buf),
   `:349`(v_proj_temp_buf), `:342`(출력 주석) 같은 세부 줄 인용은
   `claims.md`의 범위(`:32-37`, `:301-327`, `:343-371`) 안에 들어 있어
   기존 시리즈의 세부 인용 관례와 일치하지만, 원본 `src/`가 이 저장소에 없어
   그 정확한 줄 번호를 재검증하지 못했다. 초안이 근거 범위를 벗어나지
   않으므로 문제가 되지 않는다.

## Required fixes

- 차단 항목 없음.

권장(비차단) 사항:
- 인용 정밀도 일치를 위해 `:480-485`를 `:478-481`(순회)과 `:484-485`(K·V
  읽기)로 나누거나 범위를 `:478-485`로 확장해 `trace.md:158`과 맞춘다.