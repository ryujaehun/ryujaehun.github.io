# Review: en-06.md vs ko-06.md

## Decision

PASS

## Fidelity

I compared the English translation against the Korean source paragraph by paragraph and verified every source citation against the repository at commit `e25bf19`.

**Citation verification** — all cited locations are accurate:
- `src/main.cpp:32-37` (block size constants), `575-581` (kv_cache / free_blocks / block_table), `265` (prefill block lookup), `266-272` (prefill `-1`→`pop_back`), `858` (decode scatter), `859-865` (decode `token_in_block_idx == 0`→`pop_back`), `866-872` (K/V write with `V_OFFSET`), `551-552` (TODO + prefill sync), `750`/`751-757` (gpu_active_slots / seq_lens), `763` (q_proj = buf_2048_1), `876` (per-layer sync), `878` (pagedAttention call), `892` (O-proj input), `1018-1029` (teardown loop), `1030` (post-teardown sync) — all match the source.
- `src/kernels.cu:6` (dup TODO), `16-19` (redefined constants), `347`/`371`/`408` (embeddingGatherKernelDecode / ropeKernelDecode / softmaxKernelDecode signatures), `461` (kernel signature), `464-469`, `470-485`, `481` (min), `484-485` (K/V addresses), `486-508`, `509-522`, `522` (in-place output), `527` (launch) — all match.
- `cuda_to_hip.h:50` (HIP 64-bit `WARP_FULL_MASK`) and `:59` (CUDA `0xffffffff`) confirm the WARP_FULL_MASK claim.

**Factual claims** — the arithmetic (V_OFFSET = 16384 B, BLOCK_BYTES = 32768 B, NUM_BLOCKS = 65536, block table = 4096 entries) is correct. The "thread 0 / thread 32 / dot_products[2]" warp-merge description, the `1/sqrt(64)` scale, the online-softmax correction factor, and the GQA ratio `q_head_id / 4` all match the code.

**Translation fidelity** — the translation is faithful throughout: no paragraphs dropped, no content added, no meaning altered. Section structure, code blocks, the mermaid diagram, the argument table, "Further reading", and "Limitations" all carry over completely. Terminology is consistent (e.g. `physical_block` vs `물리 블록`, "logical block" vs `논리 블록`, "slot" vs `슬롯`, "scatter" vs `분산`).

No fidelity issues found. Minor stylistic note only: the Korean "-1 은 '이 자리에…'는 표시다" ("this position") is rendered as "this slot", which is a reasonable reading given the block table's slot·layer·logical-block indexing — not an error.

## Required fixes

None.