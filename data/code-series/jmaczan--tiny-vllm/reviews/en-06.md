# en-06.md 리뷰

검토 기준: 한국어 원본 `articles/ko-06.md`와 영어 번역 `articles/en-06.md`를
대조해 충실도·누락·오역을 검사했고, 번역이 인용한 모든 소스 경로·줄 범위와
`evidence/claims.md`, `evidence/trace.md`, `evidence/recon.md`, `guide.md`,
`series.yaml` 참조가 한국어 원본과 1:1로 일치하는지 일일이 대조했다. 아울러
고정 커밋(`1896ff5c…`) 소스 `src/main.cpp`, `src/kernels.cu`를 직접 열어
번역이 인용한 줄 범위가 실제 코드 위치와 맞는지도 확인했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 절·표·다이어그램·불릿까지 빠짐없이 옮겼으며, 의미
왜곡·누락·오역이 발견되지 않았다. 번역이 인용한 모든 소스 경로·줄 범위와 근거
문서 참조가 한국어 원본과 정확히 일치하고, 원본이 세운 줄 범위 자체도 고정
커밋 소스와 실제로 일치한다.

## Fidelity

**구조·범위** — 11개 절(제목·서문·파일과 범위·prefill·decode·구조로 본 두
병목·검증 경로·다루지 않는 것·한계·정리)이 모두 존재하고 순서가 원본과
일치한다. 비교표 1개, ASCII 다이어그램 1개, 세 축 요약 불릿 3개, "다루지 않는
것" 불릿 6개, "한계" 불릿 5개, 정리 2문단이 원본과 1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`embeddingGather`, `embeddingGatherDecode`,
`pagedAttention`, `pagedAttentionKernel`, `causalMask`, `softmax`,
`ropeDecode`, `prefill_attn_scores`, `embed_proj_cpu`, `block_table_gpu`,
`EMBEDDING_LENGTH`, `KV_DIM`, `VOCAB_SIZE`, `prompt_len`, `num_active_slots`,
`MAX_PROMPT_LEN`, `NUM_Q_HEADS`, `BLOCK_SIZE`, `KV_CACHE_SIZE_BYTES`,
`MAX_BUFFER_SIZE`, `BATCH_SIZE`, `CUDA_R_16BF`, `cudaMemcpy`)가 원문 그대로
보존되었다. Claim 3/4/5/6/10, 장 번호(5·7·8·9·10장),
`guide.md`/`series.yaml`/`evidence/*.md` 인용 표기도 동일하다. "병목"→
"bottleneck", "산포"→"scatter", "병렬 폭"→"parallelism width", "누적 K"→
"cumulative K", "슬롯별"→"per-slot", "토큰 단위"→"token-wise", "블록 단위"→
"block-wise" 등 기술 용어 번역이 일관적이고 원문 뉘앙스를 유지한다.

**인용 대조 (원본·번역·실제 소스 3자 대조)** — 다음이 모두 번역에서 그대로
유지되었고, 실제 소스와도 맞는다.

- prefill 진입 `src/main.cpp:152-155`, 토큰 GPU 복사 `:157`,
  `embeddingGather` 호출 `:158`(`kernels.cu:42-53`, 커널 `:32-40`) ✓
- 레이어 루프 `:164-493`, Q 투영 GEMM `m=EMBEDDING_LENGTH, n=prompt_len,
  k=EMBEDDING_LENGTH` `:177-196`, K/V 투영 GEMM `m=KV_DIM` `:201-240` ✓
- `rope` `:244-245`, KV 산포 블록 단위 `:247-288` ✓
- attention 파이프라인: 스코어 GEMM `:301-327`, `causalMask` `:329`
  (`kernels.cu:239-255`, 가드 `:241-244`), `softmax` `:331`
  (`kernels.cu:293-309`, 가드 `:295-298`), scores×V `:343-371` ✓
- `prefill_attn_scores` 버퍼 `MAX_PROMPT_LEN² × NUM_Q_HEADS` `:648` ✓
- 최종 `rmsNorm` `:494`, 로짓 GEMM `m=VOCAB_SIZE, n=prompt_len` `:509-527`,
  D2H `:529`, CPU argmax `:533-543`, 상태 갱신 `:546-548`, `block_table`
  동기화 `:552` ✓
- decode 루프 `:720-1039`, 활성 슬롯 재구성 `:722-737`, 여유 슬롯 prefill
  `:726-737`, 종료 조건 `:738-746` ✓
- `embeddingGatherDecode` `:759`(`kernels.cu:358-369`, 커널 `:347-356`) ✓
- decode 레이어 루프 `:760-972`, Q/K/V GEMM `n=num_active_slots`
  `:763-783`, `:802-821`, `:824-842` ✓
- `ropeDecode` `:844-849`, KV 산포 토큰 단위 `:851-873`, `block_table_gpu`
  재동기화 `:876`, `pagedAttention` 호출 `:878` ✓
- `pagedAttentionKernel` `kernels.cu:461-523`(누적 K만 읽는 온라인 softmax
  포함), 최종 `rmsNorm` `:974`, 로짓 GEMM `:976-994`, D2H `:996`, 슬롯별
  CPU argmax(bf16→float 캐스팅) `:1000-1012`, 종료 판정 `:1015-1037` ✓
- 근거 문서 `guide.md:11-16`, `guide.md:7-9`, `series.yaml:19-21`,
  `evidence/trace.md:228-231`, `evidence/trace.md:240-242`,
  `evidence/recon.md:4`, `evidence/recon.md:12`, `evidence/claims.md:57`,
  `src/main.cpp:531-532`, `:686`(argmax TODO), `src/main.cpp:681-682`,
  `:32-37`, `:576-581` ✓

**번역 표현 품질** — "2GiB"→"2 GiB", "레이어 루프 16회"→"layer loop ×16",
"그 제곱으로 커진다"→"grows with the square of that", "관점을 바꿔"→
"changes that view", "이 장의 몫이다"→"is this chapter's share" 등 단위
표기·다이어그램·문체상의 관용 표현이 자연스럽게 옮겨졌다. 수식
(`m=EMBEDDING_LENGTH, n=prompt_len`, `MAX_PROMPT_LEN² × NUM_Q_HEADS`,
`prompt_len²`), 표·다이어그램 내부 표기(`(:158)`, `(:759)`, `n=prompt_len`,
`n=num_active_slots`)도 원문과 동일하다.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다. 모든 인용이 원본과 1:1로
일치하고 고정 커밋 소스와도 실제로 부합한다.