# en-06 review — A Paged KV Cache Indexed in Blocks

Reviewed `articles/en-06.md` against the Korean source `articles/ko-06.md`. The
review is a translation-fidelity check: it verifies that the English is a
complete, unaltered rendering of the Korean, and that every source citation in
the English matches the Korean. The Korean source's own factual accuracy against
the pinned tree at revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf` is
covered by the companion `ko-06.md` review and is not re-adjudicated here.

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the block layout, three-term block-table indexing,
kernel traversal and address arithmetic, the four-state input contrast, the
warp-shuffle reduction and online softmax, the in-place output claim, and the
allocation/return/H2D synchronization content all match the Korean. The Mermaid
diagram and the argument table with their `<!-- visual: ... -->` markers are
retained in structure and content. No translation-introduced factual error or
dropped citation was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 13 sections of the Korean are present and
  in order: question, the three allocator pieces, single-block layout, the two
  K/V scatter points, block-table-to-address conversion, the four pieces of
  state, warp reduction and online softmax, in-place output, allocation and
  return, non-coverage, limitations, and sources. Headings translate the Korean
  directly (`이 장의 질문` → "The Question of This Chapter"; `블록 하나의 레이아웃`
  → "The Layout of a Single Block"; `할당과 반납` → "Allocation and Return"). No
  paragraph is omitted or added.
- **Citations preserved.** Every source range in the Korean appears identically
  in the English, including the allocator (`574-581`, `35`, `576`, `577-578`,
  `579`, `580-581`), dimensions (`29`, `38`, `15`, `28`, `36`), block layout
  (`32`, `33`, `34`, `37`), duplicated kernel constants (`kernels.cu:16-19`,
  `6`), prefill scatter (`251-288`, `264`, `265`, `266-272`, `273-277`,
  `253-257`, `280-282`, `285-287`), decode scatter (`851-873`, `855-857`,
  `859-865`, `866-872`), the three-term index (`265`, `858`), the kernel body
  (`kernels.cu:461-523`, `461`, `464-467`, `527`, `11`, `470-485`, `470-471`,
  `480`, `481`, `484`, `485`, `468`, `12`, `486`, `489-493`, `494-501`,
  `503-506`, `510-519`, `515-519`, `473`, `522`), the state table
  (`kernels.cu:347`, `371`, `408`, `465`, `469`, `470`, `480`, `522`, `527`;
  `main.cpp:750`, `751-757`, `763`, `878`), GQA (`main.cpp:18-23`), allocation
  and return (`268-269`, `861-862`, `1025`, `1018-1029`, `1020-1027`), H2D sync
  (`552`, `876`, `1030`, `551`), the `buf_2048_1` aliasing (`763`, `878`,
  `892`), and the `kernels.cuh:28` declaration. The two inline citation links
  `src/kernels.cu:461-523` (`...L461-L523`) and `src/kernels.cu:470-485`
  (`...L470-L485`) are reproduced unchanged.
- **Mermaid diagram.** `block-table-indexing` retains all ten nodes (A–J) with
  the same labels, the same `kernels.cu` anchors (`470-471`, `478`, `480`,
  `481`, `484`, `485`, `486-508`, `509-519`, `522`), and the same Korean-derived
  node text translated without drift (`블록 내 토큰 수` → "tokens in block",
  `온라인 소프트맥스 가중 누적` → "Online softmax weighted accumulation",
  `output 기록 = acc / d` → "Write output = acc / d"). The
  `<!-- visual: block-table-indexing supports: [block-table-indexing, kv-block-layout] -->`
  marker is carried over exactly.
- **Argument table.** The `paged-attention-inputs` table preserves all eight
  rows (`kv_cache`, `block_table_gpu`, `gpu_seq_lens`, `gpu_active_slots`,
  `q_proj`, `output`, `layer`, `num_active_slots`) with the same evidence
  citations and the same `<!-- visual: paged-attention-inputs supports: [paged-attention-inputs] -->`
  marker. The row for `num_active_slots` keeps the same observation that it is
  present in the signature but unreferenced in the body.
- **Technical content.** The block layout (`BLOCK_SIZE` 16, `V_OFFSET`
  `16×512×2 = 16384`, `BLOCK_BYTES` `32768`, `NUM_BLOCKS` `65536`), the
  `2 × 1024³ / 32768 = 65536` arithmetic, the `-1`/`pop_back`/`assert(false)`
  prefill branch, the `token_in_block_idx == 0` decode allocation branch, the
  `slot * N_LAYERS * MAX_BLOCKS_PER_SEQ + layer * MAX_BLOCKS_PER_SEQ +
  block_idx` index, the `blockIdx.x`/`blockIdx.y`/`threadIdx.x` mapping, the
  `<<<dim3(num_active_slots, NUM_Q_HEADS), HEAD_DIM>>>` launch, the
  `kv_head_idx = q_head_id / GQA_Q_TO_K_RATIO` GQA derivation (32 Q heads, 8 K
  heads, ratio 4), the two-warp `__shfl_down_sync` reduction at offsets
  16/8/4/2/1 with `SQRT_HEAD_DIM = 8`, the online softmax update with
  `correction_factor = expf(current_max - new_max)` and `acc / d` output, and
  the two distinct index spaces (actual `slot` for the block table vs.
  `active_slot` for Q/output) are all translated without drift.
- **Scope, limitations, framing.** The non-coverage delegation list (parts 7, 5,
  3, 4, 2), the GPU/CUDA-toolchain-absent caveat, the `5 code citations / 0
  tests / 0 runs` minimum evidence, the unverified index arithmetic and
  `HEAD_DIM = 64` assumption, the static nature of the in-place claim, the
  per-layer 4096-entry H2D copy and unresolved TODO, the unreferenced
  `num_active_slots`, the unverified online-softmax reference URL, and the gated
  `meta-llama/Llama-3.2-1B-Instruct` follow-up are all carried over exactly. No
  performance figure is present, so nothing can be read as a measured value.
- **Minor phrasing observation (non-blocking).** The sentence "The minimum
  evidence for this chapter is 5 code citations, 0 tests, and 0 runs" renders
  `이 장의 최소 근거는 코드 인용 5건, 테스트 0건, 실행 0건이다` and is accurate;
  the parallel `이 장은 block table 과 paged attention 설명을 소유하는 유일한
  장이다` → "This chapter is the sole owner of the block table and the paged
  attention explanation" preserves the ownership claim. Neither alters a
  citation, number, or technical claim.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered, and no translation-introduced error was found. The Korean source's
one precision observation about `embeddingGatherKernelDecode` (raised as O1 in
`ko-06.md`) is faithfully mirrored in the English and is not a translation
defect.
