# Review · en-08.md vs ko-08.md

## Decision

PASS

## Fidelity

The English translation is a faithful, paragraph-level rendering of the Korean
source. Every section, table, and diagram is preserved with equivalent content
and ordering:

- **Scope**: single target file `src/main.cpp` (`series.yaml:25-27`), Claims
  9/10 owned + Claim 6 by reference, evidence ranges
  (`main.cpp:726-737`/`claims.md:90-100`, `:876`/`:552`/`:1030`/
  `claims.md:102-110`, `:851-873`) — all match.
- **Slot release**: EOT/EOT_ID 128001/128009 or `MAX_SEQ_LEN-1`
  (`:1015-1037`, `trace.md:169-171`), the 4-row release table (`:1017`,
  `:1025`, `:1026`, `:1030`) — all match.
- **Active reconstruction**: `:722-737`, prefill after clearing
  `generated_tokens` (`:732-733`), immediate inclusion of the prefilled slot
  (`:735-736`, `trace.md:135/137`) — all match.
- **Batch width**: `BATCH_SIZE=2` (`:29`; `ko-07.md:140-141`), break/continue
  at `:739-744`/`:745` (`trace.md:204-205`) — all match.
- **Scheduler section**: no scheduler object/priority/preemption, queue
  (`:584-594`), occupancy (`:597`), prefill (`:150-553`), initial prefill loop
  (`:695-708`), reconstruction rule (`:722-737`), `briefs.md:182`,
  `guide.md:7-9` — all match.
- **KV scatter / Claim 10**: `seq_len / BLOCK_SIZE` and `seq_len % BLOCK_SIZE`
  (`:856-857`), first-token block pop (`:859-865`), D2D write (`:866-872`),
  per-layer full H2D copy `:876` → `pagedAttention` `:878`, copy size
  2×16×128×4 bytes (16KiB) with constants `:38, :15, :36`,
  `kernels.cu:464-470/480/525-527`, extra syncs at `:552`/`:1030`, TODO comment
  at `:551` — all match.
- **Verification / not covered / limitations / summary**: `test_softmax.cu`
  (`recon.md:12`), `recon.md:4`, `claims.md:100`, `:739-746`,
  `MAX_NEW_TOKENS_GENERATED`(`:12`), defensive branch `:745` with
  `trace.md:204-205`, all five limitation bullets, the ASCII timeline and the
  Claim 10 layer-loop diagram — all match.

Source citations were verified against the evidence files
(`evidence/claims.md:90-100`/`:102-110`, `evidence/trace.md:135/137/169-171/204-205`)
and are correct. No invented facts were introduced.

Minor points that do not affect fidelity:

- The cross-reference `ko-07.md:140-141` (`en-08.md:81`) is preserved
  verbatim. This is correct: the cited content lives in the Korean article —
  `en-07.md` is only 122 lines and has no `:140-141`, so rewriting the citation
  to `en-07.md` would break it.
- One line wrap in the "No performance claims" limitation splits the subject
  and verb across lines (`en-08.md:223-224`) but reads correctly.

Terminology is consistently rendered (prefill/decode/slot/block
table/free_blocks), and code identifiers, token IDs, and commit-line citations
are preserved verbatim.

## Required fixes

None.