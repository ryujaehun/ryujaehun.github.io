# en-02 review — Weight Loading and GPU Buffer Design

Reviewed `articles/en-02.md` against the Korean source `articles/ko-02.md`, and
checked every citation against the checkout at the fixed revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf` (`src/main.cpp`).

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the technical content, size arithmetic, aliasing
claims, scope delegation, and limitations match the Korean exactly. All line
citations were verified against the pinned tree and are correct. No
translation-introduced error was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 9 sections of the Korean are present and
  in order: question, place in the entry sequence, safetensors upload, the 16
  buffers, size arithmetic, aliasing, paged KV cache/slot layout, non-coverage,
  limitations, sources. The heading titles translate the Korean headings
  directly (`가중치 적재와 GPU 버퍼 설계` → "Weight Loading and GPU Buffer
  Design"; `미리 잡아 두는 버퍼 16개` → "The 16 Preallocated Buffers"). No
  paragraph is omitted or added.
- **Citations preserved.** All source ranges in the Korean appear identically in
  the English, including `12-38`, `40-62`, `64-77`, `79-147`, `555-610`,
  `617-693`, and the source-list entries `132-145`, `177`, `301-343`,
  `377-399`, `470`, `552`. The `<!-- visual: buffer-allocation-table ... -->`
  marker is retained verbatim.
- **Citations verified against source.** Confirmed in the pinned tree:
  `checkGPUStatus` `40-62`; `Weights` struct `64-77` (two single pointers plus
  nine per-layer arrays, `N_LAYERS=16` at line 15); `loadWeights` four steps
  (`81-84`, `87`, `88-93`, `96-97`, `99-101`, `104`, `106-111`, `112-117`);
  single `cudaMalloc`/H2D `120-121`, `123-125`, `127`; pointer mapping
  `132-145`; `main` init `557-563`, `566-569`, `572`, `574-581`, `583-594`,
  `597-610`, `617-693`, `695-708`. All 16 buffer rows match their
  `cudaMalloc`/host-vector lines (`617-618`, `619-620`, `622-623`, `625-626`,
  `628-629`, `635-636`, `638-639`, `647-648`, `656-657`, `662-663`, `667-668`,
  `676-677`, `681-682`, `684-685`, `689-690`, `692-693`).
- **Aliasing claims.** `buf_2048_1` q_proj (`177`) → per-head Q read (`301-327`,
  `304`) → `attn_scores_v` (`343`); `buf_2048_2` o_proj (`377`) → residual
  (`399`) → down (`470`); decode reuse of `buf_2048_1` (`878`). The Korean
  qualification that this safety is a static claim about consumption order with
  no runtime verification is preserved.
- **Paged KV cache and slot layout.** `kv_cache` 2GB (`575-576`, constant `35`),
  `free_blocks` 0..65535 (`577-578`, constant `37`), `block_table` initialized to
  `-1` (`579`), `block_table_gpu` (`580-581`), first H2D at end of `prefill`
  (`552`); `is_slot_free` (`597`), token/length state (`599-601`),
  `active_slots`/`active_tokens` (`604-605`), `gpu_active_slots`/`gpu_seq_lens`
  (`607-610`). All match the Korean.
- **Size arithmetic.** `MAX_BUFFER_SIZE = max(512, 2) = 512` (`31`);
  `prefill_attn_scores` `512×512×32 = 8,388,608` elements / 16 MiB; `gate`/`up`
  `512×8192 = 4,194,304` / 8 MiB; `embed_proj` `512×128,256 = 65,667,072` /
  125.25 MiB; ratio `≈ 7.83`; K/V temp `512×512` / 512 KiB; decode batch
  `2×512` / 2 KiB; `gpu_input_tokens` 2048 B; `gpu_last_tokens` 8 B. Identical to
  the Korean and correctly recomputed from the constants.
- **Scope, limitations, performance framing.** The non-coverage list, the
  GPU/CUDA-toolchain-absent caveat, the `code 3 / tests 0 / runs 0` minimum
  evidence, the gated-model constraint, the hardcoded-path/struct assumption, the
  TODO constants (`28-31`), and the statement that table bytes are compile-time
  estimates rather than measured values are all carried over exactly.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered.
