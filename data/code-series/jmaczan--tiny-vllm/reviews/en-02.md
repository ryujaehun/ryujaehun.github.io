# Review: en-02.md

## Decision

PASS

## Fidelity

The English translation is faithful to the Korean source paragraph by paragraph. All technical content, structure, and emphasis are preserved, including:

- Every source citation was verified against the actual `src/main.cpp` at commit `e25bf19` and is accurate: `loadWeights` (79-147), `checkGPUStatus` (40-62), error return (44-48), file open (87-93), header read (96-101), JSON offset loop (103-118), single `cudaMalloc`/read/copy (120-127), `Weights` struct (64-77), mapping loop (132-145), constants (16-31), `main()` opening (557-569), RoPE (572), KV cache allocator (574-581), request queue (583-594), batch state (597-610), all 16 table entries (617-618 … 692-693), argmax block (529-543), attention-score loop (301-327), aliasing lines (177, 343, 377, 470), O-projection cuBLAS (378-396), `residualAdd` (399), `pagedAttention` (878).
- All numbers carry over correctly: 2 + 9×16 = 146 tensors; `MAX_BUFFER_SIZE` = max(512, 2) = 512; the hand-computed byte counts (16 MiB, 8 MiB, 125.25 MiB, 2 KiB) match the table and the constants.
- Code blocks are identical to the source excerpts in the Korean version.
- The `Weights` struct field order, the bfloat16 description, the alias-safety caveat, and the "Limits of this chapter" disclosures all match the original meaning.
- Table role descriptions and the prefill-vs-decode buffer contrast are translated accurately.

## Required fixes

None. All citations verified correct and no factual or fidelity errors found.

Optional style nit (non-blocking): "When slots empty and refill is chapter 7's subject" renders the Korean "슬롯이 언제 비고 채워지는지는" awkwardly; something like "When slots free up and get filled is chapter 7's subject" would read more naturally, but meaning is intact.