# en-02.md 리뷰

검토 기준: 한국어 원본 `articles/ko-02.md`와 영어 번역 `articles/en-02.md`를
대조해 충실도·누락·오역을 검사했고, 고정 커밋
`1896ff5c37a241050dbcd9527caf9dc1d3087a61`의 실제 소스(`src/main.cpp`,
`include/json.hpp`)와 `evidence/claims.md`, `evidence/trace.md`,
`evidence/recon.md`, `guide.md`, `series.yaml`, `inventory/files.jsonl`로
번역이 인용한 모든 줄 번호를 일일이 대조했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 문단·번호 목록·표·ASCII 다이어그램까지 빠짐없이
옮겼으며, 의미 왜곡·누락·오역이 발견되지 않았다. 번역이 인용한 모든 소스
경로·줄 범위와 근거 문서 참조가 고정 커밋 소스 및 근거 문서와 일치한다.
한국어 리뷰(`ko-02.md`)가 지적한 5건의 인용 정밀도 문제는 원본(`ko-02.md`)에
이미 존재하는 내용으로 번역이 그대로 옮긴 것이며, 번역 오류가 아니다.

## Fidelity

**구조·범위** — 9개 절(제목·서문·파일과 범위·safetensors 형식·loadWeights
9단계·오프셋→포인터·검증 경로·다루지 않는 것·한계·정리)이 모두 존재하고 순서가
원본과 일치한다. 서문 2문단, 형식 도식 2줄, 형식 불릿 3개, `loadWeights` 번호
목록 9개, 역할 표 6행, "다루지 않는 것" 불릿 4개, "한계" 불릿 6개, 정리 2문단이
원본과 1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`loadWeights`, `checkGPUStatus`, `cudaMalloc`,
`model_weights`, `max_offset`, `data_offsets`, `__metadata__`, `embed_tokens`,
`norm`, `input_layernorm`, `post_attention_layernorm`, `mlp_*`, `w_k/w_o/w_q/w_v`,
`model.safetensors`, `tests/test_softmax.cu`)가 원문 그대로 보존되었다.
`Claim 1`, 장 번호(3·4·5·10장), `guide.md`/`series.yaml`/`evidence/*.md` 인용
표기도 동일하다.

**인용 대조 (고정 커밋 소스로 직접 확인)** — 다음이 모두 정확했다.

- `loadWeights` 경계 `src/main.cpp:79-147`, 실패 시 `main`의 `return 1`
  지점 `:566-569`(실제 `:566` 조건, `:568` 반환) ✓
- 8바이트 헤더 길이 `uint64_t` 읽기 `:96-97`, JSON 헤더 읽기 `:99-101`,
  파싱 `json::parse(header)` `:104` ✓
- 이름→오프셋 맵 `:103-118`, `data_offsets[1]` 최대치 `max_offset`
  `:105-115`, `__metadata__` `continue` `:108-111`, `data_offsets[0]` 저장
  `:117` ✓
- `cudaMalloc(&model_weights, max_offset)` `:120-121`, CPU `std::vector<char>`
  `:123-125`, H2D `cudaMemcpy` `:127`, 오프셋 기반 포인터 배정 `:132-145` ✓
- `checkGPUStatus` 정의 `:40-62`, 디바이스 0개면 `1` 반환 `:44-48`,
  `loadWeights` 진입 `:81-84` ✓
- `embed_tokens`가 로짓 GEMM의 전치 대상(`CUBLAS_OP_T`, `:510`·`:516`)으로
  등장 `:509-527` ✓
- `series.yaml:7-9`의 2장 대상 파일 `src/main.cpp`·`include/json.hpp` ✓
- `guide.md:11-16`(소스 경로·줄 범위 인용, README는 보조 근거), 3장 이후 챕터
  매핑(`series.yaml`) ✓
- `evidence/recon.md:15`(json.hpp 25830줄), `:12`(유일 테스트
  `tests/test_softmax.cu`), `:4`(저장소 미실행) ✓
- `evidence/claims.md:8`(1회 H2D 복사 요약), `:13`(배정 목록), `:14`(json.hpp
  범위 밖) ✓
- `evidence/trace.md:44-46`(`post_attention_layernorm` 추가 기록) ✓

**원본과 동일하게 번역으로 이월된 표현상 정밀도 사항** (번역 오류 아님,
`ko-02.md` Problems 1~5와 동일):

1. JSON 파싱 인용 `:99-101` — 실제 파싱은 `:104`이고 `:99-101`은 헤더 읽기다.
2. 역할 표의 근거 열이 전부 `src/main.cpp:132-145` — 역할 자체는 정확하나
   사용 지점(`:166`, `:401`, `:494`, `:509-527`)을 함께 인용하면 더 충실하다.
3. safetensors 형식 서술(`data_offsets` 반열림 구간, `__metadata__`가 텐서가
   아니라는 설명)이 소스 인용 없는 형식 해석이다. 한계 절에서 형식 해석임을
   밝히므로 허용된다.
4. 레이아웃 도식에 `max_offset` 경계 라벨이 없다. 텍스트가 이를 설명한다.
5. 5단계의 "각 텐서의 `data_offsets`를 모아 맵을 만든다"는 표현이 맵에 시작
   오프셋만 저장된다는 점에서 다소 넓다.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다.

- (선택) 원본의 인용 정밀도 문제(위 1~5)를 해결하려면 원본(`ko-02.md`)을 먼저
  수정하고 번역을 재동기화할 것. 이 리뷰는 번역 충실도만 판정하며 원본
  내용의 오류로 보지 않는다.
