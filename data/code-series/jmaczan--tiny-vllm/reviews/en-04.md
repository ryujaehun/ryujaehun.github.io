# en-04.md 리뷰

검토 기준: 한국어 원본 `articles/ko-04.md`와 영어 번역 `articles/en-04.md`를
대조해 충실도·누락·오역을 검사했고, 번역이 인용한 모든 소스 경로·줄 범위와
`evidence/claims.md`, `evidence/trace.md`, `evidence/recon.md`, `guide.md`,
`series.yaml` 참조가 한국어 원본과 1:1로 일치하는지 일일이 대조했다. 고정
커밋 소스(`src/main.cpp`, `src/kernels.cu`)의 줄 범위 자체는 원본(`ko-04.md`)이
세운 것과 동일하므로, 번역 검토는 이를 그대로 옮겼는지에 집중했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 절·다이어그램·불릿까지 빠짐없이 옮겼으며, 의미
왜곡·누락·오역이 발견되지 않았다. 번역이 인용한 모든 소스 경로·줄 범위와 근거
문서 참조가 한국어 원본과 정확히 일치한다. 원본의 괄호 표기 실수가 번역에서
깔끔하게 정리된 곳이 한 군데 있으나, 내용 변화가 없는 표기상의 정리일 뿐이다.

## Fidelity

**구조·범위** — 13개 절(제목·서문·파일과 범위·레이어 루프 한 바퀴·cuBLAS
전치 트릭·rmsNorm·Q/K/V 투영·RoPE·attention 자리와 GQA 상수·o_proj/residual/
SwiGLU MLP·최종 rmsNorm과 로짓 GEMM·검증 경로·다루지 않는 것·한계·정리)이 모두
존재하고 순서가 원본과 일치한다. 레이어 순서 ASCII 다이어그램 1개, 전치 트릭
다이어그램 1개, "다루지 않는 것" 불릿 6개, "한계" 불릿 6개, 정리 2문단이
원본과 1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`hidden_state`, `embeddingGather`, `rmsNorm`,
`rms_norms`, `q_proj`, `k_proj_temp_buf`, `v_proj_temp_buf`, `buf_2048_1`,
`buf_2048_2`, `attn_scores_v`, `causalMask`, `softmax`, `rope`, `silu`,
`residualAdd`, `o_proj`, `embed_tokens`, `cublasGemmEx`, `CUBLAS_OP_T`,
`CUBLAS_OP_N`, `CUBLAS_COMPUTE_32F`, `init_rope_frequencies`,
`ropeKernel_llama3`, `d_cos_table`, `d_sin_table`, `GQA_Q_TO_K_RATIO`,
`GQA_ATTN_SCORES_TO_V_RATIO`, `attn_alpha`, `input_layernorm`,
`post_attention_layernorm`, `norm`, `HEAD_DIM`, `KV_DIM`, `EMBEDDING_LENGTH`,
`HIDDEN_DIM`, `VOCAB_SIZE`, `MAX_SEQ_LEN`, `tests/test_softmax.cu`,
`python/tokenizer.py`, `python/rms_norm.py`, `model.safetensors`)가 원문 그대로
보존되었다. Claim 2/3/7, 장 번호(2·3·5·7·8·10장), `guide.md`/`series.yaml`/
`evidence/*.md` 인용 표기도 동일하다.

**인용 대조 (원본과 번역의 1:1 대조)** — 다음이 모두 번역에서 그대로
유지되었다.

- 레이어 루프 `src/main.cpp:164-493`, `prefill` 경계 `:150-553`, 임베딩 수집
  `:158`, `:160-163`, 최종 정리 `:494-548` ✓
- 전치 트릭 주석 `:168-176`, Q 투영 `:177-196`, 로짓 GEMM `:509-527`, 소스
  주석 `:496-507` ✓
- rmsNorm 호출 `:166`/`:401`/`:494`, 가중치 `:136`/`:140`/`:133` ✓
- K/V 투영 `:201-219`, `:222-240`, 버퍼 `buf_2048_1` `:628-629`,
  `k_proj_temp_buf` `:636`, `v_proj_temp_buf` `:639` ✓
- RoPE 호출 `:244-245`, `init_rope_frequencies` 호출 `:572` ✓
- GQA 상수 `:20-24`, `k_head_idx` `:303`, `v_head_idx` `:346`,
  `attn_alpha` `:649` ✓
- 스코어 GEMM `:301-327`, `causalMask` `:329`, `softmax` `:331`, scores×V
  `:343-371`, KV 산포 `:247-288` ✓
- o_proj `:377-396`, `residualAdd` `:399`/`:492`, post-attn rmsNorm `:401`,
  gate `:414-432`, up `:435-453`, silu `:459`, down `:470-489` ✓
- argmax `:533-543`, 출력 `:544`, 상태 갱신 `:546-548` ✓
- 커널 위치 `src/kernels.cu:55-81`/`:84-94`(rmsNorm), `:173-200`/`:202-222`(rope),
  `:224-237`/`:239-255`(causalMask), `:257-290`/`:293-309`(softmax),
  `:311-316`/`:319-329`(residualAdd), `:331-338`/`:341-344`(silu),
  `:100-124`/`:129-151`(NTK·표), `:22-24`(전역 포인터), `:96-98`(파라미터 순서),
  `:241-244`/`:295-298`(가드) ✓
- 근거 문서 `evidence/claims.md:18,22,23,24,28,35,71,74-76,78`,
  `evidence/trace.md:74-75,79-80,94,95,96-99,108-110,118-121`,
  `evidence/recon.md:4,12,38`, `guide.md:11-16`, `series.yaml:13-15` ✓

**원본 표기의 정리가 번역에서 이루어진 곳** (번역 오류 아님):

- 원본 "한계" 절 앞 문장에 "정의된다(`src/main.cpp:649`), `evidence/trace.md:
  108-110`)."처럼 괄호가 어긋난 표기가 있었다. 번역은 "(`src/main.cpp:649`,
  `evidence/trace.md:108-110`)"로 정리했는데, 인용 대상 두 개가 모두 유지되어
  내용 변화가 없다. 정리 방향이 자연스럽고 적절하다.

**번역 표현 품질** — 기술 용어의 번역이 일관적이다("전치 트릭" →
"transpose trick", "KV 산포" → "KV scatter", "헤드 구조" → "head structure",
"행-주/열-주" → "row-major/column-major", "추적 메모" → "trace memo", "이월된
표현" 없음). "그대로 인자로 옮긴다", "짧게만 확인한" 등 원문의 뉘앙스가
보존되었다. `Q^T = w_q^T * inputs`, `logits^T = embed_tokens * rms_norms^T`
등 수식 표기도 원문과 동일하다.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다.

- (선택) 괄호 표기 정리(`:649`와 `trace.md:108-110` 병기)는 원본을 먼저
  고치지 않아도 되는 번역측 개선이다. 그대로 두어도 무방하다.