# en-04 review — cuBLAS matrix multiplication and the column-major/row-major transpose trick

Reviewed `articles/en-04.md` against the Korean source `articles/ko-04.md`. The
review is a translation-fidelity check: it verifies that the English is a
complete, unaltered rendering of the Korean, and that every source citation in
the English matches the Korean. Every citation was additionally checked against
the pinned tree at revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf`
(`src/main.cpp`). The Korean source's own factual accuracy is covered by the
companion `ko-04.md` review and is not re-adjudicated here.

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, and a programmatic diff of
all `main.cpp:*` citation tokens between the two files shows them identical
(86 tokens each). The Q projection code block is verbatim, and the Mermaid
diagram with its `<!-- visual: column-row-major-trick supports:
[column-row-major-trick] -->` marker is retained in structure and content. No
translation-introduced factual error, dropped citation, or altered claim was
found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 13 sections of the Korean are present and
  in order: question, row-major data vs. column-major cuBLAS, the common
  structure from the transpose-flags pair, Q projection, K/V projection,
  attention scores, scores×V, O projection/SwiGLU/logits, alpha/beta rejection,
  visual, non-coverage, limitations, and sources. Headings translate the Korean
  directly (`전치 플래그 한 쌍이 만드는 공통 구조` → "The common structure built
  by one pair of transpose flags"; `alpha 와 beta: 잔차는 GEMM 이 아니라 커널이
  한다` → "alpha and beta: the residual is handled by a kernel, not the GEMM").
  No paragraph is omitted or added.
- **Citations preserved.** Every source range in the Korean appears identically
  in the English, including the Q comment (`168-176`, `168-169`, `170`,
  `170-172`, `171-175`, `171-176`), the Q call (`178-196`, `179`, `180`,
  `181-183`, `184`, `187`, `190`, `191`, `194`), K/V (`198-200`, `201-219`,
  `204`, `208`, `215`, `217`, `222-240`, `229`, `236`), attention scores
  (`301-327`, `303`, `309-310`, `311-313`, `317`, `320`, `324`), scores×V
  (`343`, `343-371`, `349`, `350`, `352-370`, `353-354`, `355-357`, `361`,
  `364`, `368`), O/SwiGLU/logits (`373-396`, `376`, `378-396`, `381`, `394`,
  `404-432`, `414-432`, `417`, `430`, `434-453`, `438`, `451`, `461-489`,
  `466`, `471-489`, `474`, `480`, `483`, `487`, `496-527`, `498-501`,
  `509-527`, `512`, `525`), residual (`399`, `492`), alpha/beta (`631-679`,
  `631`, `632`, `642`, `645`, `649`, `650`, `654`, `660`, `665`, `670`, `674`,
  `679`), decode (`765-783`), and the dimension constants (`16-25`, `16`, `18`,
  `19`). The `main.cpp:185` citation for the Q weight's row spacing is carried
  over unchanged from the Korean.
- **Citations verified against source.** The anchors match the pinned tree:
  `16`/`18`/`19` are `EMBEDDING_LENGTH`/`KV_DIM`/`HEAD_DIM`; `168-176` is the
  transpose-trick comment including the quoted `C = A * B == C^T = B^T * A^T`
  (line 170) and the "no need to transpose Q^T back to Q" reasoning (lines
  173-175); `178-196` is the Q `cublasGemmEx` with `OP_T`/`OP_N` (179/180),
  `m = n = k = 2048` (181-183), `lda = ldb = ldc = 2048` (187/190/194), and
  `q_proj_alpha`/`q_proj_beta` (184/191); `201-219`/`222-240` are the K/V calls
  with `m = ldc = KV_DIM` (204/217, 225/238) and outputs `k_proj_temp_buf`
  (215)/`v_proj_temp_buf` (236); `301-327` is the per-head attention call with
  `k_head_idx = i / GQA_Q_TO_K_RATIO` (303), `m = n = prompt_len`,
  `k = HEAD_DIM` (311-313), `lda = KV_DIM` (317), `ldb = EMBEDDING_LENGTH`
  (320), `ldc = prompt_len` (324), and `attn_alpha = 1.0f/8.0f` (649);
  `352-370` is scores×V with `OP_N`/`OP_N` (353-354), `m = HEAD_DIM`,
  `n = k = prompt_len` (355-357), `lda = KV_DIM` (361), `ldb = prompt_len`
  (364), `ldc = EMBEDDING_LENGTH` (368), `v_head` (349) and output
  `attn_scores_v + i * HEAD_DIM` (350); `378-396` is O with `m = ldc = 2048`
  (381/394) and the "same as Q projection, so copy paste" comment (376); gate
  `417`/`430`, up `438`/`451` are `HIDDEN_DIM`, down `474`/`480`/`483`/`487`
  match `m`/`lda`/`ldb`/`ldc` with the quoted comment at 466, logits `512`/`525`
  are `VOCAB_SIZE` with the correction comment at `498-501`; all ten betas
  (`632`, `642`, `645`, `650`, `654`, `660`, `665`, `670`, `674`, `679`) are
  `0.0f` and all ten alphas (`631`, `641`, `644`, `649`, `653`, `659`, `664`,
  `669`, `673`, `678`) are `1.0f` except `attn_alpha = 1.0f/8.0f` (649);
  `residualAdd` calls sit at `399` and `492`; the decode Q call with `n =
  num_active_slots` occupies `765-783`; and the ten `*_status` declarations sit
  at `178`, `201`, `222`, `308`, `352`, `378`, `414`, `435`, `471`, `509`.
- **Mermaid diagram.** `column-row-major-trick` retains all five nodes (A–E)
  with the same labels, the same `main.cpp` anchors (`168-169`, `169`, `170`,
  `179`, `180`, `171-176`), and the same Korean-derived node text translated
  without drift (`행 우선 저장` → "Stored row-major", `열 우선으로 본 전치를 되돌림`
  → "undoes the transposition seen column-major"). The
  `<!-- visual: column-row-major-trick supports: [column-row-major-trick] -->`
  marker is carried over exactly.
- **Technical content.** The row-major/column-major mismatch and the
  `(m, n)` ↔ `(n, m)` reinterpretation, the `C = A·B == C^T = B^T·A^T` identity,
  the eight-projection skeleton (`OP_T` at A, `OP_N` at B, `n = prompt_len`,
  `ldc = m`), the Q derivation via `op(A)`/`op(B)` (`w_q · rms_norms^T` written
  column-major → `Q = rms_norms · w_q^T` read row-major), the K/V
  `(KV_DIM, EMBEDDING_LENGTH) * (EMBEDDING_LENGTH, num_tokens)` comment, the
  scores×V exception where only this call has `ldc ≠ m` with the `[i*64,
  (i+1)*64)` head concatenation filling `(num_tok, 2048)`, the downstream use of
  `attn_scores_v` as the O input (`343`, `388`), the gate/up/down/logits
  repeats, and the beta = 0 / `residualAdd` rejection are all translated
  without drift.
- **Scope, limitations, framing.** The non-coverage delegation list (parts 3, 2,
  5, 6, 7), the GPU/CUDA-toolchain-absent caveat, the `3 code citations / 0
  tests / 0 executions` minimum evidence, the compile-time-constant dependence
  of the leading dimensions (`16-25`), the `Q^T = wq^T * inputs` shorthand note,
  the single `ldc ≠ m` exception (`355`, `368`), the unchecked `*_status`
  premise, and the gated `meta-llama/Llama-3.2-1B-Instruct` `model.safetensors`
  follow-up are all carried over exactly. No performance figure is present, so
  nothing can be read as a measured value.
- **Minor phrasing observation (non-blocking).** The sentence "the minimum
  evidence for this chapter is 3 code citations, 0 tests, and 0 executions"
  renders `이 장의 최소 근거는 코드 인용 3건, 테스트 0건, 실행 0건이다` and is
  accurate. The English "this chapter owns the column-major layout and leading
  dimension explanation" renders `이 장은 열 우선 레이아웃과 leading dimension
  설명을 소유한다` and preserves the ownership claim. Neither alters a citation,
  number, or technical claim.

## Required fixes

None. The translation is complete and accurate; no citation, number, or claim
was altered, and no translation-introduced error was found. The Korean source's
two precision observations about the Q-weight citation (`ko-04.md` O1) and the
strict `ld ≥ rows` wording of the leading-dimension definition (`ko-04.md` O2)
are faithfully mirrored in the English and are optional polish, not translation
defects.