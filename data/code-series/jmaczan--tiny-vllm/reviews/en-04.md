# en-04 review — The cuBLAS Transposition Trick

Reviewed `articles/en-04.md` against the Korean source `articles/ko-04.md`
paragraph by paragraph, section by section, and at the token level for every
backtick-quoted identifier, citation, constant, and URL. Source-citation
accuracy of the Korean article against the pinned revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf` was already established in
`reviews/ko-04.md` (PASS, including cross-checking every `src/main.cpp` range
against `evidence/trace.md`, `evidence/claims.md`, and the earlier `en-04.md`
line-identity verification); the task here is confirming the English renders
that Korean faithfully and carries the verified citations over unchanged.

## Decision

**PASS**

The English translation is a complete, faithful rendering of the Korean
source. All 11 sections appear in the same order with directly translated
headings, no paragraph is added or dropped, and every source citation, code
block, constant, URL, and visual marker is carried over unchanged. No
translation-introduced error was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 11 sections present and in order: the
  row-major/column-major premise, the Q projection, K/V/O/SwiGLU, logits,
  attention scores, scores×V, alpha and beta, the flow summary, further
  reading, and limitations. Heading titles translate the Korean directly
  (`행 우선 데이터와 열 우선 cuBLAS` → "Row-major data and column-major
  cuBLAS"; `Q 투영: 두 개의 플래그가 만드는 곱` → "The Q projection: the
  product built by two flags"; `점수×V: 전치 플래그가 사라진 유일한 호출` →
  "Scores×V: the only call where the transpose flags disappear";
  `이 글의 한계` → "Limitations of this article").
- **Prose fidelity.** Every technical claim reads the Korean correctly: the
  `C = A·B == C^T = B^T·A^T` identity and "transpose the call without moving
  any data"; the Q projection algebra (`op(A)=w_q` under `CUBLAS_OP_T`,
  `op(B)=rms_norms^T` under `CUBLAS_OP_N`, m = n = k = 2048 with n = token
  count, column-major C = `w_q·rms_norms^T` read row-major as
  `rms_norms·w_q^T = Q`); the "ldc = m" rule and its single exception;
  K/V's `m = ldc = KV_DIM`, gate/up's 8192, down's `lda = ldb = 8192`;
  the logits m·n-swap correction comment left "because it shows a bug in my
  thinking"; per-head scores with `k_head_idx = i / GQA_Q_TO_K_RATIO` and
  `attn_alpha = 1/8`; the two-operand "slice of a buffer" reasoning for
  attention lda/ldb; the unique all-`CUBLAS_OP_N` scores×V call with
  column-major C = `V^T·S^T` read row-major as `S·V`; the lone `ldc ≠ m`
  (ldc = 2048) with head i's 64 slots landing in `[i*64, i*64+64)`; and the
  ten alpha·beta pairs (q_proj `631-632`, k_proj `641-642`, v_proj `644-645`,
  attn `649-650`, attn_scores_v `653-654`, o_proj `659-660`, gate `664-665`,
  up `669-670`, down `673-674`, embed `678-679`) with every beta 0.0 and the
  single non-1 alpha `1/8`, residuals left to `residualAdd`(`399`, `492`).
  Numeric values, identifiers, and constants are all preserved verbatim.
- **Citations preserved and unchanged.** A token-level comparison shows every
  citation string byte-identical to the Korean (32 occurrences, same set in
  both files): `src/main.cpp:16`, `16-25`, `168-176`, `171-175`, `177-196`,
  `187`, `198-200`, `204`, `217`, `222-240`, `301-327`, `311-313`, `317`,
  `320`, `324`, `343`, `350`, `352-370`, `355-357`, `361`, `364`, `368`,
  `378-396`, `399`, `414-432`, `461-469`, `461-489`, `492`, `496-501`,
  `509-527`, `641-650`, `649`. Each range falls inside the line spans already
  verified in `reviews/ko-04.md` against `trace.md`/`claims.md` (e.g. `187`
  within `184-194`, `204`/`217` within `201-219`, `343`/`350`/`352-370`/`361`/
  `364`/`368` within the `343-371` region, the alpha·beta lines within
  `631-679`, the logits call within `509-527`). All seven source permalinks
  point at the pinned revision `e25bf1994efa90bc98b721ba7c527402f86fbeaf`,
  and the four "Further reading" URLs (Wikipedia, Paged Out!, cublasGemmEx
  reference, cuBLAS) are identical to the Korean.
- **Code and identifiers.** All eight `cpp` code blocks are byte-identical
  between the two files (code is not translated), as is every backtick-quoted
  identifier and constant (`CUBLAS_OP_T`, `CUBLAS_OP_N`, `EMBEDDING_LENGTH`,
  `KV_DIM`, `HIDDEN_DIM`, `VOCAB_SIZE`, `e25bf19`, `residualAdd`, `w_q`,
  `rms_norms`, `k_proj_temp_buf`, `q_proj`, `attn_scores_v`, etc.). One
  cosmetic addition only: the Korean writes `n 이 ... 로 바뀌는` where the
  English backticks `n` ("with only `n` changing to `num_active_slots`"); no
  identifier was dropped or altered.
- **Visual assets.** The `<!-- visual: column-row-major-trick supports:
  [column-row-major-trick] -->` marker is retained verbatim, and the mermaid
  diagram is node-for-node identical to the Korean (same five nodes A–E and
  same `main.cpp` anchors `168-169`, `169`, `170`, `179`, `180`, `171-176`)
  with only the labels translated.
- **Deferrals and cross-references.** Ownership/deferral references are
  carried over exactly: GQA's `k_head_idx` and value-head sharing to part 3,
  the decode path's `num_active_slots` contrast to part 5, and the note that
  the down projection's `gate` input already looks transposed to cuBLAS.
- **Limitations.** The not-built/not-run disclosure, the commit `e25bf19`
  source-reading basis, the "every `*_status` return is declared but never
  checked" premise, and the compile-time-constant dependency on
  `src/main.cpp:16-25` all match the Korean. No measured performance figures
  are introduced.

## Required fixes

None. The translation is complete and accurate; no citation, number, constant,
URL, code block, or claim was altered, and no content was added or omitted.
The only known observations (`GQA_ATTN_SCORES_TO_V_RATIO` identifier
cross-check and the Paged Out! homepage link) are pre-existing notes from
`reviews/ko-04.md` carried into the English unchanged; they are non-blocking
and are not translation errors.