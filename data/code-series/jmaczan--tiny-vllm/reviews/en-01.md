# en-01.md 리뷰

검토 기준: 한국어 원본 `articles/ko-01.md`와 영어 번역 `articles/en-01.md`를
대조해 충실도·누락·오역을 검사했고, 고정 커밋
`1896ff5c37a241050dbcd9527caf9dc1d3087a61`의 실제 소스(`src/main.cpp`,
`src/kernels.cu`, `src/kernels.cuh`, `CMakeLists.txt`)와
`inventory/files.jsonl`로 번역이 인용한 모든 줄 번호 인용을 일일이 대조했다.

## Decision

**PASS**

영어 번역은 한국어 원본을 문단·목록·표·다이어그램까지 빠짐없이 옮겼으며,
의미 왜곡이나 누락, 오역이 발견되지 않았다. 번역이 인용한 모든 소스 경로·줄
번호·상수 값·TODO 문구 원문·커널 가드 조건·프롬프트 토큰 수가 실제 고정 커밋
소스와 일치한다. 한국어 리뷰(ko-01.md)가 지적한 4건의 표현상 정밀도 문제는
원본에 이미 존재하는 내용으로 번역이 그대로 옮겼으며, 번역 오류가 아니다.

## Fidelity

**구조·범위** — 10개 절(제목·서문·파일과 범위·실행 상수·main 9단계·프롬프트
큐·커널 런치 가드·검증 경로·다루지 않는 것·한계·정리)이 모두 존재하고 순서가
일치한다. ASCII 다이어그램의 9단계 문구, 괄호 인용, `Claim N` 참조가 원본과
1:1로 대응한다.

**용어·고유명사** — 코드 식별자(`N_LAYERS`, `loadWeights`, `free_blocks`,
`pagedAttention` 등), TODO 주석 원문, 커널·함수 이름, `EOT/EOT_ID`,
`BATCH_SIZE=2` 등이 그대로 보존되었다. Claim 1~10, 장 번호(4~8, 10),
`guide.md`/`project.yaml`/`inventory/files.jsonl` 인용 표기도 동일하다.

**숫자·연산** — 상수 표 17행, `V_OFFSET=16384`, `BLOCK_BYTES=32768`,
`MAX_BLOCKS_PER_SEQ=128`, `NUM_BLOCKS=65536`, 프롬프트 길이 17·14·13·14,
`prefill_attn_scores=512×512×2×32`, `MAX_NEW_TOKENS_GENERATED=20`,
`END_OF_TEXT_TOKEN_ID=128001`, `EOT_ID_TOKEN_ID=128009`가 모두 원본과 일치한다.

**인용 대조 (고정 커밋 소스로 직접 확인)** — 다음이 모두 정확했다.

- `add_executable(tiny-vllm src/main.cpp src/kernels.cu)` `CMakeLists.txt:49-52` ✓
- 상수·유도값 `main.cpp:15-37`(`:12` MAX_NEW_TOKENS_GENERATED, `:15` N_LAYERS,
  `:26-27` 종료 토큰, `:28-30`, `:32-37`), `Weights` 구조체 `:64-77` ✓
- `checkGPUStatus` `:44-48`, `loadWeights` `:79-147`(디바이스 확인 `:81-84`),
  `prefill` `:150-553`(진입점 `:152-163`) ✓
- `main` `:555-1044`: cuBLAS `:557-563`, loadWeights `:566-569`,
  init_rope_frequencies `:572`(인자 500000,32,1,4,8192), KV 캐시·블록 테이블
  `:574-581`, 큐·배치 `:584-601`, 버퍼 할당 `:615-693`(`:648` prefill_attn_scores),
  초기 prefill `:695-708`, decode `:720-1039`(`:720` while, `:722-737` 재구성,
  `:739-746` break/continue), 종료 `:1040-1044` ✓
- 프롬프트 4개 토큰 수 17·14·13·14(`:585`,`:588`,`:591`,`:594`), include 목록
  `:1-8` ✓
- 커널 가드: rope `kernels.cu:204-209`(호출 `main.cpp:244-245`), causalMask
  `:241-245`·softmax `:295-299`(호출 `:329`,`:331`), ropeDecode `:390-395`
  (호출 `:847-848`), softmaxDecode `:444-448`(선언 `kernels.cuh:25`, 정의
  `:442-458`, 호출 없음), 온라인 softmax `pagedAttentionKernel:508-519` ✓
- 가드 메시지 원문("Can't launch more than 1024 threads on RTX 5090…")과
  `return` 지점(`:207`,`:243`,`:297`,`:393`,`:446`) ✓
- 상수 중복 정의(`main.cpp:12-38`/`kernels.cu:8-19`)와 TODO 인지(`kernels.cu:6`) ✓
- 인벤토리 31개 항목, 줄 수 `main.cpp` 1044(`files.jsonl:29`), `kernels.cu` 528
  (`:27`), `README.md` 1304(`:9`), `json.hpp` 25830(`:14`), 유일 테스트
  `tests/test_softmax.cu`(`:31`) ✓

**원본과 동일하게 번역으로 이월된 표현상 정밀도 사항** (번역 오류 아님):

1. 종료 범위 `:1040-1044` — `return 0`은 `:1043`, `:1044`는 닫는 괄호. 원본과
   동일하며 기능 범위로는 충분히 정확.
2. "프롬프트는 `MAX_PROMPT_LEN=512`로 상한이 걸려 있다"는 표현이 런타임
   클램프를 암시하는 듯 읽힐 수 있으나, 원본의 문구를 그대로 옮긴 것.
3. "모든 디바이스 버퍼…(:615-693)" — `gpu_active_slots`/`gpu_seq_lens`는
   `:607-610`에 할당되나, 원본과 동일한 서술.
4. 가드 메시지가 커널마다 다르다는 점 — 번역도 "for example"로 RoPE 메시지만
   인용하며 원본과 동일.

## Required fixes

없음. 번역 차원의 필수 수정 사항이 발견되지 않았다.

- (선택) 원본의 정밀도 문제(위 1~4)를 해결하려면 원본(`ko-01.md`)을 먼저
  수정하고 번역을 재동기화할 것. 이 리뷰는 번역 충실도만 판정하며 원본
  내용의 오류로 보지 않는다.