# en-01 review — Product Boundary and Execution Entry Point

Reviewed `articles/en-01.md` against the Korean source `articles/ko-01.md`, and
checked every source citation against the checkout at the fixed revision
`e25bf1994efa90bc98b721ba7c527402f86fbeaf` (`CMakeLists.txt`, the four shell
scripts, `src/cuda_to_hip.h`, `src/kernels.cuh`, `include/json.hpp`,
`src/main.cpp`, `src/kernels.cu`).

## Decision

**PASS**

The English translation is a complete and faithful rendering of the Korean
source. Every section is present in the same order, every source citation is
carried over unchanged, and the technical content, scope delegation,
terminology ownership, and limitations match the Korean exactly. All line
citations were verified against the pinned tree and are correct. No
translation-introduced error was found, and no fix is required.

## Fidelity

- **Structure and completeness.** All 8 sections of the Korean are present and
  in order: the question of the chapter, the two translation units, same
  source/two backends, the canonical execution path, where `main` begins, where
  the input comes from, what the chapter does not cover, and limitations. The
  heading titles translate the Korean headings directly (`이 장의 질문` → "The
  Question of This Chapter"; `제품으로 빌드되는 두 번역 단위` → "The Two
  Translation Units Built as the Product"; `같은 소스, 두 백엔드` → "Same
  Source, Two Backends"). No paragraph is omitted or added.
- **Citations preserved.** A token-level comparison of every backtick-quoted
  identifier between the two articles shows a single difference, the notation
  placeholder itself (`파일:시작-끝` → `file:start-end`), which is the intended
  translation of the `file:start-end` convention introduced in the opening
  paragraph. All other identifiers, ranges, and constants are byte-identical,
  including `CMakeLists.txt:49-52`, `62-63`, `54-60`, `65-69`, `71-74`,
  `src/main.cpp:555-1044`, `src/kernels.cu:32-523`, and
  `src/kernels.cu:489-493`. The two `<!-- visual: ... -->` markers
  (`product-translation-units`, `dual-backend-branch`) are retained verbatim.
- **Citations verified against source.** Confirmed in the pinned tree:
  `option(USE_HIP ... OFF)` (`3`), language branches `13`/`15`, nvcc `5-8`, CUDA
  standard/arch `21-25`, backend flags `32-38`, packages `42-44`/`46`, HIP
  translation units `54-60`, links `65-69`/`71-74`, include paths `62-63`;
  `build.sh:1-3`, `run.sh:1`, `test.sh:1-2`, `full_test.sh:1`; the shim
  condition `src/cuda_to_hip.h:6`, HIP headers `8-10`, bfloat16 mapping `13-14`,
  runtime macros `17-31`, BLAS macros `33-46`, CUDA headers `52-59`,
  `WARP_FULL_MASK` `48-50`/`58-59`; `src/kernels.cuh:3-9`; `src/main.cpp:1-10`,
  `40-62`, `79-147`, `150-553`, `555-1044`; the four host functions are the only
  ones; `src/kernels.cu` has exactly 11 `__global__` definitions at
  `32`, `55`, `173`, `224`, `257`, `311`, `331` and `347`, `371`, `408`, `461`.
- **Two translation units and the vendored header.** The `add_executable`
  target, the division of labor between `src/main.cpp` and `src/kernels.cu`,
  the three headers pulled in through the include path, and the classification
  of `include/json.hpp` as a third-party single header (version/license at
  `6-7`, `68-70`; ~92% of analyzed source bytes) all match the Korean and the
  checkout. The table's required claim `product-translation-units` and both of
  its elements are answered as in the Korean.
- **Backend branching and shim.** The seven-item branch list (language,
  compiler, CUDA standard/arch, flags, packages, translation-unit language,
  link) and the `src/cuda_to_hip.h` description are carried over exactly. The
  mermaid diagram is node-for-node identical to the Korean; only the labels
  `기본값`, `정의`, and `동일 소스의 cuda* · cublas* 이름` are translated
  ("default", "defined", "cuda* · cublas* names from the same source"), and the
  target lines (`CMakeLists.txt:15`, `13`, `54-60`, `65-69`, `71-74`;
  `src/cuda_to_hip.h:8-10`, `52-59`) are correct.
- **Execution path.** `full_test.sh`'s piped token sequence is exactly 42
  tokens; `test.sh` calls `build.sh` then `run.sh`; `build.sh` removes and
  recreates `build/` and runs `cmake .. -G Ninja && ninja`; `run.sh` runs
  `./build/tiny-vllm`. The clean-build observation and the deferral of the
  stdin question to the later section match the Korean.
- **`main` initialization order.** `cublasCreate` (`557-563`) → `loadWeights`
  (`566-569`) → RoPE init (`572`) → paged KV allocator (`574-581`) → queue
  (`583-594`) → slot/batch state (`597-610`) → compute buffers (`617-693`) →
  initial prefill loop (`695-708`) → `while (true)` decode loop (`720-1039`) →
  break (`738-746`) → `Ok bye!`/cleanup/`return 0` (`1040-1044`) all match the
  source. The `720-1039` range and the `695-708` range reproduce the Korean
  citations unchanged.
- **Input path and hardcoded prompts.** `main`'s signature (`555`) reads
  neither `argc`/`argv` nor standard input; the four chat-template token vectors
  are pushed at `583-594`; the four prompt comments and their lengths 17, 14,
  13, 14 sit at `583`, `587`, `590`, `593`; `loadWeights` opens
  `model.safetensors` at `87`. All verified and identical to the Korean.
- **Scope, limitations, performance framing.** The non-coverage list, the
  GPU/CUDA-toolchain-absent caveat, the `code 4 / tests 0 / runs 0` minimum
  evidence, the un-compiled HIP branch, the static nature of the stdin claim,
  and the exclusion of the vendored `json.hpp` are all carried over exactly.
  No performance figure that could be read as measured is present.

## Required fixes

None. The translation is complete and accurate; no citation, number, constant,
or claim was altered.
